# Examples

Reproducible example notebooks demonstrating the two protocol modules (Sorci N=2 and the open temporal instrument N≫2). This is a **Phase 3** deliverable; the directory is empty at Phase 0.

Each notebook will declare its parameters and reproducibility metadata and regenerate the results archived under [`../results/`](../results/).
