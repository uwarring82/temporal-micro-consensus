# Project Logbook

*A chronological, append-only record of substantive activities, decisions, and corrections in the Temporal Micro Consensus repository. It exists for **provenance and reproducibility** (the "R" of FAIR): each entry says what was done, when, by whom, why, and which artefacts/commits resulted.*

| Field | Value |
| --- | --- |
| Purpose | Provenance / audit trail (FAIR-Reusable) |
| Convention | **Append-only.** Entries are added chronologically (oldest first); existing entries are not rewritten. Corrections are logged as new entries that reference the earlier one. |
| Actors | **Steward** = U. Warring (decisions, authorship). **Agent** = Claude Code (execution under steward direction). |
| Scope | Decisions, version issuances, phase transitions, factual corrections. Routine edits are captured by the Git history, not duplicated here. |

---

## 2026-05-23 — Repository origin

- **Actor:** Steward. **Commit:** `c18af36` (Initial repository scaffold).
- Repository scaffolded under Local Stewardship: Coastline *Consensus-Emergence of Classical Proper Time* (v0.1 → v0.2), Ledger CL-2026-006 (v0.1 → v0.2), Sail *Visibility, the Right Witness* (v0.1 → v0.2), roadmap v0.1, split licensing (MIT / CC-BY-SA-4.0 / CC-BY-NC-SA-4.0).

## 2026-05-26 — Public remote established

- **Actor:** Steward (executed via Agent assistance). 
- Public GitHub remote created at `github.com/uwarring82/temporal-micro-consensus` and `main` pushed. Resolved the roadmap's "public visibility of repository" stewardship decision.

## 2026-05-26 — Literature review opened (plan v0.2, Phase 0)

- **Actor:** Steward (decisions) / Agent (drafting). **Commits:** `2030399`, `bc5ddce`.
- Established [`literature-review-plan.md`](literature-review-plan.md) (v0.2 after a Guardian-stance review): comprehensive broad-sweep field map, seven clusters, five phases, Tier-1 scope cap, CCUF-bridge lock, source-handling policy (PDFs local-only, git-ignored). Phase 0 scaffolding under [`literature/`](literature/) landed in a second commit for an auditable boundary.

## 2026-05-26 — Phase 1: corpus verified

- **Actor:** Agent (under steward direction). **Commit:** `d059320`.
- All corpus entries verified against Crossref / arXiv / ADS / publisher; `VERIFY` flags cleared; placeholders resolved (incl. `sorci2026`, `hartong2024`, `covey2025`, `fromonteil2025`, `fit2026`). Lineage flags recorded for later audit.

## 2026-05-26 — Phase 1/2: sources acquired, extraction notes

- **Actor:** Steward (PDF acquisition) / Agent (extraction). **Commits:** `932e6ea`, `78d3653`.
- 29 PDFs acquired into local-only `sources/`, renamed to citekeys; one findings note per paper (own-words summaries — copyright). `zych2019` added as the composite-system-action source behind `sorci2026`.

## 2026-05-26 — Phase 2: Tier-1 fixed at 13, full annotation

- **Actor:** Steward (down-tiering call) / Agent (annotation). **Commit:** `53127f5`.
- Tier 1 reduced 18 → 13; six papers down-tiered with rationale. The 13 Tier-1 notes upgraded to the full per-claim Phase-2 template. Structural-finding evaluation recorded (`hartong2024`: no v0.3 *plan* revision required; artefact-level fix logged).

## 2026-05-26 — Phase 3: synthesis

- **Actor:** Agent (under steward direction). **Commit:** `d6f00c3`.
- [`literature/synthesis-v0.1.md`](literature/synthesis-v0.1.md) committed: claim-by-claim, measure-by-measure, gaps/falsifications, lineage audit. **Headline finding:** the configurational→temporal transplant (Claim II redundancy) has no temporal exemplar in the corpus.

## 2026-05-26 — Coastline v0.3 issued

- **Actor:** Steward (explicit go) / Agent (drafting). **Commit:** `a2d037f`.
- [`coastlines/consensus-emergence-v0.3.md`](../coastlines/consensus-emergence-v0.3.md) issued (v0.2 frozen). Lineage fixes (Hartong removed, Mendes 2018→2019, Ollivier–Poulin–Zurek precursor added); transplant gap registered (Claim II paragraph, Anti-Claim #6, Deferred priority); "multipartite ≠ redundant" added; Claim III softened. FIT / GR-boundary / measure-registration deferred.

## 2026-05-26 — FAIR principles adopted; logbook established

- **Actor:** Steward (direction) / Agent (execution). **Commit:** *(this change).*
- Adopted FAIR principles (see [README §FAIR](../README.md)): added machine-readable citation metadata ([`CITATION.cff`](../CITATION.cff)) and established this logbook. Next step toward full *Findability* is a tagged release minted to a Zenodo DOI (requires steward action: connect Zenodo to the GitHub repo, then publish a release; optionally add the steward's ORCID to `CITATION.cff`).

## 2026-05-26 — Correction: no steward relationship with C. Sanner

- **Actor:** Steward (factual correction). **Commit:** *(this change).*
- The steward reports having **never met or directly interacted with C. Sanner**. This corrects an earlier inference (logged 2026-05-26, Phase 3) that treated both Leibfried and Sanner as "close contacts" of the steward.
- **Effect:** The Sail's Acknowledgements (`sails/sorci-commentary-v0.2.md`) state "ongoing exchanges" with C. Sanner — this is **factually inaccurate** and is flagged for correction. The genuine prior association is with **D. Leibfried** (discussions at NIST Boulder, 2010–2012). Both Leibfried and Sanner are co-authors of the subject paper (Sorci et al.); the independence-of-commentary consideration for any external circulation therefore concerns the steward's prior association with **Leibfried** alone.
- **Propagated:** `literature/synthesis-v0.1.md` §4 and `roadmap.md` (external-circulation item) corrected. **Pending:** the Sail acknowledgement itself awaits the steward's decision on correction (remove the Sanner clause / reformulate / handle in the Phase-4 Sail pass).

## 2026-05-26 — Sail v0.3 issued (Sanner acknowledgement removed)

- **Actor:** Steward (decision: minimal factual correction) / Agent (drafting). **Commit:** *(this change).*
- Resolves the "Pending" item in the entry above. [`sails/sorci-commentary-v0.3.md`](../sails/sorci-commentary-v0.3.md) issued (v0.2 frozen); the only changes versus v0.2 are the metadata block, the removal of the C. Sanner acknowledgement clause (the steward has no relationship with Sanner), and the version-history row. The D. Leibfried acknowledgement is retained.
- **Deliberately *not* changed:** anchoring remains Coastline **v0.2** / Ledger CL-2026-006 v0.2 (re-anchoring to Coastline v0.3 is a substantive change), and the broader acknowledgement decision (retain / reformulate / remove) remains open — both deferred to the Phase-4 Sail pass.

## 2026-05-26 — Phase 4 step 1: Ledger CL-2026-007 opened

- **Actor:** Steward (direction: "go step by step" through Phase 4) / Agent (drafting). **Commit:** *(this change).*
- [`ledger/CL-2026-007-smith-ahmadi-v0.1.md`](../ledger/CL-2026-007-smith-ahmadi-v0.1.md) issued (v0.1), classifying Smith & Ahmadi (2020) **UNDERDETERMINED** against **Coastline v0.3**. Declared measure: Fisher information F[τ] (deliberately distinct from CL-2026-006's mutual information, road-testing measure pluralism). Crux/discriminant: whether the quantum time-dilation correction is coherence-dependent (superposition ≠ classical mixture) — the coherence-sensitive test it lacks is what the Sorci visibility witness (CL-2026-006) provides.
- Roadmap "possible future Ledger entries" updated (CL-2026-007 → opened); README Ledger mention + structure tree updated. **Next steps (Phase 4):** CL-2026-008 (distributed networks); Sail re-anchor to v0.3; methodological notes.

## 2026-05-26 — CL-2026-007 corrected to v0.2 (Verifier catch)

- **Actor:** Verifier review (catch) / Steward (decision) / Agent (correction + source re-read). **Commit:** *(this change).*
- A Verifier-stance review flagged that CL-2026-007 v0.1's central rationale was **physically wrong**: v0.1 claimed Smith & Ahmadi do not settle whether the quantum time-dilation correction is coherence-dependent. Re-reading the source PDF (Eqs. 22–25, Figs. 1–2) confirmed the Verifier: `γ_C⁻¹` (Eq. 24) is explicitly the classical *statistical-mixture* value and `γ_Q⁻¹` (Eq. 25) is the *coherence-dependent* quantum term that vanishes for a mixture (θ ∈ {0,π/2} or p̄_A = p̄'_A). The theoretical superposition-vs-mixture distinction is therefore established by the paper; v0.1 had the compressibility backwards.
- **Correction:** [`ledger/CL-2026-007-smith-ahmadi-v0.2.md`](../ledger/CL-2026-007-smith-ahmadi-v0.2.md) issued (v0.1 frozen). Classification unchanged (UNDERDETERMINED) but the crux is **relocated** from the theoretical level to the **experimental-witness** level (isolating γ_Q⁻¹ from preparation/dephasing/verification nuisances) — the same location as CL-2026-006. Discriminant conditions rewritten as experimental nuisance-isolation tests; the "Fisher stable across anchors" overclaim was withdrawn (Fisher retained only as the registered resolution anchor, with a caveat that the coherence content lives in the cross-probe γ_Q⁻¹).
- **Lesson logged:** classify framework-relative claims only after reading the source's defining equations, not from the abstract-level note alone. README "most recent Ledger entry" line and the roadmap crux description corrected.

## 2026-05-26 — Design note captured: resolution anchor vs classification anchor

- **Actor:** Verifier review (insight) / Agent (capture). **Commit:** *(this change).*
- Forward-looking refinement for the deferred Claim IV **measure-registration protocol** (roadmap): distinguish a *resolution anchor* (bounds what must be resolved — e.g. Fisher F[τ]) from a *classification anchor* (carries the structural content classified — e.g. cross-probe γ_Q⁻¹ or mutual information). Conflating them was the CL-2026-007 v0.1 failure mode. Recorded in the roadmap's measure-registration item so it informs the protocol when written; no Coastline change made now.

## 2026-05-26 — Phase 4 step 2: Ledger CL-2026-008 opened (clock networks)

- **Actor:** Steward (direction + guardrails) / Agent (source reads + drafting). **Commit:** *(this change).*
- Per the steward's guardrails, read the defining equations in *both* PDFs first (Covey Eqs. 2–11 incl. Δω Eq. 10; Fromonteil Eqs. 1–7 incl. the redshift Hamiltonian and τ_dec) before drafting. [`ledger/CL-2026-008-clock-networks-v0.1.md`](../ledger/CL-2026-008-clock-networks-v0.1.md) issued (v0.1), classifying the **class** of distributed entanglement-enhanced clock-network proposals (Covey 2025; Fromonteil 2025) **UNDERDETERMINED** against Coastline v0.3.
- **Framing decision (per guardrail):** class claim, not single-paper — the two diverge (Covey: curvature via 3-node W; Fromonteil: redshift/decoherence via N-atom ensembles) while sharing the entanglement-enhanced/anti-redundant structure.
- **Firsts:** classification anchor = cross-probe mismatch (Δω), resolution anchor = QFI/coherence budget (the resolution/classification distinction applied); first exercise of v0.3 "multipartite ≠ redundant" (anti-redundancy noted but **not** held against C2, which does not claim redundancy — per guardrail); first entry to show the **GR-boundary item is load-bearing** (D1), feeding back to the roadmap (item elevated to a prerequisite).
- **Next steps (Phase 4):** Sail re-anchor to Coastline v0.3; methodological notes (temporal-redundancy toy functional; nuisance-discrimination kit). The GR-boundary statement is now a candidate next Coastline action in its own right.
- **Rev. 2026-05-26 (Verifier wording fix):** CL-2026-008 v0.1 corrected *in place* (current/unsuperseded version; wording-only, no load-bearing change). The GR-boundary subsection said Covey's curvature residual is "beyond the Newtonian/weak-field regime" — physically imprecise: Δω is a higher-order *post-Newtonian* term but still **weak-field**. Corrected to "outside the Coastline's written SR/inertial-worldline scope," matching the roadmap. Recorded in the entry's version-history row.

## 2026-05-26 — Phase 4 step 3: Coastline v0.4 (GR-regime boundary)

- **Actor:** Steward (decision: "follow your suggestion" — take the GR boundary before the Sail re-anchor) / Agent (drafting). **Commit:** *(this change).*
- [`coastlines/consensus-emergence-v0.4.md`](../coastlines/consensus-emergence-v0.4.md) issued (v0.3 frozen). Writes the GR-regime boundary statement that CL-2026-008 D1 made load-bearing.
- **Design decision — extend, not bound.** The framework's scope is *extended* to the SR-and-weak-field (post-Newtonian) regime rather than capped at SR, because its own machinery already accommodates it: a spacetime-curvature signal is a Claim IV cross-probe mismatch / Claim II.3 incompressible residual (as CL-2026-008 showed via Covey's Δω). Strong-field and quantum-gravity remain out of scope (Anti-Claim #7).
- **Changes (diff vs v0.3 = exactly these):** External Constraints item 5 (GR, weak-field/post-Newtonian); new *Regime of Validity* section (scope; comparison-protocol proviso; curvature-as-incompressible-residual; exclusions); Anti-Claim #7; a curvature clause on the Claim IV cross-probe anchor; a Deferred strong-field-extension item; version self-references v0.3→v0.4. No claim retired; Load-Bearing Sentence unchanged.
- **Consequence:** satisfies the *framework-side* condition of CL-2026-008 D1 (its experimental D2 still stands); CL-2026-008 may now be re-evaluated against v0.4 — flagged, not done.
- **Next steps (Phase 4):** Sail re-anchor to Coastline v0.4 (now cleaner — the Sail §2 non-inertial-regime paragraph is what the Regime-of-Validity section makes rigorous); methodological notes. Optionally, re-anchor/re-evaluate CL-2026-008 against v0.4.

## 2026-05-26 — Phase 4 step 4: Sail re-anchored to Coastline v0.4

- **Actor:** Steward (decision + scope) / Agent (drafting). **Commit:** *(this change).*
- [`sails/sorci-commentary-v0.4.md`](../sails/sorci-commentary-v0.4.md) issued (v0.3 frozen). Re-anchored the **Coastline** reference v0.2 → **v0.4**; diff vs v0.3 is metadata, three §2 paragraphs, the two Anchoring-Frameworks bullets, and the version-history row only — **no change to the scientific argument**.
- §2 aligned to v0.4: the Claim II triad now flags *many vs redundant* (the two-carrier protocol minimally engages but does not *satisfy* redundancy) and the *transplant gap* (so the reading is a structural alignment, not a satisfied-criterion claim); the distributed-network paragraph notes such entanglement-enhanced networks are *multipartite but anti-redundant* (Claim V; CL-2026-008); the accelerated-worldline caveat rewritten on v0.4's *Regime of Validity* (Anti-Claim #7 for the strong-field exclusion).
- **Ledger anchor kept at CL-2026-006 v0.2** (per scope), with a mixed-anchor note: the v0.2→v0.4 Coastline changes are refinements consistent with the Ledger's UNDERDETERMINED verdict; CL-2026-006's own re-anchor is a separate pending step.
- **Bookkeeping (per steward note):** stale roadmap headings normalised — "Coastline v0.3 — issued" → "Coastline — issued revisions & remaining candidates"; "Sail v0.3 candidates" → "Sail — pending decisions & candidates" (+ fixed the cross-reference). README current-Sail pointer/tree → v0.4.
- **Remaining Phase 4:** methodological notes (temporal-redundancy toy functional; nuisance-discrimination kit). Pending separate steps: re-anchor CL-2026-006 → v0.4; re-evaluate CL-2026-008 against v0.4.

## 2026-05-26 — Version-consistency loop: CL-2026-006 v0.3, CL-2026-008 v0.2

- **Actor:** Steward (direction: "the cheap consistency loop") / Agent (drafting). **Commit:** *(this change).*
- Also fixed a bookkeeping miss the steward caught: README item 3 still described the Sail as anchored to Coastline v0.2 — corrected to v0.4 (mixed anchor).
- **CL-2026-006 → v0.3** (v0.2 frozen): re-anchored Coastline v0.2 → v0.4. **No verdict or discriminant change** — UNDERDETERMINED, I(C : M), D1–D3/D−1 all stand. The v0.2→v0.4 changes are refinements the entry already anticipated ("two carriers, not many"); added a note that the trapped-ion setting falls within v0.4's *Regime of Validity*.
- **CL-2026-008 → v0.2** (v0.1 frozen): re-anchored v0.3 → v0.4 **and re-evaluated**. Coastline v0.4 wrote the GR boundary that v0.1's **D1** demanded, so the **framework-side gap is closed** (D1 ✓ satisfied); classification **stays UNDERDETERMINED**, now resting on **D2** (experimental discriminability) alone. Verdict, Classification, D1, and the roadmap-feedback note rewritten; the CL-006/007/008 set now all sit at the experimental-witness level.
- **Mixed-anchor residue (flagged, not actioned):** Sail v0.4 still cites CL-2026-006 as v0.2 — valid (no verdict/discriminant change), and a pointer bump to v0.3 can ride the next Sail revision rather than forcing a Sail v0.5 now.
- **PAUSE here** (per steward): the next item — the temporal-redundancy toy functional — is research work, not bookkeeping. Holding before it.

## 2026-05-26 — Cleanup: CL-2026-007 v0.3 re-anchor + roadmap normalisation

- **Actor:** Verifier review (catch) / Agent (cleanup). **Commit:** *(this change).*
- Verifier caught that the "all three entries anchored to v0.4" claim was not literally true — **CL-2026-007 was still on Coastline v0.3**. Re-anchored: **CL-2026-007 → v0.3** ([entry](../ledger/CL-2026-007-smith-ahmadi-v0.3.md); v0.2 frozen), a pure re-anchor to Coastline v0.4 with **no verdict/discriminant change** (v0.4's only addition over v0.3 — the GR boundary — is irrelevant to this bipartite SR/weak-field case). All three Ledger entries are now literally anchored to Coastline v0.4.
- Normalised the stale roadmap **active** section: "Possible future Ledger entries" → "Ledger entries opened from candidates", with both bullets refreshed to current versions (007 v0.3, 008 v0.2) and current verdicts. README ledger tree → CL-2026-007 v0.3 (current).
- **PAUSE holds:** next is the temporal-redundancy toy functional (research). Nothing further actioned.

## 2026-05-26 — Tutorial drafted and integrated

- **Actor:** Steward (review) / Agent (drafting + integration). **Commit:** *(this change).*
- [`docs/tutorial.md`](tutorial.md) v0.1 issued — a guided on-ramp covering Harbour architecture, the five Claims, Ledger system, literature-review methodology, synthesis headline, regime of validity, reading order, open problems, and key distinctions. Reviewed by the steward: praised as "genuinely strong," "accurate and version-current," with five identified issues (three substantive, two integration). Issues #1–#3 fixed in place (007 anchor framing softened; Sail mixed-anchor nuance added; mutual-information notation I(S:F) → I(C:M)). Issue #4 resolved: README structure tree updated (tutorial added, docs/ licence clarified as CC BY-SA 4.0), README reading order updated (tutorial as step 0), tutorial metadata licence field set to CC BY-SA 4.0. Issue #5 acknowledged (tutorial-level glosses not in Coastline) — no change required.
- **Status:** tutorial committed and tracked. Next research step remains the temporal-redundancy toy functional (research, not bookkeeping).

## 2026-05-26 — Public website: GitHub Pages

- **Actor:** Steward (direction) / Agent (setup). **Commit:** *(this change).*
- Stood up a dedicated GitHub Pages site presenting the **most recent version** of each artefact. Added `_config.yml` (Jekyll, `jekyll-theme-cayman`, served from `main` root) and a curated landing page [`index.md`](../index.md) linking to the current Coastline (v0.4), Sail (v0.4), Ledger entries (006 v0.3 / 007 v0.3 / 008 v0.2), tutorial, and synthesis. Verified beforehand that no tracked `.md` contained Liquid template delimiters (double-brace variable tags or brace-percent block tags), so the whole-repo Jekyll build is safe. `.gitignore` updated for local Jekyll build artefacts.
- **Outward-facing publish step:** enabling Pages is a *Create Public Surface* action (like the original public-repo creation). If the Pages API call is permission-gated, the site files are nonetheless committed and the steward can enable it in **Settings → Pages → Deploy from a branch → `main` / `(root)`**. Expected URL: `https://uwarring82.github.io/temporal-micro-consensus/`.

## 2026-05-26 — Pages build fix (self-inflicted Liquid error)

- **Actor:** Steward (report: "error building") / Agent (diagnosis + fix). **Commit:** *(this change).*
- The first Pages build (commit `3da95dc`) **failed**. Root cause, from the build log: a Liquid syntax error in `docs/logbook.md` — the entry above, while *describing* the pre-build safety check, included a literal pair of Liquid variable delimiters in its prose, and Jekyll tried to parse them as a template variable. The pre-check itself had been truthful: it ran before that sentence was written.
- **Fix:** (1) reworded that line to name the delimiters in words rather than print them; (2) added `liquid: error_mode: lax` to `_config.yml` so the documentation site renders incidental brace sequences literally instead of failing the whole build — appropriate here because the repo never uses Liquid templating, while the physics notes and logbook routinely contain braces. Re-verified that no other tracked `.md` carries the delimiters.

## 2026-05-27 — Phase 4 research step: temporal-redundancy functional drafted (v0.1)

- **Actor:** Steward (decision: cross the held pause line; Verifier-stance review) / Agent (drafting + computation). **Commit:** *(this change).*
- Crossed the repeatedly-logged PAUSE (the toy functional is research, not bookkeeping). Drafted [`docs/notes/temporal-redundancy-functional-v0.1.md`](notes/temporal-redundancy-functional-v0.1.md): a Riedel-style temporal-redundancy functional `R_δ[τ]` on a finite temporal pointer, with a *computed* partial-information plot and redundancy count. **Headline:** the redundant (product) pole gives `R_{0.10} = 7.1` (plateau reached at `m_δ = 9` of `N = 64`); the anti-redundant (GHZ-shadow) pole gives `R_δ = 1` (zero information in every proper fragment) — "multipartite ≠ redundant" as a number. Maps onto the Sorci case (CL-2026-006) and its D1–D3; argues the bounded-vs-extensive basis for the resolution/classification-anchor split. The Appendix-A script (MIT) regenerates every number; independently re-run and confirmed.
- **Lock-Key held:** the note edits **no** Coastline/Ledger/Sail and explicitly does **not** close the principal open problem (Anti-Claim #6) — the product pole is a *candidate toy exemplar* gated by four stated idealisations; a non-toy (continuous-`τ`, many-carrier, dynamically einselected) instance is carried forward.
- **Steward review (Verifier-stance), two High findings corrected in place before integration:** (1) the GHZ phase realisation `e^{iNθ}` aliased the binary pointer for `N = 64` (both bins map to the same phase) — replaced by the abstract cat encoding `(|0…0⟩ + (−1)^T|1…1⟩)/√2`; (2) the headline `R_δ = 1` did not follow from the stated threshold definition (`I(N) = 1 − h₂(e) < 0.9` bit ⇒ `m_δ` undefined) — resolved by taking the anti-redundant pole's global read-out as ideal (`I(N) = H_T`, `m_δ = N`, `R_δ = 1`), which also removed a per-pole ambiguity in the noise parameter `e`. Further fixes: the Sorci `N = 2` scaling softened to directional/signature (not a fitted `R_δ(r)` curve); `R_δ` reworded as an *effective ratio* (`⌊N/m_δ⌋` the integer count); "`T` is einselected" softened to "treated as the pointer variable (assumed)".
- **Decisions:** licence confirmed (note text CC BY-SA 4.0; Appendix-A code MIT). A formal `MN-2026-xxx` methodological-note ID scheme **deferred** — one note does not justify the taxonomy. Roadmap bumped to v0.3; README structure tree and `index.md` updated to surface `docs/notes/`.

## 2026-05-27 — Documentation weave: temporal-redundancy finding surfaced

- **Actor:** Steward (direction: "weave the toy-example findings into the web page and other relevant spots") / Agent (execution). **Commit:** *(this change).*
- Surfaced the temporal-redundancy functional result (commit `8451bde`) into the public/communication layer:
  - **`index.md` (Pages landing page):** new *A first computed instrument* section with the `R_{0.10} = 7.1` (redundant) vs `1` (anti-redundant) headline table, the resolution/classification-anchor point, the Sorci map, and the toy/open-problem caveat; the earlier Supporting-material bullet folded into it.
  - **`docs/tutorial.md` → v0.2:** §10 rewritten (the toy functional is now drafted, with the headline result and the residual non-toy open problem); the §4 *multipartite ≠ redundant* caveat and the §11 key-distinctions row now cross-reference the computed result. The five-Claim exposition is unchanged.
- **Deliberately left unchanged (Lock-Key):** the Coastline, the three Ledger entries, and the Sail — folding the toy result into the framework itself would be a Coastline-version decision (flagged in the note's §7), not a documentation weave; and the dated Phase-3 snapshot `synthesis-v0.1.md` is frozen by its own scope statement.

## 2026-05-27 — MN v0.2: temporal-redundancy functional (Guardian-review revision)

- **Actor:** Steward (Guardian-stance review, prioritised change list) / Agent (revision + verification). **Commit:** *(this change).*
- Issued [`docs/notes/temporal-redundancy-functional-v0.2.md`](notes/temporal-redundancy-functional-v0.2.md) (v0.1 frozen, retained). Five surgical changes from the review, none touching the load-bearing §4 argument or the refusal to close Anti-Claim #6:
  1. **§1.1** — the *einselection* of the temporal pointer reframed as the central assumption/deferral (the hardest part of Anti-Claim #6); redundancy is computed *conditional on* it. Correctly sizes the contribution as "an instrument that would read redundancy if a temporal pointer were einselected."
  2. **§2 + new Appendix B** — the anti-redundant (zero-in-every-proper-fragment) result is derived from the Schmidt structure of the cat state (reduced fragment is `T`-independent ⇒ *no* measurement carries `T`), measurement-independent; the v0.1 parity model is retained as one explicit read-out channel.
  3. **§3** — Sorci `N = 2` softened: it *exemplifies* the anti-redundant pole but, taking values in `{undefined, 1, 2}`, gives a single transition, not a quantitative `R_δ(r)` curve or a test.
  4. **§4** — anchor-split argument confined to the binary-pointer / continuous-`τ` regime (Fisher extensive/unbounded vs MI bounded); Heisenberg `∝ N²` demoted to a non-load-bearing illustration.
  5. **§5** — the `r`-scan discrimination acknowledged as *conditional* on an unsupplied infidelity-scaling model (inherited from CL-2026-006 D3).
- **External grounding (verified):** the Guardian-flagged preprint **arXiv:2509.17775** (*Functional Information in Quantum Darwinism*, submitted 2025-09, published 2026-02) was confirmed via web search to define `FI(δ) = log₂ R_δ` and compute `R_δ` for a heterogeneous pure-dephasing model — **configurational**. It is now cited as the operational configurational measure this note transplants, used to make the "first computed" claim corpus-relative, and positioned as the configurational counterpart of the open *temporal* instrument (§6c). Flagged as a candidate corpus addition (§7); **not** added to `references.bib` in this step (a literature-review action).
- **Pointer cascade (the three downstream artefacts were public, so versioned):** tutorial → **v0.3** (citations re-pointed to v0.2; content unchanged), roadmap → **v0.4** (references re-pointed; item status unchanged), README structure tree + `index.md` link updated to v0.2. v0.1 left byte-for-byte (matching the repo's no-retro-edit freeze convention — cf. Coastline v0.3 still reads "Successor (none yet)").
- **Lock-Key held:** no Coastline/Ledger/Sail edited. The instrument-vs-fitting architectural question and the Sail-citation coupling are registered (note §7) for steward decision.

## 2026-05-27 — Source archived + findings note: tank2025 (functional information in QD)

- **Actor:** Steward (direction: "download, archive locally, extract to md note") / Agent (acquisition + extraction). **Commit:** *(this change).*
- Archived **arXiv:2509.17775** — A. B. Tank, *Functional Information in Quantum Darwinism: An Operational Measure of Classical Objectivity* (single author, Yeditepe University; v1 2025-09-22, v3 2026-02-01; CC BY 4.0). PDF downloaded to `docs/literature/sources/tank2025.pdf` (11 pp.) — **local-only, git-ignored** per the source-handling policy (not committed; confirmed `git check-ignore`).
- Wrote the own-words findings note [`docs/literature/notes/tank2025.md`](literature/notes/tank2025.md): full per-claim analysis. **Why it matters:** this is the operational *configurational* functional-information measure `F_QD(δ) = log₂ R_δ` (onset-based `R_δ ≈ N/m*_δ`, Holevo-grounded) that MN v0.2 transplants onto a temporal pointer; its pure-dephasing *computed* `R_δ(t)` (early growth → capacity plateau `≲ log₂ N`) is the configurational counterpart of the MN's open temporal instrument (§6c). Its Holevo-over-QMI grounding answers the synthesis §2 "MI overcounts" limitation and is a candidate refinement of the Claim IV measure-registration anchor.
- **Registered per convention:** added a verified `references.bib` entry (`tank2025`, cluster-B) and a provisional row + a *Post-synthesis addition* note in the corpus tracker `index.md`.
- **Deliberately NOT done (flagged):** `synthesis-v0.1.md` is left untouched (frozen Phase-3 snapshot of the 29-entry corpus); the entry's **tier is provisional** (the "Tier-1 fixed at 13" decision predates it) and a synthesis refresh / formal tiering is a future literature-review action for the steward. No Coastline/Ledger/Sail edited (Lock-Key).

## 2026-05-27 — CL-2026-006 v0.4: R_δ added as a second classification anchor

- **Actor:** Steward (Guardian-stance decision: the next coupling work sits in the Ledger, not the Sail) / Agent (drafting). **Commit:** *(this change).*
- Issued [`ledger/CL-2026-006-sorci-v0.4.md`](../ledger/CL-2026-006-sorci-v0.4.md) (v0.3 frozen, retained). Adds the **temporal-redundancy functional R_δ** (MN v0.2) as a second declared **classification anchor** alongside I(C:M), and registers the **resolution-vs-classification split** (resolution anchor = Fisher/QFI F[τ]). New *Engagement under the R_δ anchor* subsection: the Sorci protocol is the minimum-multipartite (N=2) anti-redundant case; squeezing drives the reduced clock toward the anti-redundant pole (R_δ ↓ as V ↓) — the same Claim II.2 stability failure the I(C:M) reading records.
- **Verdict unchanged: UNDERDETERMINED**, now cross-consistent under both anchors (both locate the failure at II.2 and the crux at experimental discriminability), which strengthens its robustness. D1–D3 unchanged; the R_δ-specific quantitative discriminant `R_δ(r) ∼ sinh²(2r)` requires N≫2 and is registered as a forward target, not a condition on the two-carrier claim.
- **Venue rationale (Guardian decision 2026-05-27):** the MN is cited *internally by the Ledger*, **not** from the Sail — citing a methodological instrument from the Sail would, by co-mention, risk implying the Coastline's principal open problem (Anti-Claim #6) is nearer resolution than it is, and the Sail's argument is complete at Coastline v0.4 + this entry. Instrument-vs-fitting placement of MNs stays deferred (note §7) until a second MN forces it.
- **Operationally previews — does NOT commit — the Coastline v0.5 measure-registration protocol** (the split is exercised in the Ledger, its proper venue). **Cascade:** roadmap → v0.5; README ledger tree + reading-order + `index.md` Ledger row → v0.4. Tutorial's CL-2026-006 v0.3 citations left as valid (verdict unchanged); pointer bump optional. Lock-Key: no Coastline/Sail edited.

## 2026-05-27 — Zenodo DOI minted (FAIR-Findability complete)

- **Actor:** Steward (Zenodo↔GitHub integration + release) / Agent (CITATION.cff + README + diagnosis). **Commit:** *(this change).*
- The framework repository is archived at a persistent **Zenodo DOI: [10.5281/zenodo.20411120](https://doi.org/10.5281/zenodo.20411120)**, release tag `framework-status-2026.05.27`. This completes the FAIR *Findable* step the README had flagged as remaining.
- **Blocker resolved en route:** the first Zenodo attempt failed with *"Citation metadata load failed."* Diagnosed (via `cffconvert -f zenodo`, which emulates Zenodo's mapping) as the `CITATION.cff` `license:` **list** mapping to `license.id = [array]`, which Zenodo's deposition model rejects (it accepts a single SPDX id). Fixed by collapsing to the single headline licence **CC-BY-SA-4.0** (steward decision); the repository remains split-licensed (MIT / CC-BY-SA / CC-BY-NC-SA), governed per-file by the in-archive `LICENSE-*` files and documented in a `CITATION.cff` comment.
- **Wired in:** `CITATION.cff` now carries `doi: 10.5281/zenodo.20411120` and `date-released: 2026-05-27`; README gains a DOI row, a completed FAIR-Findable bullet, and a repository-level citation pointer. **Still open (steward):** the ORCID TODO in `CITATION.cff` — provide it to fully complete the author identifier.
- **Consequence:** the toolkit's DOI prerequisite (2026-05-27 Guardian pre-toolkit audit) is now satisfied; the remaining gate before a Toolkit Work Plan v0.1 is steward confirmation of the toolkit's scope.

## 2026-05-27 — ORCID added (author identifier complete)

- **Actor:** Steward (provided ORCID) / Agent (edit). **Commit:** *(this change).*
- Resolves the *"Still open (steward): ORCID TODO"* flagged in the Zenodo-DOI entry above. Added the steward's ORCID — **0000-0001-8081-9718** (checksum-verified, ISO 7064 MOD 11-2) — to `CITATION.cff`, replacing the placeholder. CFF re-validated against schema 1.2.0; the Zenodo creator mapping now carries the ORCID. With the DOI and the ORCID both in place, the FAIR *Findable* author/identifier metadata is complete.
- *Note:* the live Zenodo record was archived before this metadata update, so the ORCID will appear there only on the next published version (or via a manual edit in the Zenodo deposition form); `CITATION.cff` is authoritative for citation regardless.

## 2026-05-27 — Toolkit Work Plan v0.1 (numerical toolkit scope confirmed)

- **Actor:** Steward (scope confirmation + direction; in-IDE Endorsement-Marker edit) / Agent (drafting, review reconciliation, edits). **Commits:** `45b368f` (plan + rev. (a)); *(this change)* (rev. (b) + this entry + README cascade).
- Created [`workplans/toolkit-work-plan-v0.1.md`](../workplans/toolkit-work-plan-v0.1.md) — the **toolkit-scope confirmation** that was the remaining gate flagged in the Zenodo-DOI entry above (2026-05-27 Guardian pre-toolkit audit). New top-level `workplans/` family. Defines scope, locked decisions (D1–D6), gated phases (0–4), validation strategy, framework-artefact discharge mapping, and toolkit anti-claims (A1–A5) for a numerical toolkit (at `numerics/`) operationalising the anchor measures I(C:M), F[τ], R_δ.
- **Multi-pass review (rev. (a)):** steward + IDE reviewer + Claude, nine reconciled findings folded in place. Substantive corrections (not cosmetic): the `R_δ` post-processor re-based on an explicit `TemporalRecordEnsemble` read-out abstraction (R_δ is mutual information between a *classical* pointer and fragment read-outs per MN v0.2 §1.1, not a state-intrinsic quantum MI); the nuisance model split into four computational layers (only the dynamical-Lindblad layer is Liouvillian — preparation/drive-phase/detection errors are not); the impossible `R_δ(r)` curve at N=2 rewritten as a directional transition; the stray squeezing knob `r` dropped from the redundant-pole observable `R_δ(N, ε)`. Plus over-claim corrections (D1 = 1 of 3, not "satisfied"; "candidate exemplar within the assumed regime"; publication staging as proposal), explicit-invariant cross-anchor tests guarding the CL-2026-007 v0.1 failure mode, and CL-2026-005/CBG registration (Q5).
- **Guardian review (rev. (b)):** corrected the §3 Module 3a D1-mapping. CL-2026-006 D1 enumerates five *physical-channel* contributions (i)–(v); the toolkit's four *computational* layers are a distinct taxonomy (heating+dephasing share the dynamical layer, the signal sits in the Hamiltonian), and the sweep covers drive-phase noise + mode-mixing as channels from CL-2026-006's wider discriminability gap, outside D1's formal five — so the budget is a *superset* of the D1 decomposition. *(The Guardian's suggested fix placed drive-phase noise inside D1(i)–(v); verified against CL-2026-006 line 120, which does not list it — corrected accordingly.)*
- **Cascade:** README — new `workplans/` entry in the structure tree, a *What this repository contains* mention, and a CC BY-SA 4.0 licence note. **Lock-Key held:** no Coastline/Ledger/Sail/MN edited; the plan is an instrument, not a framework artefact. **Still open (steward):** Phase 0 kickoff (scaffold `numerics/`), which carries the deferred Q1 (CITATION placement) and Q2 (CI) decisions.

## 2026-05-27 — Toolkit Phase 0: numerics/ scaffold (first code commit)

- **Actor:** Steward (Phase 0 go-ahead; Q1/Q2 defaults accepted) / Agent (scaffold + verification). **Commit:** *(this change).*
- Scaffolded the **`numerics/` numerical toolkit** per work-plan Phase 0 — the first code in the repository. Package `tmc-numerics` (src-layout under `src/tmc_numerics/`, MIT-licensed per the split), `pyproject.toml` (authoritative deps: `qutip`≥5, `numpy`≥1.24, `scipy`≥1.10; extras test/viz/examples/progress), `requirements.txt` (minimum-version manifest, §4), and a QuTiP-free pytest smoke test (`tests/test_smoke.py`, 2 tests). Placeholder `examples/` and `results/` directories with READMEs (Phase 3 deliverables).
- **Decisions resolved:** **Q1** → code citation is an *appended comment section* in the root `CITATION.cff` (re-validated against CFF schema 1.2.0 via `cffconvert`), not a separate file. **Q2** → GitHub Actions CI enabled (`.github/workflows/numerics-ci.yml`; Python 3.10–3.12, path-filtered to `numerics/`). Root `.gitignore` extended for numerics scratch/cache (final results stay committed, per Q3).
- **Q5 still open (Phase 0 TODO):** reconcile with CL-2026-005 (the "CBG" pipeline) before building Module 3a — registered in `numerics/README.md`.
- **Gate to Phase 1:** scaffold committed; `pytest` passes locally (2 passed in 0.01 s). The "runs cleanly on a fresh clone with declared dependencies" half of the gate is confirmed by the CI run triggered on this push. **Lock-Key held:** no Coastline/Ledger/Sail/MN edited.

## 2026-05-27 — Toolkit Q5 resolved: CBG (CL-2026-005) reconciliation

- **Actor:** Steward (decision: QuTiP + CBG-as-optional-backend; flag the ledger note) / Agent (investigation across repos + drafting). **Commit:** *(this change).*
- **Located** CL-2026-005 — it is **not** in this repo; it lives in the separate `oqs-cbg-pipeline` repository. **CBG = Colla–Breuer–Gasbarri**, the minimal-dissipation effective-Hamiltonian construction for *non-Markovian* open quantum systems (two *Phys. Rev. A* 112, 2025 papers); CL-2026-005 v0.4 is its Council-cleared, immutable Breakwater dossier (Sail v0.5).
- **Finding (scope mismatch):** the CBG pipeline computes the coherent effective Hamiltonian `K(t)` and its recursive TCL expansion in the non-Markovian / strong-coupling regime — **not** the Markovian-Lindblad mutual-information nuisance budget that CL-2026-006 D1 requires. Hosting Module 3a inside CBG was rejected because (i) scope mismatch (`K(t)` ≠ MI/Lindblad budget; and toolkit D3 already commits the Sorci module to QuTiP), (ii) CBG's trapped-ion territory is *permanently stewardship-conflict-bound* (steward co-authors the cited Nat. Commun. experiment, CL-2026-005 Entry 6) under Council-clearance discipline, and (iii) different anchor chains / update cadences.
- **Resolution (recorded in the work plan §6/§8/§10 rev. (c) + `numerics/README.md`):** Module 3a is reimplemented in QuTiP (per D3); CBG is retained only as (a) an *optional* non-Markovian validation backend (its HEOM/pseudomode reference solvers, for later cross-checks of the Markovian approximation) and (b) a methodological template (its benchmark-card / frozen-parameter / cause-labelled failure-envelope / validity-envelope discipline).
- **Registered flag (not actioned — Lock-Key):** CL-2026-006's "Relation to other Ledger entries" note claims CBG "produces precisely the nuisance-channel decomposition required by D1"; this overstates the scope mismatch and is flagged for softening when **CL-2026-006 v0.5** is drafted (work-plan Phase 4). No Coastline/Ledger/Sail/MN edited; CL-2026-005 (immutable, other repo) untouched.

## 2026-05-27 — Toolkit Phase 1 API contract accepted (signatures locked, D6)

- **Actor:** Steward (two-review pass → accept after blocking fixes; directive "mirror + commit, pause before code") / Agent (drafting, fix, mirror). **Commit:** *(this change).*
- Created [`workplans/toolkit-phase1-api-contract-v0.1.md`](../workplans/toolkit-phase1-api-contract-v0.1.md) — the Phase 1 **core-layer API contract** (the D6-locked public surface: `SystemLayout`, `HamiltonianTerm`/`LocalOp`/`Op`, `LindbladChannel`/`ChannelKind`, `DriveNoise`, `ReadoutModel`, `MasterEquationSystem`/`evolve`, `ObservableSpec`/`SolverOptions`/`EvolutionResult`, prep primitives). **Design-only; no implementation written.**
- **Two-pass review reconciled:** one review accepted as-is; the other found 4 Blocking + 2 High + 2 Medium. The blocking findings were correct on the merits — including a genuine physics error the accepting review mis-scored as non-blocking: HEATING as `D[a†]` gives state-dependent `d⟨n⟩/dt = ṅ(⟨n⟩+1)`, not the constant `ṅ` its test asserts. All resolved (contract §13): **HEATING redefined** as `ṅ·(D[a†]+D[a])` (state-independent `d⟨n⟩/dt = ṅ`) with a new **`THERMAL_RELAXATION`** channel for steady-state behaviour; **prep made local** (primitives return local density matrices, `product_state` tensors, `prep_infidelity` per-primitive); **`ObservableSpec`** for `e_ops` (bare `LocalOp` had no target); explicit **full-Hilbert (Phase 1) / factorised (Phase 3) backend boundary** (D3); constructor `dim` args removed (`TWO_LEVEL`==2); `DriveNoise.effective_rate` (explicit Lindblad rate, no hidden PSD factor); `SolverOptions.solver` + `fock_population_warn_threshold`.
- **Steward calls O1–O7 recorded** (contract §11): SI/`ℏ=1`; `TWO_LEVEL`=2; bare `Qobj` + `validate_state`; local symmetric-depolarising prep infidelity; `WHITE`→explicit effective rate; `mesolve` only; `drive_noise`+`readout` carried on the system as declared specs.
- **Accepted by the steward** ("mirror + commit, pause before code"): the signatures are mirrored into [`numerics/README.md`](../numerics/README.md) (new *Phase 1 — core-layer API* section) and locked per D6. **Implementation deliberately NOT started** — the steward will review the README-form locked contract before a separate implementation go-ahead. **Lock-Key held:** no Coastline/Ledger/Sail/MN edited.

## 2026-05-27 — Phase 1 contract LOCKED (drive-noise factor fixed); implementation begins

- **Actor:** Steward (final review: one remaining blocker, then "Go") / Agent (fix + implementation). **Commit:** *(this change; implementation follows).*
- **Final blocker fixed:** the `DriveNoise.effective_rate` factor convention. The first rev. said WHITE noise adds `effective_rate·D[operator]` and called that "dephasing at effective_rate" — but `D[σ_z]` sends a coherence `ρ_01 → −2ρ_01`, so `rate·D[σ_z]` decays as `e^{−2·rate·t}`, inconsistent with `ChannelKind.DEPHASING`'s `(γ_φ/2)·D[σ_z] → e^{−γ_φ t}`. **Fixed (steward option 1):** `effective_rate` is the *coherence-decay rate*; WHITE adds `(effective_rate/2)·D[operator]`, so a σ_z WHITE drive noise is exactly a `DEPHASING` channel at `γ_φ = effective_rate` (contract §5.3 + README mirror).
- **Contract ACCEPTED + D6-LOCKED.** All four Blocking + two High + two Medium findings + this factor fix resolved across two review passes. Public signatures are stable from v0.1; Phase 1 implementation proceeds against them (next commit). **Lock-Key held.**

## 2026-05-27 — Toolkit Phase 1 core layer implemented (37 tests green)

- **Actor:** Steward ("Go" after the drive-noise fix) / Agent (implementation + local test run). **Commit:** *(this change).*
- Implemented the Phase 1 core layer in `numerics/src/tmc_numerics/` against the D6-locked contract: `layout.py` (`SystemLayout`/`Subsystem`/`SubsystemKind`), `operators.py` (`Op`/`LocalOp`/`HamiltonianTerm`/`ObservableSpec`, full-space embedding, Hermiticity-checked assembly), `channels.py` (`ChannelKind`/`LindbladChannel`/`NoiseModel`/`DriveNoise`/`ReadoutModel`, collapse-operator construction), `system.py` (`MasterEquationSystem`/`evolve` over QuTiP `mesolve`; `SolverOptions`/`EvolutionResult`/`EvolutionDiagnostics`), `prep.py` (local prep primitives + `product_state`), `exceptions.py`.
- **Closed-form validation (the contract §10 test list):** 37 tests pass locally on QuTiP 5.2.3. Verified — DEPHASING coherence `e^{−γ_φ t}`; HEATING constant `d⟨n⟩/dt = ṅ` (the symmetric-pair generator); THERMAL_RELAXATION cooling `e^{−κt}` and steady-state `n̄`; **WHITE σ_z drive noise == DEPHASING at `γ_φ=effective_rate`** (confirms the `/2` factor fix at runtime); squeezed `⟨n⟩=sinh²r` + sub-vacuum quadrature variance; closed-system trace/purity preservation; truncation diagnostic flags overflow at tiny cutoff; layout/Hermiticity/kind-mismatch/firewall guards.
- **Firewall test passes:** no `R_δ` / pointer / `I(C:M)` / `F[τ]` symbol in the Phase-1 public namespace. README status + API section updated to "implemented". **Lock-Key held:** no Coastline/Ledger/Sail/MN edited. **Next:** Phase 2 (anchor-measure layer) — and the D5 regression gate (reproduce MN v0.2 §2 analytic poles) before Phase 3.

## 2026-05-27 — Toolkit Phase 2 implemented: anchor-measure layer + D5 gate (68 tests green)

- **Actor:** Steward (lightweight Phase-2 contract requested; accepted after a two-review pass; "Go") / Agent (contract + implementation + local validation). **Commit:** *(this change).*
- Accepted + D6-locked the Phase-2 contract addendum [`workplans/toolkit-phase2-api-contract-v0.1.md`](../workplans/toolkit-phase2-api-contract-v0.1.md) (P1–P5 confirmed as recommended). Implemented the anchor layer in `numerics/src/tmc_numerics/anchors/`: `ensemble.py` (`TemporalPointer`/`InfoMode`/`TemporalRecordEnsemble` with `iid`+`from_evolution`, `partial_information`), `info.py` (`mutual_information` I(C:M), `quantum_fisher` F[τ]), `redundancy.py` (`RedundancyResult`, `temporal_redundancy` R_δ).
- **D5 gate MET** (architectural-integrity gate). The regression reproduces the MN v0.2 §2 analytic poles **exactly**: product (redundant) pole via the **iid** representation matches Appendix A to ≤1e-9 — `R_{0.10}=64/9≈7.1` at e=0.20, *undefined* at e≥0.40 — and never builds a 2⁶⁴ state; GHZ-shadow (anti-redundant) pole via the **joint** representation at small N gives `R_δ=1` under `HOLEVO`, with `test_ghz_per_carrier_reads_zero` the negative confirmation (per-carrier read-out sees `I(N)=0` — why the ideal-global/HOLEVO mode is required). The "multipartite ≠ redundant" contrast (7.1 vs 1) is a single test.
- **Design (per contract):** `TemporalRecordEnsemble` has two representations — **iid** (single-carrier state + count; binomial MI; scales to N=64 and is the Phase-3 Module-3b substrate per D3) and **joint** (small-N dense, fragments via `ptrace`). `InfoMode.HOLEVO` makes MN's "ideal global read-out" an explicit typed mode (= `PER_CARRIER` on the redundant pole). All information in bits; QFI dimensionful; pointer basis fixed computational/binary for v0.1.
- **Cross-anchor invariants implemented** (`tests/test_cross_anchor.py`): `F[τ]` extensive (`F_τ(m)=m·F_1`); the **anti-correlation** — GHZ has Heisenberg `F[τ]=N²` but `R_δ=1`, product has `F[τ]=N` but `R_δ≫1` (F rises where R_δ falls); per-carrier nuisance collapses the `R_δ` plateau with no entanglement change. This is the resolution/classification split (F[τ] is *not* a classifier — the CL-2026-007 v0.1 guard).
- **68 tests pass** locally (QuTiP 5.2.3). Firewall updated: the Phase-1 core (`import tmc_numerics`) stays clean; anchors live in `tmc_numerics.anchors`. **Lock-Key held:** no Coastline/Ledger/Sail/MN edited. **Next: Phase 3** — protocol modules (Module 3a Sorci nuisance budget; Module 3b open-temporal-instrument via the iid backend).

## 2026-05-27 — Toolkit Phase 3 Module 3a implemented: Sorci nuisance budget (79 tests green)

- **Actor:** Steward (lightweight Module-3a contract; accepted after a two-review pass; "Go") / Agent (contract fixes + implementation + local validation). **Commit:** *(this change).*
- Accepted + D6-locked the Module 3a contract [`workplans/toolkit-module3a-sorci-contract-v0.1.md`](../workplans/toolkit-module3a-sorci-contract-v0.1.md) (M1–M4 confirmed). Implemented `numerics/src/tmc_numerics/modules/sorci.py`: `SorciParams`, `build_sorci_system` (secular/full), `sorci_initial_state`, `sorci_visibility`/`sorci_observed_visibility`/`clock_motion_mutual_information`, `baseline_visibility_check`, `rwa_error`, `nuisance_budget`/`NuisanceBudget`.
- **Two-pass review reconciled** (one accept-as-is; one with 2 Blocking + 2 High + 1 Medium — all correct, all folded): **B1** latent/observed split — `I(C:M)`/`V_state` are read-out-invariant; detection acts only on `V_obs`, so the detection budget row has latent `ΔI = ΔL_state ≡ 0` (an executable invariant). **B2** M3 transfer conditions made explicit (`λ=ε_m ω_c t`, `r`, `γ_φ t`, `ṅ t`; secular hierarchy `|g_md|/ω ≪ 1`, `ωt ≫ 1`). **H1** `g_md = −ε_m ω_c/4` **derived** (the baseline *validates* Sorci Eq. (12)'s coefficient, not calibrates). **H2** sign tests are benchmark-specific diagnostics, not global invariants. **M** D1-minimal single-mode; mode-mixing deferred.
- **Physics validated (79 tests, QuTiP 5.2.3):** the secular model reproduces Sorci Eq. (12)'s coefficient (`(1−V)/(λ²sinh²2r) → 1/16` as λ→0 — the derived `g_md` pinned, not fitted); the full `σ_z P̂²` model agrees within the RWA error at `ωt=10`; the ²⁷Al⁺ secular extrapolation gives `V≈0.93` (matching Sorci); `I(C:M)>0` confirms the signal entanglement.
- **A genuine physics finding the harness surfaced:** in the secular dispersive model, **motional dephasing `D[n̂]` is invisible to the clock** (it leaves the Fock populations — and hence `⟨e^{-2i g_md t n̂}⟩` — unchanged), while heating, clock drive-phase, and prep infidelity do contribute. Concrete evidence for why sign tests must be benchmark-specific (review H2); the contribution would differ in the full `σ_z P̂²` model.
- **Budget:** marginal-from-signal per channel + full + interaction residual (honest non-additivity, A4), latent/observed columns; scaled headline + extrapolated ²⁷Al⁺ column. **Lock-Key held.** **Next: Module 3b** (open-temporal-instrument via the iid backend — derive `ε(γ_φ,ṅ,t)` from a single-carrier master equation; a short 3b addendum first), then Phase-4 feed-forward (CL-2026-006 v0.5 D1 grounding + the registered CBG-note softening).

## 2026-05-27 — Toolkit Phase 3 Module 3b implemented: open temporal instrument (85 tests green)

- **Actor:** Steward (lightweight 3b addendum; accepted after a two-review pass; "Go") / Agent (contract fixes + implementation + validation + committed results). **Commit:** *(this change).*
- Accepted + D6-locked the Module 3b addendum [`workplans/toolkit-module3b-open-instrument-contract-v0.1.md`](../workplans/toolkit-module3b-open-instrument-contract-v0.1.md) (B1–B3 confirmed). Implemented `numerics/src/tmc_numerics/modules/open_instrument.py`: `OpenInstrumentParams`, `single_carrier_coherence`, `carrier_distinguishability`, `carrier_conditional_states`, `redundancy_at`, `redundancy_curve`.
- **The dynamical bridge:** a single-carrier pure-dephasing `evolve` (`|+⟩` under `(γ_φ/2)D[σ_z]`) gives `c=e^{−γ_φ t}`; `ε(γ_φ,t)=(1−c)/2`, where the trace-distance/Helstrom, the phase-bin projective flip, and the Holevo information (`χ=1−h₂(ε)`) all agree — so MN Appendix A's hand-fixed `e` is now *derived*. The Phase-2 `iid` ensemble + `temporal_redundancy` give `R_δ(N, γ_φ t)` (any N, no 2ᴺ state).
- **Two-pass review reconciled** (1 Blocking + 1 High + 2 Medium, all folded): **B** the `σ_x` read-out renamed the *assumed temporal / phase-bin readout basis* (NOT "einselected" — the dephasing's physical pointer is σ_z; what is einselected for the temporal record is the §4 *assumption*). **H** Holevo `χ = 1 − h₂(ε)` (consistent with, not equal to, ε). **M** single-carrier label `carrier_00`; artifacts → `results/` + notebook.
- **D5 cross-link passes to the D5 standard:** at `γ_φ t = ln(5/3)` (`ε=0.20`) the dynamical path lands on `R_{0.10}=64/9` exactly (`m_δ=9`) — Module 3b inherits the Phase-2 exactness.
- **Results committed** (Q3): `numerics/results/open_instrument_redundancy_v0.1.json` (the `R_δ(N, γ_φ t)` grid + undefined boundary — **`γ_φt_crit` rises with N: 0.27 (N=4) → 1.83 (N=128)**, i.e. more independent carriers tolerate more per-carrier nuisance, MN §5) and `numerics/results/sorci_nuisance_budget_v0.1.json` (the Module 3a D1 budget; the detection row confirms latent `ΔI=ΔL_state=0`, observed `ΔL_obs≈0.099`; Al⁺ extrapolation `V=0.943`).
- **85 tests pass** (QuTiP 5.2.3). The **einselection caveat (§4) is verbatim-ready** for Phase-4 feed-forward; **Anti-Claim #6 stays open**. **Lock-Key held:** no Coastline/Ledger/Sail/MN edited. **Next: Phase 4** — CL-2026-006 v0.5 (D1 grounding + the registered CBG-note softening) and MN v0.3 (Module 3b curves), plus the Phase-3 example notebooks (both modules) to close the gate. All steward/Lock-Key.

## 2026-05-27 — Phase 3 formally closed: example notebooks (gate condition met)

- **Actor:** Steward (direction: close the gate before Phase 4) / Agent (notebooks). **Commit:** *(this change).*
- Added the two reproducible example notebooks (`numerics/examples/`), **executed end-to-end** (nbconvert, QuTiP 5.2.3, zero errors), regenerating the committed results from scratch:
  - [`sorci_module.ipynb`](../numerics/examples/sorci_module.ipynb) — Module 3a: baseline Eq. (12)-coefficient validation, the latent/observed D1 budget table (detection row latent `ΔI=ΔL_state≡0`), the ²⁷Al⁺ `V≈0.943` extrapolation; asserts a match to `results/sorci_nuisance_budget_v0.1.json`.
  - [`open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) — Module 3b: derives `ε=(1−e^{−γ_φ t})/2`, the D5 cross-link (`ε=0.20 → R_{0.10}=64/9` exactly), the `R_δ(N, γ_φ t)` curves (plot) + the undefined boundary rising with N; carries the **einselection caveat verbatim**; asserts a match to `results/open_instrument_redundancy_v0.1.json`.
- **Phase-3 gate condition MET** (work-plan §3): both protocol modules produce reproducible results stored as notebooks in `numerics/examples/` and documented results in `numerics/results/`. **Phase 3 is complete.** **Next: Phase 4 feed-forward** — CL-2026-006 v0.5 drafted first (Module 3a D1 grounding + the registered CBG-note softening; Lock-Key — the numerical results inform but do **not** auto-update the verdict, Anti-Claim A5). Lock-Key held.

## 2026-05-27 — Ledger CL-2026-006 v0.5 issued: D1 quantitatively grounded by the toolkit

- **Actor:** Steward (two-review pass → accept + issue after two text fixes) / Agent (draft + fixes + cascade). **Commit:** *(this change).*
- Issued [`ledger/CL-2026-006-sorci-v0.5.md`](../ledger/CL-2026-006-sorci-v0.5.md) (v0.4 frozen, retained) — the first Phase-4 framework feed-forward. tmc-numerics **Module 3a's D1 nuisance budget** (committed `numerics/results/sorci_nuisance_budget_v0.1.json`) is incorporated as quantitative grounding for Discriminant D1.
- **Verdict UNCHANGED — UNDERDETERMINED** (Lock-Key / Anti-Claim A5: numerics inform, do not auto-update). D1's *theoretical* decomposition is now produced and reproducible, but the budget shows it is **model-conditional**, so it *sharpens* D1 (a demand on controlled scaling + explicit channel models) rather than discharging it; D2/D3 remain experimental and unmet. Three honestly-reported findings: per-channel signs are non-uniform (heating/dephasing **reduce** I(C:M); the depolarising prep-infidelity model **raises** and dominates it); detection is **latent-invariant** (ΔI=ΔV_state=0, observed-only); motional dephasing is **visibility-invisible** in the secular model yet still lowers I(C:M) (channels non-additive).
- **CBG-note softened** (toolkit Q5): the v0.4 "CBG produces precisely the D1 decomposition" corrected — CL-2026-005 computes the coherent K(t) (non-Markovian), not the Markovian D1 budget; D1 is produced by tmc-numerics. **Module 3b** registered as a candidate Anti-Claim #6 exemplar under the **verbatim einselection caveat** (Anti-Claim #6 open).
- **Two review fixes folded before issuance:** (1) the toolkit grounding cites the committing commit `a6518a7` with "Zenodo update pending" — the existing DOI snapshot (`framework-status-2026.05.27`, tag → `d4ca52d`) **predates** the toolkit; (2) motional dephasing reworded "visibility-invisible (secular)" — it still lowers quantum I(C:M) — removing a draft self-contradiction.
- **Cascade:** README structure tree + reading-order → v0.5; `index.md` Ledger row → v0.5; roadmap → v0.6. **Toolkit-v0.2 refinement registered:** a physical squeezing-prep-infidelity model (r-jitter / thermal admixture) — the v0.1 symmetric-depolarising model is aggressive for a high-cutoff bosonic mode. **Next Phase-4:** MN v0.3 (Module 3b curves), then journal-paper integration. **Lock-Key held:** Coastline untouched; v0.4 frozen.

## 2026-05-27 — MN v0.3 issued: numerical companion (§8) to the temporal-redundancy functional

- **Actor:** Steward (two-review pass → accept + issue after two wording fixes) / Agent (draft + fixes + cascade). **Commit:** *(this change).*
- Issued [`docs/notes/temporal-redundancy-functional-v0.3.md`](notes/temporal-redundancy-functional-v0.3.md) (v0.2 frozen, retained). v0.3 **adds §8** — the open-temporal-instrument numerical companion (toolkit Module 3b) — and carries §1–§7 + Appendices A/B **verbatim**; §2's analytic poles are byte-identical (diff-verified).
- **§8 content:** the dynamical bridge `ε(γ_φ,t)=(1−e^{−γ_φ t})/2` derived from a single-carrier pure-dephasing master equation (the four-way trace-distance / Helstrom / projective / Holevo coincidence); the `R_δ(N, γ_φ t)` curves with the undefined-boundary table (first undefined sweep point, rising with N); the D5 cross-link validation (`ε=0.20, N=64 → R_{0.10}=64/9` exactly); the *redundancy-lost-without-entanglement* structural point (§8.4); and the **match to arXiv:2509.17775** (§8.5 — the temporal-side pure-dephasing `R_δ` now exists, matching the configurational computation in *structure*, not numerically — the asymmetry closed at the level of the computation).
- **Einselection caveat carried verbatim (§8.6)** — identical block to CL-2026-006 v0.5; **Anti-Claim #6 remains open** (§8 is a candidate exemplar within the assumed regime, not a resolution).
- **Two review fixes before issuance:** (1) the §8.2 boundary table relabelled "first undefined `γ_φ t` in the committed sweep" (grid-resolution-dependent), **not** the exact continuous `ε_crit` — with the distinction stated; (2) explicit artifact provenance added (`numerics/results/open_instrument_redundancy_v0.1.json`, `numerics/examples/open_instrument_module.ipynb`).
- **Cascade:** `index.md` / README structure tree / `docs/tutorial.md` (×2) MN pointers → v0.3; roadmap "Toy-model functional" item → v0.3 + roadmap → v0.7. v0.2 frozen; **Coastline untouched (Lock-Key).** **Next Phase-4 (final): journal-paper integration** (Coastline + MN v0.3 + the worked exemplar).

## 2026-05-28 — Harbour View artefact category opened: spec v0.6 + first-draft task card HV-DRAFT-001

- **Actor:** Steward (deliberation, lock candidate, task-card issuance) / Agent (capture). **Commit:** *(this change).*
- Spec captured: [`docs/harbour-view-structural-deliberation-note-v0.6.md`](harbour-view-structural-deliberation-note-v0.6.md) — the locked design specification for the new **Harbour View** artefact category (consolidated, externally-readable, citable presentation of a body of Harbour work; derived from but not authoritative over the underlying Coastline/Ledger/MN/Sail artefacts). v0.6 is the lock candidate (Discovery → v0.1 → … → v0.6 lineage; Guardian + Scout + structural + Architect passes; adds the Q12 language-boundary / external-readability rule). Two-layer structure: category-level rules (binding on every future View) vs. first-instance choices (this View only).
- Task card issued: [`docs/harbour-view-task-card-HV-DRAFT-001.md`](harbour-view-task-card-HV-DRAFT-001.md) — **HV-DRAFT-001**, companion to spec v0.6, drives the **first Harbour View** (`view-framework-overview-v0.1.md`): five sections (executive overview / framework / methodology / worked exemplar / open problems + invitations) + reference apparatus (commented bibliography, version-pinning appendix, Toolkit Provenance, Figure Provenance, acknowledgements & COI — Leibfried acknowledged, Sanner not, per the 2026-05-26 correction). Pinned source set: Coastline v0.4, MN v0.3, CL-2026-006 v0.5, toolkit Modules 3a + 3b (committing commit `a6518a7`).
- **Five hard rules (acceptance gates):** (Q1) no new claims; (Q10b / A7) every operating-point figure shows both sweet **and** anti-sweet regions; (Q11b) every "consistent with X" cites a Ledger entry or Coastline §; (Q12) externally readable — specialist terms defined on first use, Harbour shorthand kept out of the body; (A7) no sentence may read more confidently than the source artefact it presents.
- **First move (per the card):** drafter returns table-of-contents + per-section bullet outline (with the §6 figure plan) for steward sign-off **before any prose**. **Lock-Key held:** Coastline / Ledger / MN / Sail untouched; the View presents, does not edit.

## 2026-05-28 — First Harbour View: TOC outline drafted (pre-prose, awaiting steward + Guardian sign-off)

- **Actor:** Steward (direction: execute task card HV-DRAFT-001) / Agent (drafter, outline only — no prose). **Commit:** *(this change).*
- Issued the first-move deliverable: [`docs/view-framework-overview-v0.1-toc-outline.md`](view-framework-overview-v0.1-toc-outline.md) — table of contents + per-section bullet outline + §6 figure plan for the first Harbour View ([HV-DRAFT-001](harbour-view-task-card-HV-DRAFT-001.md)). **Not the View itself; not prose.** Five sections + reference apparatus, declared structural cap per spec v0.6 §6 first-instance; declared primary audience (foundations-metrology physicists; task-card hard-rule 4 strengthening: executive overview followable with no Harbour background); declared structural pattern (Option B layered + Option D question-driven entry, per §5).
- **Pinned source set explicit:** Coastline v0.4, MN v0.3, CL-2026-006 v0.5, toolkit Modules 3a + 3b at commit **`a6518a7`** (results JSONs + example notebooks). Per-section anchors recorded; every section bullet cross-references the pinned source from which it draws.
- **Figure plan locked at TOC-level (§6):** type (a) schematic in §§1–2 (×2 — orienting + comparison-geometry); type (b) sweet/anti-sweet `R_δ(γ_φ t)` at fixed `N` in §3 with explicit anti-sweet demarcation (Q10b load-bearing); type (c) `R_δ(N, γ_φ t)` heatmap landscape in §4 with the undefined-boundary curve marked + anti-sweet region demarcated (Q10b binds type-(c) sweeps displaying operating points per v0.5 closure); live Colab link in §4 (Q10d convenience layer, static figure is the citable artefact). Verbatim insertions: the MN v0.3 §8.6 einselection caveat in §3, single inline cross-reference in §4.
- **Seven open items flagged for the steward + Guardian lock call:** (1) schematic-figure generators for §1 + §2 (no pinned generator yet — recommend toolkit-side schematic notebook); (2) type-(b) `N`-value set (proposed `{4, 32, 128}`); (3) notebook-header einselection caveat surfacing for Q10e (toolkit-side edit if needed, not a View edit — Lock-Key preserved); (4) pilot-reader identity (foundations-metrology physicist outside Freiburg–NIST, mandatory for first View, no omission permitted); (5) concordance-paragraph N/A confirmation (first View); (6) post-lock placement at `views/` per O2; (7) cross-View pointers in §2 (CL-2026-007 v0.2 + CL-2026-008 v0.2, recommend brief presentational pointer).
- **Discipline gates recorded per section** (Q1 / Q10b / Q11b / Q12 / A7 / A8) — each section's outline carries the hard-rule audit checks that bind at prose stage. **Lock-Key held:** Coastline / Ledger / MN / Sail untouched. **Next:** steward + Guardian review of the outline; on sign-off, proceed to spec §12 next-moves 5–6 (Jekyll pre-issuance check; figure generation), then bibliography assembly and prose drafting for §1 + §2 → pilot reader.

## 2026-05-28 — First Harbour View TOC outline LOCKED (Guardian sign-off; Q10c carve-out for schematics)

- **Actor:** Steward (sign-off review, Guardian stance) / Agent (drafter; lock record + outline updates). **Commit:** *(this change).*
- TOC outline ([`docs/view-framework-overview-v0.1-toc-outline.md`](view-framework-overview-v0.1-toc-outline.md)) **LOCKED 2026-05-28**. Sign-off review surfaced exactly one substantive finding from drafting-contact (validating the "outline-before-prose" discipline of HV-DRAFT-001), and it is a **spec defect, not an outline defect**: Q10c ("every figure is a static render generated by a version-pinned notebook") is load-bearing for *data* figures (types b/c) but a **category error for type-(a) schematics**, which encode no results — they illustrate structure. Forcing a conceptual diagram through a QuTiP notebook uses a notebook as a vector-graphics editor — ceremony without reproducibility payoff.
- **First-instance figure-handling resolution (binding for this View, recorded in the outline's Lock Record):** type-(a) schematics are **authored vector sources (SVG or TikZ)** committed to the repository and **pinned by source-file commit hash** in Figure Provenance; type-(b) and type-(c) data figures **remain notebook-pinned per Q10c at commit `a6518a7`**. The provenance discipline (commit-pinning) is fully satisfied for both regimes; the tool differs because the artefact differs. The outline-stage recommendation to extend the toolkit with a schematic-generator notebook is **rejected as over-engineering**.
- **Spec amendment DEFERRED (not spec v0.7):** a one-line Q10c carve-out — *"data figures notebook-pinned; schematics source-file-pinned"* — to be folded into the spec at its next genuine revision, whenever one is independently warranted. No spec v0.7 spun for this finding alone. Discipline registered: amend from task-contact, not from another review pass. **Action carried in the outline's Lock Record + this logbook entry; no spec edit at this point** ([`docs/harbour-view-structural-deliberation-note-v0.6.md`](harbour-view-structural-deliberation-note-v0.6.md) untouched).
- **Six other open items resolved at the lock call:** (2) type-(b) `N`-set = `{4, 32, 128}` confirmed (rising-boundary set displays the structural finding; single `N=64` would hide it); (3) notebook caveat surfacing (Q10e) — verify first, update only if absent (toolkit-side, not View-side; Lock-Key preserved); (4) pilot-reader identity — steward's call, **must be outside the Freiburg–NIST orbit** (COI runs through Leibfried at NIST; inside-orbit reader is not an independent audience test), mandatory no omission for the first View; (5) concordance paragraph confirmed N/A (first View); (6) post-lock placement `views/` per O2 confirmed; (7) cross-View pointers — drafter's choice at prose stage, Guardian leans §5.1 over §2 (status point sits more naturally with open problems than with "what the Coastline claims"); not a blocker.
- **Cleared forward sequence (post-lock, spec §12 next-moves 5 → 6 → 7 → prose):** Jekyll versioned-subpath check → figure generation (notebook caveat verification + source-file-pinned schematics + notebook-pinned data figures from commit `a6518a7`, sweet/anti-sweet labelling per Q10b) → commented-bibliography assembly (Q11) → prose for §1 + §2 → pilot reader on those two sections (Scout EC-5) → remaining-section prose → two-pass review (Q10b figure honesty / Q11 bibliography / Q12 readability) → tagged release with O6 citation locator. **Lock-Key held throughout.**

## 2026-05-28 — First Harbour View build: §12 next-moves 5, 6a, 6b, 7 advanced (read-only checks + schematics + bibliography draft)

- **Actor:** Steward (Guardian sign-off, direction "bibliography + schematics in parallel; hold notebook extension") / Agent (drafter; checks, SVGs, worksheet). **Commit:** *(this change).*
- **Next-move 5 — Jekyll versioned-subpath check (read-only, FINDING REPORTED):** [`_config.yml`](../_config.yml) is minimal — `jekyll-theme-cayman` + `jekyll-relative-links` default + one global `defaults` block; **no `collections`, no `permalink:` pattern, no front-matter on artefact `.md` files**. Current rendering is flat file-mirror URLs (`views/view-framework-overview-v0.1.html`), NOT the `/views/framework-overview/v0.1/` versioned-subpath structure Q9b assumes for supersession discoverability. Scout EC-5 anticipated this. **Steward-decided:** **per-View front-matter `permalink: /views/framework-overview/v0.1/`** plus a `views/framework-overview/index.md` redirect stub for the Q9b(b) canonical-current URL. Minimum-viable change; no `_config.yml` edit required; jekyll-relative-links coexists. `_views` collection approach rejected as premature for single-digit View count. **Physically applies at release; does not block figure or bibliography work.**
- **Next-move 6a — notebook einselection-caveat surfacing (Q10e, read-only, NO-OP CONFIRMED):** read markdown cell 0 of [`numerics/examples/open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) — the einselection caveat is **already surfaced verbatim as a blockquote** before any code cell. Visitor sees it at first interaction on Colab open. Anti-Claim #6 named; module marked as *candidate* exemplar **within the assumed regime**; Coastline-level judgement reserved to steward (Lock-Key). **No toolkit-side header update required.** In-notebook cross-reference points to "MN v0.2 §1.1" (canonical source location; v0.3 §§1–7 byte-identical to v0.2); the View independently anchors its §3/§4 verbatim insertions to MN v0.3 §8.6. Figure-citing commit stays at **`a6518a7`** until the data-figure extension below moves it.
- **Next-move 6b — type-(a) schematics authored (Guardian-shaped, source-file-pinned per Q10c carve-out):** two SVGs committed to [`views/figures/`](../views/figures/):
  - [`schematic-01-framework-orientation.svg`](../views/figures/schematic-01-framework-orientation.svg) — §1: N carriers (F₁…F_N) coupling to a single shared effective proper-time variable τ; Page–Wootters bipartite slice highlighted as the N=2 sub-structure (F₁ + F₁→τ arrow in blue). Pure structure; no axes, no parameter values, no operating-point claim.
  - [`schematic-02-comparison-geometry.svg`](../views/figures/schematic-02-comparison-geometry.svg) — §2: top panel = nested L_source ⊂ L_apparatus ⊂ L_comparison hierarchy; bottom panels = side-by-side anti-redundant (Ψ_total, no proper fragment carries record R) vs redundant (N fragments, each ✓ R). Pure structure; the multipartite-≠-redundant contrast drawn as which-fragment-carries-R (yes/no), never as `R_δ`-vs-parameter (would convert it into a type-(b) figure and trigger Q10b).
  - Both XML-valid (parsed); both Q10b-clean (no operating points); both Q1-clean (illustrate structure the Coastline already asserts). Source-file-pinned; commit hash recorded in Figure Provenance at the figure-gen step.
- **Next-move 7 — commented bibliography drafted (Q11 assembly worksheet):** [`docs/view-framework-overview-v0.1-bibliography-draft.md`](view-framework-overview-v0.1-bibliography-draft.md) — every entry carries anchor + role annotation + (for external-constraint entries) endorsement-scope marker. Five sections: **A** External constraints (QM / SR / GR weak-field / QIT / thermodynamics — all five **STEWARD-CONFIRM** for canonical-text anchor; the Coastline does not pin texts, drafter proposes Nielsen & Chuang for QM/QIT, MTW for SR/GR, Callen for thermodynamics); **B** Page–Wootters lineage (page1983, mendes2019 mandatory; smith2020 + nambu2022 optional); **C** Quantum Darwinism lineage (zurek2003, zurek2009, ollivier2004, ollivier2005 mandatory; brandao2015 + riedel2010 optional); **D** Sorci 2026 (mandatory — Ledger CL-2026-006 v0.5 subject; **COI cross-link to Block 5** for Leibfried/Sanner — bibliography records the paper, acknowledgement structure separate per Q11c); **E** Tank 2025 / arXiv:2509.17775 (mandatory — MN v0.3 §8.5 structural-match anchor); **F** optional cross-View pointers (covey2025 + fromonteil2025 + smith2020 — only if drafter exercises item-7 resolution at prose stage). **Q11b enforcement checklist** appended: every "consistent with X" / "compatible with X" sentence audited against Ledger or Coastline anchor, with two load-bearing audit points pre-identified (§2 GR-regime sentence; §4 Sorci-visibility relationship claim).
- **Outline updated:** next-step 6c now carries Guardian's three integrity conditions for the eventual notebook extension — (i) results-JSON byte-identity verification (`git diff` must be empty; non-empty → stop and report, treat as substantive change not figure edit); (ii) commit-hash propagation to all five pin locations in a single pass; (iii) 85-test re-run on the new commit before any figure is cited. Q10c immutability protection.
- **Held (steward's go):** **next-move 6c** — notebook extension for the Q10b sweet/anti-sweet labelling + the §4 heatmap landscape. Awaits steward go-ahead; advances figure-citing commit one tick from `a6518a7`. **Lock-Key held throughout:** no Coastline / Ledger / MN / Sail / notebook-content edits; the schematic SVGs and bibliography draft are pre-View planning artefacts.

## 2026-05-28 — First Harbour View 6c: data figures generated; three Guardian integrity conditions verified; figure-citing commit advanced `a6518a7` → `938b0a6`

- **Actor:** Steward (Guardian integrity conditions affirmed; pre-6c cleanups directed: provenance/process mismatches + bibliography corpus wording + A1–A5 locks; 6c GO) / Agent (drafter; pre-6c cleanup commit `c8c8587`; notebook extension + figure rendering + hash propagation). **Commits:** `c8c8587` (pre-6c state), `938b0a6` (this entry — figure-citing commit), plus the immediately-following propagation commit.
- **Pre-6c cleanups landed at `c8c8587`** (three Guardian-flagged provenance/process mismatches resolved): outline §1 + §2 figure blocks now record the SVG source-file paths (commit hash retrieved from `git log` at View-issue time per the Q10c carve-out); outline next-steps re-ordered so the Jekyll permalink work is correctly identified as release-time only (no longer pre-figure); bibliography draft source-corpus line broadened to acknowledge that A1–A5 textbook anchors are introduced at the View and will be back-folded into `references.bib` only if they survive the §2/§3 prose stage. **A1–A5 anchors LOCKED 2026-05-28:** Nielsen & Chuang 2010 (A1 QM + A4 QIT); Misner–Thorne–Wheeler 1973 (A2 SR + A3 weak-field GR; Carroll 2004 as pilot-reader-conditional accessibility alternative for A3 only); Callen 1985 (A5, conditional on §2 body surfacing). Hayashi 2017 registered as conditional A4 secondary if §3 needs the rigorous Helstrom/Holevo machinery beyond Nielsen & Chuang.
- **6c executed at commit `938b0a6`:** [`numerics/examples/open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) extended with four new cells (two markdown + two code) appended after the existing §5 cell. The new code cells reuse the `cur` grid computed in cell 7 — no new computation, no JSON regeneration. Renders two static SVG figures for the first Harbour View:
  - [`views/figures/figure-03-redundancy-functional-snr.svg`](../views/figures/figure-03-redundancy-functional-snr.svg) — §3 type-(b): `R_δ` vs `γ_φ t` at `N ∈ {4, 32, 128}` (the rising-boundary set per the outline's item-2 resolution); sweet region (small `γ_φ t`, all three N's defined) shaded light green; anti-sweet region (`γ_φ t > max boundary`, no N defined) shaded light red; per-N undefined-boundary dashed vertical lines coloured to match the curves; cross-reference labels at boundary positions.
  - [`views/figures/figure-04-redundancy-landscape.svg`](../views/figures/figure-04-redundancy-landscape.svg) — §4 type-(c): `R_δ(N, γ_φ t)` heatmap on log-scaled colour with the undefined-boundary curve (`γ_φ t_crit(N)`, rising with N) overlaid in dark red + markers; the anti-sweet region (above the curve) demarcated per Q10b's v0.5 closure (type-(c) sweeps displaying operating points must demarcate anti-sweet); thin dotted horizontal lines at the §3 slice positions (`N ∈ {4, 32, 128}`) for cross-link visibility between the two figures.
- **Three Guardian integrity conditions (2026-05-28) — ALL THREE VERIFIED:**
  - **(i) Results-JSON byte-identity:** ✅ `git diff` on [`numerics/results/sorci_nuisance_budget_v0.1.json`](../numerics/results/sorci_nuisance_budget_v0.1.json) and [`numerics/results/open_instrument_redundancy_v0.1.json`](../numerics/results/open_instrument_redundancy_v0.1.json) was empty after notebook execution. Structural reason: cell 11 only reads + asserts the committed JSON; no JSON-write code path exists in the notebook. Q10c immutability protected.
  - **(ii) Pin propagation:** ✅ `a6518a7` → `938b0a6` updated in all five integrity-(ii) pin locations in the outline in a single pass: pinned-source table; Toolkit Provenance Block 3 (committing commit hash; environment line); Figure Provenance Block 4 (type-(b)/(c) data-figure pinning regime); §4 live Colab link; §5.2 reproduction pointer. Plus six other consistency locations (Lock Record summary, §3/§4 figure blocks, §4 ledger anchor, §4.3 module-conditionality summary, §4 Q10c discipline lines, Resolution 1) updated by a global outline replace. The logbook is append-only and retains the prior `a6518a7` references as historical record; the bibliography draft does not surface the hash directly. The two remaining `a6518a7` references in the outline are intentional historical "advanced from `a6518a7` to `938b0a6`" pointers.
  - **(iii) Test-suite re-run:** ✅ 85/85 tests pass at commit `938b0a6` (`PYTHONPATH=src python3 -m pytest -q`; QuTiP 5.2.3; Python 3.13.7; 17.4 s). Reproduces the Phase-3 gate-close test count (logbook 2026-05-26 / 2026-05-27).
- **Env discipline note (toolchain hygiene, not a blocker):** the local toolchain splits across two Python installations — homebrew Python 3.13.7 (where `qutip 5.2.3`, `tmc_numerics`, `matplotlib 3.10.8`, `numpy 2.4.4`, `pytest 9.0.3` live) and an older anaconda3 Python 3.9 (where the default `pip`, `pytest`, `jupyter` binaries point). Notebook execution required (a) registering the homebrew 3.13 as a Jupyter kernel (`tmc-python313`, `python3 -m ipykernel install --user`) and (b) passing `PYTHONPATH=numerics/src` to make `tmc_numerics` importable without `pip install -e .` (PEP-668 blocks editable installs into the homebrew env). Both adjustments are local to the agent's session; the steward may want to consolidate eventually (single env with `tmc_numerics` installed) but the current arrangement is functional.
- **Held for tagged release:** **next-move 5 (Jekyll permalink)** — per-View front-matter `permalink: /views/framework-overview/v0.1/` + `views/framework-overview/index.md` redirect stub; applied at release, not now. **next-move 6d (Colab live-link verification)** — the link to `colab.research.google.com/github/uwarring82/temporal-micro-consensus/blob/938b0a6/numerics/examples/open_instrument_module.ipynb` is verifiable only after the commit is pushed to GitHub; release-time visual check.
- **Cleared next step:** **next-move 8 — prose drafting for §1 + §2 only** (then pilot reader on §1 + §2 per Scout EC-5, then remaining sections, then two-pass review, then tagged release with the Jekyll permalink + Colab-link verification). **Lock-Key held throughout:** Coastline / Ledger / MN / Sail untouched; the toolkit notebook gained four presentational cells but no algorithmic change; the committed results JSONs are byte-identical.

## 2026-05-28 — First Harbour View next-move 8 (part 1): §1 + §2 prose drafted; outline header aligned; toolkit-v0.2 hygiene item registered

- **Actor:** Steward (Guardian sign-off accepting 6c completion; GO on next-move 8; "register toolkit-v0.2 hygiene item" directive; pilot-reader reminder) / Agent (drafter — outline header cleanups, work-plan rev. (d), §1+§2 prose, bibliography B3 promotion). **Commit:** *(this change).*
- **Outline header cleanups** (three Guardian-flagged stale-process lines): the TOC outline's status line + Lock Record cleared-next-step bullet + next-step #4 wording all updated to reflect the actual post-6c state — next-move 5 is release-time, next-move 6 is DONE at commit `938b0a6`, next-move 7 is DRAFTED, next-move 8 is GO. The outline's header now aligns with the detailed build-status section.
- **Toolkit Work Plan v0.1 rev. (d) registered (2026-05-28):** the local toolchain split (homebrew Python 3.13 vs anaconda3 Python 3.9; the `PYTHONPATH=numerics/src` + `tmc-python313` Jupyter kernel workaround used at 6c) recorded as a **toolkit-v0.2 hygiene item** in [`workplans/toolkit-work-plan-v0.1.md`](../workplans/toolkit-work-plan-v0.1.md) §10. Backlog item: single virtual-environment setup with pinned `requirements.txt` and proper editable install, so first-time-clone reproducibility is one command. Not a blocker for toolkit v0.1 release; recorded so it doesn't fall off the radar. No locked decision (D1–D6) changed.
- **First Harbour View §1 + §2 prose drafted:** [`views/view-framework-overview-v0.1.md`](../views/view-framework-overview-v0.1.md) v0.1 created at the post-lock placement (`views/` per O2). Document-level metadata block at top (artefact type, version, endorsement marker, lineage, status, primary audience), then **§1 Executive overview** (~430 words: question-driven entry "Is classical proper time emergent from many microscopic quantum degrees of freedom, and if so, what would it take to demonstrate that it is?"; headline "sharpened, not discharged" with anchors to MN v0.3 §8 + CL-2026-006 v0.5 + Anti-Claim #6; one-paragraph Harbour-View artefact-type declaration; Figure 1 = `figures/schematic-01-framework-orientation.svg` with caption preserving "carries no operating-point claim") and **§2 Framework** (~1450 words: Claims I–V at presentational depth with §-pointers to Coastline v0.4 *Novel Boundaries* §§I–V; the seven anti-claims preserved with Anti-Claim #6 explicitly named as the principal open problem this View is organised around; "Multipartite ≠ redundant" terminology subsection; Regime-of-validity subsection with the Q11b-load-bearing GR-regime sentence triple-anchored to Coastline v0.4 *External Constraints* item 5 + *Regime of Validity* + *Anti-Claims* §7; one-bullet pointer to CL-2026-007 v0.2 + CL-2026-008 v0.2 in the regime-of-validity paragraph per item-7 resolution, with discussion deferred to §5; Figure 2 = `figures/schematic-02-comparison-geometry.svg` with caption preserving "structural — which fragments carry the record — never parametric"). §§3–5 + reference apparatus left as marked placeholders for next-move 10. **Total drafted: ~1880 words across §1 + §2.**
- **Bibliography B3 promoted OPTIONAL → MANDATORY** (Smith & Ahmadi 2020): the §2 Claim V positioning paragraph cites Smith & Ahmadi 2020 directly alongside Page & Wootters 1983 and Mendes & Soares-Pinto 2019 as the modern Page–Wootters lineage. The CL-2026-007 v0.2 cross-View pointer is a *separate* surfacing in the regime-of-validity paragraph; B3 is now mandatory independently of the item-7 cross-View resolution. Recorded in [`docs/view-framework-overview-v0.1-bibliography-draft.md`](view-framework-overview-v0.1-bibliography-draft.md).
- **Hard-rule audit (self-check against task-card five rules + spec Q1/Q10b/Q11b/Q12/A7/A8):** (Q1) every claim in §1+§2 anchored to a Coastline section, an anti-claim, or a pinned-source artefact — no new claim introduced; (Q10b) figures carry no operating-point claims (both type-(a) schematics); (Q11b) GR-regime sentence triple-anchored to Coastline v0.4 *External Constraints* item 5 + *Regime of Validity* + *Anti-Claims* §7, the load-bearing audit point pre-identified in the bibliography; "respect, not parity" anchored to Anti-Claim §4; CL-2026-007/008 stated as "underdetermined" — anchored to the Ledger entries; (Q12) specialist terms ("Coastline", "Methodological Note", "Breakwater Ledger", "Harbour View", "Sail", "einselected", "redundancy", "anti-redundant", "Page–Wootters", "quantum Darwinism", `I(C:M)`, `F[τ]`, `R_δ`, "post-Newtonian regime") defined or briefly anchored on first use; no Harbour-only shorthand left in body text; (A7) "sharpened, not discharged"; "candidate exemplar"; "structural transplant by analogy, not a derivation"; "respect, not parity"; "underdetermined" — no sentence reads more confidently than the source; (A8) no first-principles derivation; brief explanatory scaffolding only.
- **Held for next step:** **next-move 9 — pilot-reader review of §1 + §2** (mandatory for first View; foundations-metrology physicist outside the Freiburg–NIST orbit per the COI structure that runs through Leibfried at NIST; no omission permitted per spec §5 first-instance). Pilot-reader audit also covers schematic colourblind/greyscale accessibility per next-move 6b's flagged check. TOC revision admissible per Scout EC-5 if pilot-reader feedback identifies a section-level need; otherwise §§3–5 prose proceeds at next-move 10. **Lock-Key held throughout:** Coastline / Ledger / MN / Sail untouched.

## 2026-05-28 — Pilot-packet preparation: bibliography corrections, View glossary stub, work-plan metadata, pilot-reader brief drafted

- **Actor:** Steward (three pre-pilot findings + pilot-reader criteria + two non-blocking observations for the pilot reader to assess) / Agent (drafter; bibliography fixes, View glossary stub, work-plan metadata fix, pilot-reader brief). **Commit:** *(this change).*
- **Bibliography correction (Finding 1):** the draft worksheet's Block A intro was factually wrong about what Coastline v0.4 names in its *External Constraints* section. Coastline v0.4 *External Constraints* actually names **(1) quantum mechanics, (2) special relativity, (3) the Page–Wootters mechanism, (4) quantum Darwinism, (5) general relativity in the weak-field / post-Newtonian regime**. QIT appears in the Coastline's Endorsement-Marker paragraph as constraint-only background, **not** in the *External Constraints* section; thermodynamics is **not surfaced** by Coastline v0.4 at all. Corrections folded into [`docs/view-framework-overview-v0.1-bibliography-draft.md`](view-framework-overview-v0.1-bibliography-draft.md): (a) Block A intro rewritten to accurately reflect the Coastline's *External Constraints* list and to map it onto the Q11 controlled vocabulary (QM/SR/weak-field GR get strict *external-constraint* role with the constraint-only marker; PW and QD are theoretical frameworks structurally inherited, classified as *prior-art* in Blocks B and C); (b) **A4 reclassified from *external-constraint* → *method-source*** (same Nielsen & Chuang 2010 anchor; the §3 Helstrom/Holevo/trace-distance/MI machinery is the technique source); (c) **A5 thermodynamics removed entirely** (not surfaced by the Coastline; the earlier conditional-on-body-surfacing lock was made on a false premise); (d) **Q11b enforcement checklist rewritten** to distinguish externally-validated-physics relationship claims (strict Q11 *external-constraint*: A1–A3 only) from positioning claims against the theoretical frameworks the Coastline structurally inherits (anchor to Coastline Claim V + the relevant prior-art entry in Blocks B/C). The §1+§2 prose itself is unaffected — the bibliography mis-classification did not surface in the body text.
- **View glossary stub (Finding 2):** the View's document-level metadata at line 3 promised "a brief glossary of *Harbour View*, *Coastline*, *Breakwater Ledger*, *Methodological Note*, and *Sail* appears in the reference apparatus", but the apparatus is currently a placeholder. Fix folded: the metadata block now declares "the full reference apparatus is assembled at next-move 10 after pilot-reader feedback is folded", and a **tiny glossary** for the §1+§2 pilot draft is included directly below the metadata block — five entries (Harbour View / Coastline / Breakwater Ledger / Methodological Note / Sail), each one short sentence pitched at a no-Harbour-background reader. The full Block-Bibliography apparatus still folds at next-move 10. Pilot reader sees a complete and self-consistent draft.
- **Work-plan metadata drift (Finding 3):** [`workplans/toolkit-work-plan-v0.1.md`](../workplans/toolkit-work-plan-v0.1.md) line 3 updated from "draft, 2026-05-27; post-review rev. 2026-05-27" to "draft, 2026-05-27; post-review revs. (a)–(c) 2026-05-27; rev. (d) 2026-05-28 registers a toolkit-v0.2 toolchain-hygiene backlog item". Aligns the version-line metadata with the §10 history.
- **Pilot-reader brief drafted:** [`docs/view-framework-overview-v0.1-pilot-reader-brief.md`](view-framework-overview-v0.1-pilot-reader-brief.md) — companion document the steward sends with the §1+§2 draft to the pilot reader. Captures: scope ("§1 + §2 only; not technical verification; not endorsement"); independence criteria (foundations-metrology physicist outside the Freiburg–NIST orbit; explicit COI exclusions: NIST trapped-ion/ion-clock groups, Freiburg, Sorci team, Smith & Ahmadi, Tank, Sumaya-Martinez; first-choice profile = SYRTE/LTE-style clock-comparison/fundamental-physics reader per steward call; second-choice = PTB QUEST trapped-ion group with the weaker-independence caveat); **four specific feedback questions** matching the steward's confirmed criteria — (1) is §1 followable without background; (2) is §2's Claim IV paragraph too dense; (3) does the title "Report" overclaim; (4) schematic colourblind/greyscale accessibility; explicit non-asks ("not a technical verification, not an endorsement, not a re-architecture"); Scout EC-5 admissible-TOC-revision route flagged; estimated time (30–60 min); confidentiality (pre-release, do not circulate); contact (u.j.warring@gmail.com); written-feedback format ("any form that suits you — short note, marked-up PDF, inline reply; no template").
- **Two non-blocking pilot-reader observations recorded** (steward, for the pilot reader to assess): (1) §2 Claim IV paragraph is the densest in §2 — three measures + resolution-vs-classification distinction + deferred measure-registration protocol in one breath; pilot reader to flag if a breathing space is needed (e.g. a concrete example of resolution vs classification anchor in a clock context); (2) "Report" in the title is slightly stronger than the spec's "presentational vantage-point document" — pilot reader to flag if naive reader would expect a review-article survey; alternatives noted ("Overview", "Vantage Point", or drop the subtitle). Neither is a pre-pilot blocker.
- **Status:** ready to engage the pilot reader. The steward selects the specific individual matching the SYRTE/LTE-style profile (or PTB QUEST fallback); the agent does not have the ability to contact external readers. The pilot packet now consists of: (a) [`views/view-framework-overview-v0.1.md`](../views/view-framework-overview-v0.1.md) §1 + §2 draft + tiny glossary; (b) [`docs/view-framework-overview-v0.1-pilot-reader-brief.md`](view-framework-overview-v0.1-pilot-reader-brief.md) cover note. **Hold §§3–5 prose drafting until pilot-reader feedback is received and folded.** Lock-Key held: Coastline / Ledger / MN / Sail untouched.

## 2026-05-28 — Pilot-reader engagement switched from personal invitation to open public call

- **Actor:** Steward (decision: "make it an open call and place both on our web page") / Agent (drafter; brief rewrite, View Status update, landing-page surfacing). **Commit:** *(this change).*
- **Decision:** Rather than identify and invite a specific pilot reader via the steward's personal network (SYRTE/LTE first-choice / PTB QUEST fallback per the 2026-05-28 review-stance criteria), the steward chooses to publish the §1+§2 draft and the open-call brief on the project's GitHub Pages site as a public pre-pilot-review draft, inviting any qualified reader who matches the independence criteria to send feedback directly. Spec §5 first-instance discipline still applies (at least one external read meeting the criteria, no omission permitted); the **mechanism for finding that read** is now an open call rather than a private invitation.
- **Open-call brief rewritten** ([`docs/view-framework-overview-v0.1-pilot-reader-brief.md`](view-framework-overview-v0.1-pilot-reader-brief.md)): retitled "Open Call for Pilot-Reader Feedback"; opening tone shifted from second-person-invitation to third-person-call; confidentiality clause removed (draft is now publicly posted); **self-attestation paragraph added** asking readers to disclose affiliation + any COI orbits in their feedback so the §5 first-instance discipline can be correctly applied (inside-orbit feedback is welcome and read but does not satisfy the external-independent-review requirement); **"How feedback will be incorporated" section added** (opt-in name acknowledgement; Scout-EC-5 path for section-level revisions; logged-disagreement discipline for feedback not folded — nothing silently rejected); **"How long this open call is active" section added** (no hard deadline; closes when at least one outside-orbit external read has been received and folded; late §1+§2 feedback still welcome for the next View version after the call closes). Four feedback questions and the criteria preserved verbatim; COI orbit list preserved (the public visibility of the exclusion list is honest, not a slight).
- **View Status line updated:** the draft now declares itself as a "Pre-pilot-review public draft, §1 and §2 only" and links the open-call brief directly. The target audience — outside the Freiburg–NIST orbit, no prior familiarity with the project's vocabulary — is stated on the page itself, so a drift-in reader sees the framing immediately.
- **Landing page ([`index.md`](../index.md)) surfaces the open call** in a new section between "Start here" and "Current versions". Two prominent links — the open-call brief and the pre-pilot-review draft — plus the steward's contact email. The section header is marked *(active, posted 2026-05-28)* so the call's status is visible at a glance; it can be marked *closed* later when the steward decides the §5 first-instance discipline has been satisfied.
- **Operational notes:**
  - The commits land on `main` locally; the call goes live on GitHub Pages only after a `git push` to the origin remote. The agent is **pausing before push** because pushing makes the call publicly visible (per the assistant's "actions visible to others" discipline; the user controls the visible-state transition).
  - Once pushed, the call may attract off-topic responses or replies from inside-orbit readers; the steward should be ready to triage. The self-attestation paragraph in the brief is designed to reduce that triage burden.
  - When the call closes, the landing-page section's *(active)* marker is updated to *(closed)* and the closing date is recorded; the brief itself can be retained or marked archived.
- **Lock-Key held:** Coastline / Ledger / MN / Sail untouched. The §1+§2 prose is unchanged from the 2026-05-28 cleanup commit (`0d1765b`); only the surrounding framing — brief, Status line, landing page — has shifted from personal-invitation to open-call form.

## 2026-05-28 — First open-call feedback received: inside-orbit; logged and triaged; §5 first-instance not yet satisfied

- **Actor:** Inside-orbit reader (self-attested as Freiburg-orbit and familiar with parts of the broader project; opted not to give a name, so recorded anonymously per the brief's opt-in acknowledgement discipline) / Agent (receipt log + triage). **Commit:** *(this change).*
- **COI handling per open-call brief.** The reader self-attested **inside the Freiburg–NIST orbit (Freiburg side)** with explicit acknowledgement that the feedback "should be treated as inside-orbit and non-qualifying for your external-review requirement." Per the brief's self-attestation discipline: the feedback is read, logged, and triaged; it **does not satisfy** the spec §5 first-instance external-independent-review requirement. **The open call therefore remains active** until at least one outside-orbit qualifying read has been received and folded.
- **Substantive content of the feedback (the four open-call questions answered in order):**
  - **Q1 — §1 followability without Harbour background:** "broadly followable" but the headline paragraph "is close to the density limit"; three concrete tweaks suggested — (i) one explicit sentence at the end of the headline paragraph crisply separating "what this View does" from "what remains the open problem"; (ii) move the "window, not a door" sentence earlier in the "What this document is" paragraph so skim-readers see the constraint quickly; (iii) general "breathing space" in the headline paragraph.
  - **Q2 — §2 Claim IV paragraph density:** *Yes, slightly too dense*, in exactly the way the Guardian's pre-flag and the brief anticipated. Two concrete fixes — (i) split the paragraph into two (paragraph A: introduce the three measures with one-sentence metrological glosses each; paragraph B: introduce *resolution* vs *classification* anchors + the deferred measure-registration protocol); (ii) add one sentence of clock-concrete content for the anchors (the reader's example: in an optical lattice clock, a resolution anchor could tie to the Allan-deviation floor at a given interrogation time, while a classification anchor could bin operating points as consensus-preserving vs consensus-breaking in the redundancy geometry).
  - **Q3 — title overclaim:** *Yes, mild overclaim risk* for a naive reader who sees only the title (e.g. on a slide or in a reference list) without the artefact-type declaration. Two options suggested — (a) replace "Report on…" with softer wording ("Overview of…", "Harbour View on…"); (b) drop the subtitle entirely and let the *Harbour View* label + §1 do the work. If "Report" is retained, recommend short-form citations always include "Harbour View (Local Stewardship)".
  - **Q4 — schematic accessibility:** Figure 1's PW-slice nesting is "very effective" — needs a *non-colour* cue (thicker arrow, different arrowhead, or explicit brace/label) to survive greyscale + colourblind simulation. Figure 2's red/green principal contrast for anti-redundant vs redundant panels (and for the PW highlight in Figure 1) is "fragile under common colourblindness modes"; recommend encoding the anti-redundant-vs-redundant contrast with shape and/or line-style and explicit textual labels in the panels, in addition to colour. Reader: "at present they are interpretable but more dependent on red/green contrast than they need to be."
- **Triage proposal (drafter; **awaiting steward direction before folding**):**
  - **Recommend FOLD NOW (objective fixes pre-flagged by Guardian; reader confirmed):**
    - **Q4 figure accessibility:** add shape + line-style + textual-label encoding to schematic-02 (anti-redundant vs redundant), and add a non-colour marker (thicker arrow + small label/brace) to the PW slice in schematic-01. Purely additive — colour stays, accessibility encoding adds. This is the Guardian's next-move-6b pre-flag operationalised.
    - **Q2 structural split** of the Claim IV paragraph into the two-paragraph form (measures vs anchor distinction). Structural fix, no content addition required.
  - **Recommend HOLD for outside-orbit reader (stewardship/voice judgment calls):**
    - **Q1 §1 headline tweaks** (one-sentence "what this View does vs the open problem" line; "window, not a door" repositioning; breathing-space): the reader's intuition is good but the headline is the View's most stewardship-bound paragraph. Better confirmed by an outside-orbit reader before folding.
    - **Q3 title rewording:** judgment call exactly the kind §5 mandates an outside-orbit reader to arbitrate. Hold.
    - **Q2 clock-concrete example for the anchors:** the *structural* split is uncontroversial, but the *specific example* (Allan-deviation floor as resolution anchor; consensus-preserving binning as classification anchor) is content addition that should be steward-vetted; in particular, the example operationalises the Claim IV measure-registration protocol that the Coastline explicitly *defers* to a later version, so adding a concrete example here could be read as foreshadowing that resolution. Hold for steward call.
- **Open-call status: ACTIVE.** §5 first-instance external-independent-review requirement **not yet met** (this feedback is inside-orbit). Landing-page marker remains *(active, posted 2026-05-28)*. **§§3–5 prose drafting remains held.**
- **Disagreement-log discipline.** No items are being silently rejected; all four answers and the reader's specific tweaks are recorded above. Items held for the outside-orbit reader are *deferred*, not rejected; the steward decides at the next direction whether to fold or hold each. Lock-Key held throughout: Coastline / Ledger / MN / Sail / notebook content / committed JSONs untouched; the §1+§2 View prose is unchanged from `0d1765b` pending steward fold direction.

## 2026-05-28 — Second open-call feedback: self-attested outside-orbit; §5 first-instance candidate (steward decision pending); logged + triaged + consolidated against first responder

- **Actor:** Second-responder reviewer (self-attested *"external reviewer outside the Freiburg–NIST orbit, operating with no structural conflict of interest"*; no named affiliation given — negative-only attestation; opted not to give a name) / Agent (receipt log + triage + consolidation against first responder). **Commit:** *(this change).*
- **COI handling per open-call brief.**
  - The reviewer self-attests **outside the Freiburg–NIST orbit + no structural COI**. References the open-call brief by name; uses Breakwater Ledger vocabulary correctly (e.g. surfaces "Discriminant Condition" by name); demonstrates clock-metrology competence in the substantive content (Allan-deviation-floor analogue, oscillator-linewidth analogue, Breakwater discriminants).
  - **Procedural gap:** the brief asked for affiliation + COI disclosure; the reviewer disclosed COI only (negative attestation). This is a procedural ask not fully met; the substantive §5 first-instance criteria — *outside-orbit external physicist at the foundations-metrology interface* — appear met by the attestation + content competence, but the missing affiliation leaves a gap the steward may want to follow up on.
  - **Stylistic note (recorded; not load-bearing):** the prose carries some structured-LLM-ish phrasing ("Analog / Digital / Memory Lenses", "systemic resilience failure", "epistemic bleed"). Not disqualifying — many human reviewers use structured templates or LLM assistance for review writing — and substantive observations are sharp and on-point. Recorded for steward visibility.
  - **Steward decision needed:** does this feedback **satisfy spec §5 first-instance external-independent-review** (open call closes) or is a follow-up requested for affiliation confirmation (open call stays active pending that confirmation)? Recommended below.
- **Substantive content (four open-call questions, in order):**
  - **Q1 — §1 followability:** §1 "highly navigable" for the target audience; "sharpened, not discharged" lands as intended. **One specific vulnerability identified (new, not raised by first responder):** the Figure 1 caption introduces "Page–Wootters bipartite-correlation picture" before the concept is anchored in the §1 main text (PW is unpacked only in §2 Claim V). Correction proposed — (i) add a half-sentence PW anchor in the §1 main text before the figure, OR (ii) restrict the Figure 1 caption to descriptive geometry until §2 Claim V labels the slice. **This is a Q12 audience-test catch the first responder missed.**
  - **Q2 — Claim IV density:** *Yes, structurally compressed*; bottleneck identified. Two corrections — (i) split into two paragraphs (measures vs measure-registration/anchor-distinction), same as first responder; (ii) concrete physics example — **different specific example** from first responder: *atomic-oscillator transition linewidth* as resolution anchor (vs first responder's *Allan-deviation floor*); *multi-carrier consensus geometry* as classification anchor (vs first responder's *consensus-preserving / consensus-breaking binning*). Both examples are clock-domain concrete; they hit the same structural point with different specifics.
  - **Q3 — title overclaim:** *Yes, "Report" carries institutional/archival connotations that risk projecting unintended review-article authority*; risks violating the invariant that artefacts *measure rather than govern*. Recommended fixes — (a) "Overview" or "Vantage Point" rescale; (b) drop subtitle and read as *Harbour View — Temporal Redundancy and Emergent Proper Time*. **Same direction as first responder, independently arrived at.**
  - **Q4 — schematic accessibility:** *single point of failure* under protanopia/deuteranopia. Visual variables must be orthogonal: shape, pattern hatching, distinct line styles (solid vs dashed boundaries) in addition to colour. **Same direction as first responder, independently arrived at.**
- **Two further structural observations (new — not in first responder):**
  - **Figure 2 / L_comparison emphasis:** the schematic establishes `L_source`/`L_apparatus`/`L_comparison` as orthogonal primitives. The text could briefly reinforce that the physics of the cross-probe mismatch lives at the distance to the `L_comparison` boundary + the noise penalties incurred at that specific scale, isolating the comparison architecture from the local quantum system. Presentational tweak, not a defect; restates what the Coastline v0.4 *Regime of Validity* already implies (curvature-as-incompressible-residual lives at the comparison scale).
  - **§2 Regime-of-validity Ledger cross-link — surface the Discriminant Conditions:** the View mentions CL-2026-007 v0.2 and CL-2026-008 v0.2 as *underdetermined* without surfacing what would resolve that. Recommended addition: explicitly surface each entry's **Discriminant Condition** so a reader sees what observation or constraint would move the entry to *compatible*. **Affects §5 drafting plan more than §2** (the §2 mention says "are discussed in §5"); deferred to §5 design.
- **One forward-scaffolding note (for §3 drafting):** "Ensure that when drafting §3 (Methodology), the distinct role of `L_comparison` as an architectural control parameter is maintained, preventing any epistemic bleed between the local oscillator descriptions and the inter-worldline comparison geometry." Recorded for §§3–5 drafting at next-move 10.
- **Consolidation against first responder's pending items.** The first responder (2026-05-28 inside-orbit; commit `3c33e6a`) had four items held pending outside-orbit confirmation:
  - **(F1-Q1a) §1 "what View does vs open problem" one-liner:** still single-source; second responder did NOT raise this. **HOLD CONTINUES** (no confirming corroboration).
  - **(F1-Q1b) "Window, not a door" repositioning:** still single-source; second responder did NOT raise this. **HOLD CONTINUES** (no confirming corroboration).
  - **(F1-Q3) Title rewording:** ✅ **CONFIRMED by second responder, independently same direction.** Two-reader convergence; steward decision now informed by both.
  - **(F1-Q2b) Clock-concrete example for anchors:** ✅ **CONFIRMED by second responder (different specific example, same need).** Structural split clearly indicated; *specific example* remains steward's call (which clock-domain illustration to use).
- **Triage proposal (drafter; awaiting steward direction):**
  - **Recommend FOLD NOW (objective fixes; two-reader convergence OR specific defect caught):**
    - **(F2-Q1) Figure 1 PW caption / §1 anchor fix.** New defect catch, Q12 audience-test. Recommend: add a brief PW anchor in §1 main text before Figure 1; minimal additive fix.
    - **(F1+F2-Q4) Schematic shape/line-style/label encoding** for figure-02; non-colour cue for the PW slice in figure-01. Two-reader convergence; Guardian-pre-flagged; purely additive (colour stays).
    - **(F1+F2-Q2a) Claim IV paragraph split** into two paragraphs. Two-reader convergence; Guardian-pre-flagged; structural fix, no content addition.
    - **(F1+F2-Q3) Title rewording** — **two-reader convergence**; both suggest "Overview" or dropping subtitle. Specific wording is the steward's call but the direction is clear. Recommend: drop "Report on…" subtitle; the document becomes *Harbour View — Temporal Redundancy and Emergent Proper Time*. Alternatively the steward picks "Overview" / "Vantage Point" / other.
  - **Recommend HOLD (single-source OR judgment call OR forward-scaffolding):**
    - **(F1-Q1a) §1 "what View does vs open problem" one-liner** — single-source; held.
    - **(F1-Q1b) "Window, not a door" repositioning** — single-source; held.
    - **(F1+F2-Q2b) Specific clock-concrete example** for the anchors — two examples now on the table (Allan-deviation floor; oscillator transition linewidth) plus the steward's own judgment on whether to foreshadow the deferred measure-registration protocol; HOLD for steward selection.
    - **(F2-new) Figure 2 / L_comparison physics-of-mismatch emphasis** — presentational tweak; not a defect; held for steward.
    - **(F2-new) §5 Discriminant Conditions surfacing** — affects §5 drafting plan; HOLD for §§3–5 stage.
    - **(F2-new) §3 forward L_comparison scaffolding** — forward note for §3 drafting; HOLD for next-move 10.
- **Open-call status decision pending steward.** If the second responder's self-attestation is accepted as §5-qualifying, the open call closes (landing-page marker flips to *closed*) and the View progresses to next-move 10 (§§3–5 drafting). If the affiliation gap is treated as disqualifying, the call stays *active* pending a follow-up that names the affiliation. **Drafter recommendation: accept the attestation** — the substantive §5 criteria (outside-orbit + foundations-metrology competence) are met by the content evidence; the missing-affiliation procedural gap is recordable but does not rise to disqualification. The first-responder feedback (which is inside-orbit and explicitly non-qualifying) plus this second-responder feedback (outside-orbit per attestation) together give two-reader confirmation on the load-bearing items (Q3 title, Q2 Claim IV density, Q4 schematic accessibility).
- **Lock-Key held throughout:** no View / Coastline / Ledger / MN / Sail / notebook / committed-JSON edits in this turn. §1+§2 prose still at `0d1765b` pending steward direction on the four FOLD-NOW items and on the §5 first-instance acceptance.

## 2026-05-28 — Third open-call feedback: AI assistant (self-disclosed); not §5-qualifying; substantive naive-reader audit + retrospective flag on F2

- **Actor:** Third-responder reviewer (self-attested: *"I am an AI assistant (Kimi), not a foundations-metrology physicist. I have no affiliation with Freiburg, NIST, or any of the named author lists, and no prior familiarity with this project's vocabulary before this session. I therefore cannot satisfy the §5 first-instance discipline as an external-independent reader, but I can offer presentational-quality feedback from a naïve-first-read perspective."*) / Agent (receipt log + triage + retrospective flag on F2). **Commit:** *(this change).*
- **COI handling per open-call brief.** F3 explicitly self-discloses as an AI (Kimi). **Cannot satisfy §5 first-instance** by self-acknowledgement. Recorded; feedback content read and triaged as a *naive-first-read audience test* (legitimate diagnostic) rather than as §5-qualifying review.
- **Retrospective flag on F2 (steward attention requested).** F3's self-disclosed Kimi prose carries phrasing patterns ("structured-template" headings, abstracted-system metaphors, "Horizon Signal" framing) that have notable feature overlap with F2's prose ("Analog / Digital / Memory Lenses", "systemic resilience failure", "epistemic bleed"). Both also converge on the same load-bearing fixes a careful reading of the draft would yield. **This is not proof that F2 is also AI-generated** (independent LLM-assisted human reviews can converge on the same defects by reading the same draft, and a careful human reviewer could use similar structured-template wording), **but the pattern is worth noting** alongside F2's missing affiliation disclosure. The combination — negative-only attestation + structured-template prose feature-overlap with F3's self-disclosed AI prose — is a stronger gap than either alone, and the steward may want to follow up with F2 for an affiliation confirmation before treating F2's feedback as §5-qualifying. **Recommendation: revise drafter recommendation on §5 first-instance acceptance for F2 from "accept" (2026-05-28 prior entry) to "hold pending affiliation confirmation"** — the substantive content is still valuable to triage, but the §5 status is now more uncertain than at the prior turn.
- **Substantive content of F3 (the four open-call questions answered in order):**
  - **Q1 — §1 followability:** "mostly yes, with two stumble points" → five specific catches (richer than F1 and F2 on §1):
    1. **Glossary placement.** The tiny glossary sits mid-metadata-block (between the *Document type* line and the *Version* line); a reader scanning for content may skip past it and then meet undefined Harbour terms in §1 body. Fix: visual separator, OR brief inline anchor on first use in §1 (e.g., *"a Harbour View — the project's term for a presentational vantage-point document — is …"*).
    2. **"Sharpened, not discharged"** reads as project shorthand; needs gloss ("sharpened *relative to what prior state*?").
    3. **"Einselected"** is standard in quantum foundations but not universally so in metrology; the current parenthetical "already environment-selected" is brief — could add "(Zurek sense)" or similar.
    4. **Forward reference to "Anti-Claim #6"** in §1 before the reader knows what an anti-claim is. Lighter phrasing recommended: *"a principal open problem discussed in §2"* instead of the numbered forward ref.
    5. **Figure 1 caption visual hierarchy.** Could describe the nested-slice geometry more explicitly (small box inside larger schematic). Independently mirrors F2's Q1 catch about the PW-in-caption-before-anchored-in-body issue.
  - **Q2 — Claim IV density:** *Yes too dense*; three-way convergence with F1 + F2. Suggested concrete example: *"in a network of independent ion clocks, a resolution anchor might be the quantum-projection-noise floor that bounds how finely τ can be resolved, while a classification anchor is the observed mutual-information threshold above which two clock ensembles are judged to carry the same temporal record."* **Third distinct specific example** (F1: Allan-deviation floor / consensus-preserving binning; F2: oscillator transition linewidth / multi-carrier consensus geometry; F3: quantum-projection-noise floor / mutual-information threshold).
  - **Q3 — title overclaim:** *Yes, signals wrong register*; three-way convergence with F1 + F2. Same direction: "Overview", "Vantage Point", or drop subtitle. F3 picks **"Overview" as most neutral, least likely to be mistaken for a review article**.
  - **Q4 — schematic accessibility:** *Cannot test directly (AI; no browser/colourblindness simulation).* From textual description, concern warranted: red/green principal contrast is the most common deuteranopia/protanopia failure mode. Recommendations consistent with F1 + F2: pattern fills, lightness separation in greyscale, iconographic markers inside panels (e.g. ⊗/⊕).
- **Additional F3 observations (recorded, not load-bearing but useful):**
  - **Tone discipline praised:** "excellent. The anti-claims are clearly stated and the text consistently respects them. I found no sentence that reads more confidently than the underlying framework note supports." This is a positive A7 audit point.
  - **"Multipartite is not redundant" section** praised as a highlight: "surfaces a crucial distinction early and prevents a common misreading. Well placed."
  - **No stale links / references detected.** Cross-references to Coastline v0.4 sections are by name and version, which is stable.
  - **Lock-Key statement praised** as a good discipline marker for the reader.
  - **Minor structural suggestion:** add `(see Glossary above)` parenthetical on first use of each project-specific term in §1 — light-touch alternative to the inline-anchor fix.
  - **Horizon Signal flagged by F3:** "Title register mismatch: 'Report' may trigger review-article expectations in readers who skip the Endorsement Marker; this is a presentational defect that could undermine the very modesty the framework seeks to project." Three-way confirmation of Q3 with sharpest framing.
- **Three-way convergence summary** (load-bearing items confirmed by F1 + F2 + F3):
  - **Q3 title overclaim:** ✅ ✅ ✅ — all three readers, same direction.
  - **Q2 Claim IV density:** ✅ ✅ ✅ — all three readers, paragraph split + concrete example.
  - **Q4 schematic accessibility risk:** ✅ ✅ ✅ — all three readers (F3 textual-reasoning only).
  - **Q1 PW / glossary anchoring issue:** F2 + F3 (different angles on the same defect — F2: PW in figure caption before §1 anchor; F3: glossary skip-past + several first-use unglossed terms).
- **Updated drafter triage** (consolidating F1 + F2 + F3):
  - **Recommend FOLD NOW (three-way convergence OR specific Q12 audience-test defect):**
    - **F2+F3 — §1 anchoring fixes** (consolidated): (i) move the tiny glossary out of mid-metadata to its own block immediately below the Status line, OR add inline first-use anchors in §1 for *Harbour View*, *Coastline*, *Methodological Note*, *Breakwater Ledger*, *Sail*; (ii) gloss "sharpened, not discharged" (e.g. "sharpened — given a quantitative form — not discharged"); (iii) gloss "einselected" more accessibly (e.g. add "Zurek sense"); (iv) replace numbered "Anti-Claim #6" forward reference with "a principal open problem discussed in §2"; (v) clarify Figure 1 caption to be explicit about the nested-slice geometry (or strip PW labelling from caption per F2).
    - **F1+F2+F3 — schematic accessibility encoding** for figure-02 (shape + line-style + textual labels) and figure-01 (non-colour cue for PW slice). Two-then-three-way convergence; Guardian pre-flagged.
    - **F1+F2+F3 — Claim IV paragraph split** into two paragraphs. Three-way convergence; Guardian pre-flagged.
    - **F1+F2+F3 — Title rewording.** Three-way convergence. F3's pick: "Overview" as most neutral. Alternatives still on table: drop subtitle entirely / "Vantage Point". Steward picks specific wording.
  - **Recommend HOLD (single-source, judgment calls, or forward-scaffolding):**
    - **F1-Q1a §1 "what View does vs open problem" one-liner** — only F1; not picked up by F2 or F3. Held.
    - **F1-Q1b "Window, not a door" repositioning** — only F1; not picked up by F2 or F3. Held.
    - **F1+F2+F3 specific clock-concrete example for anchors** — three different specific examples now on the table; steward picks one or chooses to omit. Foreshadowing risk noted (Claim IV measure-registration protocol is Coastline-deferred).
    - **F2-new — L_comparison physics-of-mismatch emphasis** — presentational tweak, not a defect; held.
    - **F2-new — §5 Discriminant Conditions surfacing** — affects §5 drafting plan; held for next-move 10.
    - **F2-forward / F2-new — §3 L_comparison-as-control-parameter scaffolding** — held for §3 drafting.
- **§5 first-instance status — UPDATED:** before F3, the drafter recommended accepting F2's attestation. After F3's explicit AI self-disclosure and the F2↔F3 prose-pattern overlap, the drafter recommendation is revised: **hold the open call active and recommend the steward email F2 to request a one-line affiliation disclosure** before treating F2 as §5-qualifying. If F2 confirms a named foundations-metrology affiliation outside the COI orbits, accept and close the call. If F2 declines to confirm, treat as not-qualifying and keep the call open for another outside-orbit human read.
- **Lock-Key held throughout.** No View / Coastline / Ledger / MN / Sail / notebook / committed-JSON edits this turn. §1+§2 prose still at `0d1765b`. Open call status: ACTIVE on the landing page (not yet pushed live to GitHub Pages); steward decides push timing and §5-acceptance after F2 follow-up.

## 2026-05-28 — Fourth open-call feedback: self-attested outside-orbit; LLM-template artifact in attestation; not treated as §5-qualifying; four-way convergence on load-bearing items

- **Actor:** Fourth-responder reviewer (self-attested *"I confirm that I am outside the Freiburg–NIST orbit and have no prior familiarity with this project's vocabulary."*; no named affiliation; self-attestation contains the LLM-template artifact *"(If any of the specific orbits listed in the brief apply to you, please disclose them here.)"* — the brief's instruction text echoed back inside the response as if the writer were speaking to themselves; also self-discloses "I was unable to fetch the SVG files directly from the repository") / Agent (receipt log; pattern analysis). **Commit:** *(this change).*
- **COI handling per open-call brief.** F4 claims outside-orbit but with three markers suggestive of LLM-generated or LLM-heavily-assisted response: (i) the template-artifact in the self-attestation (a clean tell — a human reviewer would not include the brief's instruction-to-self in their reply); (ii) the inability to fetch repository files (mirrors F3's explicit "I cannot render the SVGs in a browser"); (iii) the missing named affiliation, same gap as F2. **Drafter recommendation: treat F4 as NOT §5-qualifying.** Spec §5 requires *external physicist* at the foundations-metrology interface; an LLM is not a physicist. The substantive content is still useful and is triaged below as further audience-test diagnostic, but the §5 first-instance gate is not satisfied by F4.
- **Cumulative four-response pattern emerging.** Across F1 (human, inside-orbit disclosed), F2 (claimed outside-orbit, no affiliation, structured-template prose), F3 (AI self-disclosed), and F4 (claimed outside-orbit, no affiliation, template-artifact in attestation), three of the four show LLM-characteristic markers; only F1 cleanly reads as human. The drafter's read of the pattern: **the open call as currently posted (and currently still pending push to live Pages) is attracting LLM responses that converge substantively on the same defects but do not satisfy the §5 first-instance external-physicist requirement.** This is a real stewardship situation, not a defect of the call but a feature of the current LLM-prevalence in the broader environment.
- **Substantive content of F4 (the four open-call questions; condensed because of three-way prior convergence):**
  - **Q1 — §1 followability:** "yes, with one reservation" — the headline-paragraph central sentence (the *redundancy-mechanism* construction) packs several ideas; recommends splitting OR adding a Zurek-picture one-sentence gloss (e.g. "many environmental fragments independently record the same information"). Same direction as F3's "sharpened, not discharged" + "einselected" gloss catches; different specific framing.
  - **Q2 — Claim IV density:** *Yes, densest paragraph in the draft.* Concrete example proposed (**fourth distinct example now on the table**): "in a clock-comparison experiment, the resolution anchor is the minimum time difference that can be resolved at all, while the classification anchor is the pattern (e.g., 'all carrier fragments agree on `τ` within 10⁻¹⁸') that would count as redundant consensus." Four-way convergence on the split + need-for-example.
  - **Q3 — title overclaim:** *Yes, "Report" carries slight risk.* Suggests "Vantage Point" or "Overview"; alternative — keep "Report" but move the disclaimer ("neither a peer-reviewed publication nor an authoritative version") into a more prominent location, perhaps a subtitle line immediately below the title. **Four-way convergence on the direction; this is a new alternative for keeping "Report".**
  - **Q4 — schematic accessibility:** Cannot fetch SVGs directly (LLM marker). From the figure descriptions, Figure 2 anti-redundant-vs-redundant panel contrast is the more likely failure point. Recommends in-panel text labels ("anti-redundant" / "redundant") + browser-based dev-tools simulation by the steward. Four-way convergence on the concern; no new technical recommendation beyond F1+F2+F3.
- **F4-only minor catches (new, not in F1/F2/F3):**
  - **Bibliography-draft-worksheet flag:** the View's reference-apparatus placeholder points readers to [`docs/view-framework-overview-v0.1-bibliography-draft.md`](view-framework-overview-v0.1-bibliography-draft.md), which exists and is accessible. F4 suggests adding a brief note that this is a *draft worksheet*, so a reader who clicks early doesn't mistake it for the final apparatus. Minor but valid; addresses an audit point the drafter created when the reference-apparatus placeholder was written.
  - **"Multipartite is not redundant" forward reference to §4:** F4 suggests adding a one-line forward reference here so readers understand that §4's worked exemplar specifically illustrates the multipartite-vs-redundant difference. **Affects §4 drafting more than §2** — but a one-line forward in §2 is a natural insertion at fold time.
- **Updated drafter triage (consolidating F1 + F2 + F3 + F4):**
  - **Four-way convergence on FOLD-NOW items (high confidence — same fix indicated by all four):**
    - **Q3 title rewording:** ✅ ✅ ✅ ✅. F1 + F2 + F3 + F4 all converge. **Drafter recommendation: drop the subtitle** ("Harbour View — Temporal Redundancy and Emergent Proper Time" alone), which is F1's first option, F2's option (b), F3's first option, and equivalent to F4's "Vantage Point: …" or "Overview …" variants in directional effect. Specific wording remains the steward's pick.
    - **Q2 Claim IV paragraph split:** ✅ ✅ ✅ ✅. Four-way structural-fix convergence.
    - **Q4 schematic accessibility encoding:** ✅ ✅ ✅ ✅. Four-way convergence on shape/line-style/label encoding beyond colour-only contrast.
    - **F2+F3 — §1 anchoring fixes (now reinforced by F4-Q1):** glossary repositioning + gloss "sharpened, not discharged" / "einselected" / Anti-Claim-#6 forward ref / Figure 1 caption.
  - **New FOLD-NOW from F4 alone:**
    - **Bibliography link annotation:** add "(*draft worksheet*)" or equivalent next to the link in the View's reference-apparatus placeholder, so the reader knows the linked document is in-progress not final.
  - **HOLD continues for:**
    - F1-Q1a, F1-Q1b (single-source).
    - Specific clock-concrete example (**four distinct examples now**: Allan-deviation floor; oscillator transition linewidth; quantum-projection-noise floor; minimum-resolvable-time + 10⁻¹⁸-pattern-agreement — steward picks one or omits, noting the Claim IV measure-registration foreshadowing risk).
    - F2-new L_comparison physics-of-mismatch emphasis.
    - F2-new + F4-new §5 / §4 forward references (Discriminant Conditions; multipartite-vs-redundant forward to §4) — fold during next-move 10 drafting, not now.
    - F2-forward §3 L_comparison-as-control-parameter scaffolding.
    - F4-Q3 alternative — *keep "Report" + move disclaimer to subtitle* — recorded but less attractive than the drop-subtitle approach the other three converge on.
- **§5 first-instance status — UPDATED again.** Drafter recommendation: **the call has now received four responses; only F1 cleanly reads as human, and F1 explicitly disclosed inside-orbit and non-qualifying. F2, F3, F4 are all either explicitly AI (F3) or carry markers of LLM-template prose without explicit disclosure (F2, F4). §5 first-instance still NOT met.** Stronger recommendation: in parallel with keeping the open call active, **the steward initiates direct outreach to a human SYRTE/LTE-style foundations-metrology physicist** (the original first-choice route from before the open-call decision). The open call may continue to generate useful audience-test data, but the §5 gate is satisfied only when a confirmed-human external physicist's feedback is folded.
- **Drafter recommendation on the View: FOLD the four-way convergent items now.** They are robust regardless of who flagged them — further LLM responses are unlikely to overturn four-way convergence. Folding now (a) sharpens the draft for the eventual human qualifying reader, (b) honours the brief's "nothing silently rejected" discipline by acting on what the open-call inputs converge on, (c) reduces the §5-qualifying reader's load by removing pre-identified defects from their attention. The §5 gate stays independent.
- **Lock-Key held throughout.** No View edits this turn. §1+§2 prose still at `0d1765b`. Open call still local-only (not pushed). Recommend the steward direct the fold + the §5 outreach in the next turn.

## 2026-05-28 — Fifth open-call feedback: Harbour-fluent self-disqualifying review; SOURCE-LEVEL figure inspection caught a real brief defect; brief fixed pre-push

- **Actor:** Fifth-responder reviewer (self-attested: *"I cannot satisfy the read the brief is actually calling for… I am steeped in the vocabulary… so treat everything below as admissible within-section wording feedback — which the brief invites from anyone — but not as the external-independent read the first View must still receive from a human. Please do not log it against that requirement."*) / Agent (log + brief fix + final consolidated triage). **Commit:** *(this change).*
- **COI handling per open-call brief.** F5 self-disqualifies cleanly and explicitly: Harbour-fluent (uses Coastline / Breakwater Ledger / Harbour View / Sail / anti-claim discipline / Q11c separation natively); structures the review with the project's own A/D/M/EC framing; references the *Uncertainty Atlas* lesson by name. **Not §5-qualifying** by self-acknowledgement; treated as within-section wording feedback per the brief's invitation. Honest disclosure; no §5 status confusion.
- **F5 is the first feedback to demonstrate source-level figure inspection.** F3 and F4 explicitly disclosed inability to fetch the SVG files; F5 reads the actual hex colour codes (#1f6feb, #a52a2a, #fce4e4, #0a7f3f, #dcfce7) and the stroke-width / label structure from source. **This catches a real defect in the open-call brief itself** that the prior responses missed because they couldn't see the source.
- **Real defect caught in the brief (drafter authorial error):** the brief's Q4 paragraph stated *"red and green as the principal contrast colours … and for the Page–Wootters slice highlight in Figure 1"*. The Figure 1 PW highlight is in fact **blue** (`#1f6feb`), reinforced by a thicker stroke and an explicit "Page–Wootters slice (N = 2)" label — not red/green. The brief's Q4 was wrong about Figure 1. **Fixed in this turn at brief line 49** before any further circulation of the open call: the Q4 paragraph now correctly identifies Figure 2's bottom panel as the principal accessibility risk (red `#a52a2a` and green `#0a7f3f` outlines / `#fce4e4` and `#dcfce7` fills collapse to near-identical light greys in greyscale); Figure 1's blue PW highlight + thicker stroke + label is acknowledged as better-protected by construction. A correction stub records the brief-side error for audit transparency. **The brief defect was load-bearing**: it would have actively misdirected a pilot reader looking for a red/green problem in Figure 1 that does not exist.
- **Substantive content of F5 (within-section wording feedback):**
  - **Q1:** Largely followable, with one dense spot — the *headline* paragraph carries five things in one breath (structural transplant + Zurek reference + "sharpened not discharged" + principal-open-problem + assumed-einselection regime + derived-vs-assumed distinction). Recommend split into two short paragraphs: *the transplant claim*, then *the regime caveat*. New catch: **"discharged" is used in a specialist sense ("discharge a problem") that a metrologist may pause on.** Einselection gloss praised as good.
  - **Q2:** *Yes — your own worry is correct.* Three measures fine as one clause each, but the resolution-anchor/classification-anchor distinction "is introduced abstractly, with no concrete instance, and then immediately deferred to a later Coastline. A first reader is left wondering why it appears here at all." **New alternative fix proposed:** either add the concrete example, *or* demote the anchor distinction to a single trailing sentence explicitly marked "deferred to a later Coastline," so it stops competing with the three live measures. This is a fifth distinct path through Claim IV.
  - **Q3:** *Yes, mildly — and it is a clarity flag.* "Title is in tension with the document's own discipline. 'Overview' or 'Vantage Point' is safer and matches the §1 heading." **New specific variant proposed:** *"Harbour View — Temporal Redundancy and Emergent Proper Time: An Overview"* — drop "Report on" prefix, append ": An Overview" as a status marker, reads as proposal-status rather than results-report. Five-way Q3 convergence on the *direction*; F5 offers the most specific composite form.
  - **Q4:** From source — both figures already encode redundantly (stroke-width + label for Figure 1; nesting + labels for Figure 2 top); the only real risk is Figure 2 bottom, where the red/green fills collapse to near-identical greys. The ✓ R presence-vs-absence asymmetry carries the load, but **absence is harder to perceive than contrast**. **New fix recommendation:** add an explicit "✗ R" or "no R per fragment" mark on the anti-redundant circles so the contrast is symmetric and colour-independent. Sharper than F1/F2/F3/F4's generic "add shape/label" recommendation because F5 read the source and knows what's already encoded.
- **F5 additional catches (within-section wording):**
  - **Editorial tangle:** *"one specific claim … decomposed into five published claims"* tangles *claim/claim*; *"one specific thesis, decomposed into five published claims"* reads cleaner.
  - **COI reminder (not a defect):** the View leans on CL-2026-006 v0.5 (Sorci et al.) as its numerical anchor; Sorci et al. is a named COI orbit in the brief; the deferred §5 Acknowledgements & COI block must declare the steward's prior Leibfried association explicitly (per the project's Q11c separation discipline and the 2026-05-26 Sanner-not-Leibfried correction). Already captured in [`docs/view-framework-overview-v0.1-bibliography-draft.md`](view-framework-overview-v0.1-bibliography-draft.md) Block 5 specification; reiterated for visibility at §5 drafting time.
  - **Veto determination:** **NO VETO.** Nothing in §1+§2 is ambiguous or ethically unsafe to the threshold of *Violation of Clarity or Ethics*; the anti-claim scaffolding does its protective job; the deferrals are honestly flagged. The title and the Claim IV paragraph are clarity *frictions*, not violations. The Memory lens lesson: *"robustness comes from the kernel rotation, not the repetition"* — restating "emergent proper time" confidently in the title does not make it less conditional in the body; it only widens the calibration gap. **One escalation:** fix the brief's Figure 1 colour error before the call circulates further. **Done in this turn.**
- **Five-response cumulative pattern:**
  - F1: human, inside-orbit (Freiburg) — not §5-qualifying.
  - F2: claimed outside-orbit, missing affiliation, structured-template prose — uncertain status; recommended affiliation follow-up.
  - F3: explicitly AI (Kimi) — not §5-qualifying.
  - F4: claimed outside-orbit, template-artifact in attestation + inability to fetch files — drafter assessment: not §5-qualifying.
  - F5: Harbour-fluent, self-disqualifying, source-level inspection real — not §5-qualifying by self-acknowledgement.
  - **Only F1 cleanly reads as human; only F5 demonstrably read the figure source; none satisfies the §5 first-instance external-physicist-outside-orbit requirement.**
- **Five-way convergence on the FOLD-NOW items is now extremely robust.** Title rewording (Q3), Claim IV paragraph re-framing (Q2), schematic accessibility encoding (Q4), and §1 anchoring/density fixes (Q1) are confirmed by every reader who could see the text. Further LLM responses will not overturn this; further human responses are vanishingly likely to overturn it either. **The remaining unknown is precisely the §5-qualifying human external read, which the call has not yet attracted and likely will not without direct outreach.**
- **Drafter recommendation (escalated):** the open call has exhausted its informational yield. Five rounds, four-way+ convergence, no §5-qualifying response. Recommendation:
  1. **FOLD the five-way convergent items now** at the next steward direction. The brief has already been corrected for the Figure 1 colour error (this turn, pre-push, uncontroversial).
  2. **Switch the §5 first-instance route from open call to direct invitation.** The original first-choice profile (SYRTE/LTE-style clock-comparison physicist) was the cleaner path before the open-call decision; reverting to direct invitation now is honest with the data the open call has yielded. The open call landing-page section can be either retired (marker flipped to *closed* with a note that direct invitation has taken over) or kept active as a parallel channel.
  3. **Push the corrected brief + the folded View** together once direction A is given. Pushing before the fold lands the in-flight (still uncorrected) draft on the live site; pushing after the fold lands the improved draft as the call's first impression.
- **Lock-Key held throughout.** No Coastline / Ledger / MN / Sail / notebook / committed-JSON edits this turn. The View prose is still at `0d1765b`. The brief is updated (Figure 1 colour correction) in this commit. Open call still local-only (not pushed).

## 2026-05-28 — Sixth open-call feedback: Harbour-fluent Guardian-style pre-flight review; six-way convergence stable; recommendation unchanged

- **Actor:** Sixth-responder reviewer (self-attested *"this is not a qualifying external-independent pilot read, because I am already familiar with your Harbour vocabulary and prior framework discussions. It is useful as an internal Guardian-style pre-flight review, not as the §5 independence requirement described in the brief."*) / Agent (log + final consolidated state). **Commit:** *(this change).*
- **COI handling per open-call brief.** F6 self-disqualifies cleanly: Harbour-fluent, explicit framing as "internal Guardian-style pre-flight review", request that the feedback not be counted against §5. Honest disclosure; treated as **within-section wording + structural-discipline feedback**, not §5-qualifying.
- **Brief-correction reach confirmed.** F6's Q4 paragraph states "Figure 2 uses red/green contrast for anti-redundant versus redundant panels, and Figure 1 uses a blue highlight for the Page–Wootters slice" — this matches the corrected brief Q4 exactly (i.e., F6 was reading the post-`8ab1183` brief). The Figure 1 colour error correction has reached at least one downstream reader; no re-flag from F6.
- **Substantive content of F6 (Q-by-Q condensed):**
  - **Q1:** Mostly yes, with friction; scientific opening ("Is classical proper time emergent…?") works well; *"sharpened, not discharged"* and the einselection caveat both anchor correctly. Friction location: **the first-page Harbour-term density** (governance vocabulary before scientific orientation). **New specific fix proposed: add an explicit *language-boundary note* near the start of §1**, e.g. *"This document avoids project-internal shorthand where possible. Terms such as Coastline, Ledger, and Sail are used only where they identify a specific project artefact. The scientific claim itself is stated in ordinary descriptive language first."* This is a Q12 meta-discipline addition — structural rather than wording. Notably it would address F3's glossary-skip-past concern, F4's "redundancy mechanism" gloss request, and F6's own first-page-density observation all at once.
  - **Q2:** *Yes, not fatally; needs one breathing sentence.* New fifth distinct concrete example proposed (notably **clock-language but platform-agnostic**, unlike F1/F2/F3/F4's platform-specific examples): *"In clock language, a resolution anchor asks how well the carrier set can distinguish nearby values of τ at all; a classification anchor asks whether the temporal record is distributed redundantly, so that several disjoint carrier fragments each support the same effective τ rather than only the full many-body state doing so."* Avoids picking a specific platform (Allan-deviation floor, oscillator linewidth, QPN floor, 10⁻¹⁸ pattern) and avoids foreshadowing the deferred measure-registration protocol — a clean middle path.
  - **Q3:** Slightly overclaims; six-way convergence on direction; F6's specific pick: *"Harbour View — A Vantage Point on Temporal Redundancy and Emergent Proper Time"* as best matching the declared artefact type; *"An Overview of …"* as alternative.
  - **Q4:** Could not directly inspect SVG (tool limitation; "SVG fetch failed even though the page itself loaded"). Same diagnostic posture: don't wait — make figures robust by design with shape + label + line-style distinctions as primary channels; for Figure 2 mark anti-redundant fragments with crossed or dashed fragment boundaries; for Figure 1 make the PW slice visually distinct by enclosure or bracket. Adds "this also helps printed or photocopied versions" — a generalisation beyond colourblindness.
- **F6 §5 (new section in the feedback): sentence-level and language-boundary edits (very specific, all Q12-driven):**
  - *"A specific structural transplant…"* → *"The framework transfers one structure from quantum Darwinism to proper time: the idea that classical behaviour appears when many fragments carry redundant records."*
  - *"has been sharpened, not discharged"* → *"has been made more precise, but not solved."*
  - *"The View is a window onto them, not a door."* → *"The View summarises those records; it does not replace them."*
  - *"Nuisance-discrimination… is load-bearing"* → *"A decisive test must distinguish genuine loss of temporal consensus from ordinary measurement noise."*
  - F6 reframes these as Q12 internal-idiom-to-descriptive-language replacements. They directly operationalise the Q12 load-bearing rule.
- **F6 §6: strong points to preserve.** Anti-claim discipline; "multipartite is not redundant" subsection. Same as F3's praise.
- **F6 §7: ordered pre-pilot fix list (a clean Guardian-style ordering):**
  1. Rename "Report" → "Overview" or "Vantage Point".
  2. Add concrete Claim IV clock-context sentence.
  3. Add language-boundary note.
  4. Revise 3–5 internal idioms (specific list above).
  5. Make figure distinctions non-colour-dependent.
- **Six-response cumulative pattern.**
  - F1 (clean human, inside-orbit disclosed) → not §5-qualifying.
  - F2 (claimed outside-orbit, missing affiliation, structured-template prose) → drafter assessment: doubtful.
  - F3 (explicit AI) → not §5-qualifying.
  - F4 (claimed outside-orbit, template-artifact in attestation, cannot fetch files) → drafter assessment: not §5-qualifying.
  - F5 (Harbour-fluent, source-level inspection; caught and fixed brief defect) → not §5-qualifying.
  - F6 (Harbour-fluent, Guardian-style pre-flight, brief-correction-aware) → not §5-qualifying.
  - **F1 and F6 read as cleanest humans; F5 demonstrably read source; F2 and F4 carry LLM markers; F3 disclosed AI. NONE satisfies §5 first-instance.**
- **Six-way convergence stable.** Title rewording (Q3), Claim IV breathing space (Q2), schematic accessibility encoding (Q4), §1 friction (Q1 — first-page Harbour-term density). Five distinct Claim IV concrete examples now on the table (Allan-deviation; oscillator linewidth; QPN floor; 10⁻¹⁸ pattern; clock-language platform-agnostic — F6 latest). Multiple title-form candidates ("Overview" / "Vantage Point" / drop subtitle / composite). The signal is stable and saturated.
- **Drafter recommendation: unchanged from after F4/F5.** Fold the six-way convergent items at the next steward direction; switch §5 first-instance route from open call to direct invitation; push after fold. Two **new items** from F6 to fold alongside the prior set: (i) **language-boundary note** at start of §1 (Q12 meta-discipline addition; surgical insertion, one sentence; the cleanest single fix for the first-page Harbour-term-density problem F3/F4/F6 all flagged); (ii) **four specific idiom-to-descriptive-language replacements** ("structural transplant", "sharpened, not discharged", "window/door", "load-bearing" → descriptive equivalents). And **F6's Claim IV concrete example is the cleanest pick** because it's platform-agnostic, doesn't foreshadow the deferred measure-registration protocol, and reads as a definition rather than an illustration.
- **Lock-Key held throughout.** No Coastline / Ledger / MN / Sail / notebook / committed-JSON edits this turn. View prose still at `0d1765b`. Open call still local-only (not pushed).

## 2026-05-28 — Consolidated fold from six rounds of feedback; open call closed; pushed live

- **Actor:** Steward (direction: "1. Fold, 2. Tried – no yield, 3. Retracted open call and push. GO with your preferences") / Agent (consolidated revision pass + retraction + push). **Commit:** *(this change).*
- **Folded into the View** ([`views/view-framework-overview-v0.1.md`](../views/view-framework-overview-v0.1.md)) — single consolidated revision pass derived from the six-way convergent triage:
  - **Title:** dropped the "Report on" subtitle → *Harbour View — Temporal Redundancy and Emergent Proper Time*. Cleanest of the convergent options (F1/F2/F3/F4/F5/F6 all on the same direction; "drop subtitle" appeared in every reader's option set; matches the spec's "presentational vantage-point document" register).
  - **Document structure:** the tiny glossary was extracted from mid-metadata into its own clearly-titled section *"A note on language, and a short glossary"* placed below the metadata block and above §1. The section opens with F6's language-boundary note verbatim (Q12 meta-discipline operationalised), followed by the five glossary entries (Harbour View, Coastline, Breakwater Ledger, Methodological Note, Sail).
  - **§1 headline split into two paragraphs** per F5/F6: *"The headline — the transplant"* introduces quantum Darwinism + Page–Wootters as a single coherent picture (F2's PW anchor concern addressed in §1 main text before Figure 1, ahead of the figure caption); *"The headline — the regime caveat"* gives the Anti-Claim #6 framing without the numbered forward reference (F3's softening adopted: *"the framework's principal open problem (discussed in §2)"*).
  - **§1 idiom revisions per F6** (Q12 load-bearing): *"specific structural transplant"* → *"borrows one structure"* + descriptive unpacking; *"sharpened, not discharged"* → *"made more precise, but not solved"*; *"window onto them, not a door"* → *"summarises those records, it does not replace them"*. Einselection gloss expanded to *"already environment-selected, in the Zurek sense"* per F3.
  - **§2 wording revisions:** *"one specific claim … decomposed into five published claims"* → *"one specific thesis … decomposed into five published claims"* (F5 editorial); Claim III *"nuisance-discrimination … is load-bearing"* → F6's descriptive replacement *"Falsifiability here depends on a decisive test: distinguishing a genuine loss of temporal consensus from ordinary measurement noise"*.
  - **§2 Claim IV split into two paragraphs:** paragraph (A) introduces the three measures (`I(C:M)`, `F[τ]`, `R_δ`) with their definitions; paragraph (B) introduces the *resolution-anchor vs classification-anchor* distinction with F6's platform-agnostic clock-language explanation as the working definition — chosen over the four platform-specific examples on the table (F1 Allan-deviation floor; F2 oscillator linewidth; F3 quantum-projection-noise floor; F4 10⁻¹⁸-pattern-agreement) because it reads as a *definition* rather than an *illustration* and does not foreshadow the Coastline-deferred measure-registration protocol. The protocol's deferral to a later Coastline version is preserved verbatim.
  - **Figure 1 caption rewritten:** explicit nested-slice visual hierarchy ("thicker arrow, a dashed enclosure around the slice, and an explicit label") per F3/F5/F6; PW labelling kept in caption only after §1 main text anchors it (F2 fix).
  - **Figure 2 caption rewritten:** explicit dashed-borders + ✗ R markers for anti-redundant fragments + solid-borders + ✓ R markers for redundant fragments (F5 symmetric-contrast recommendation); explicit statement that the contrast is encoded by colour + shape/line-style/marker.
  - **Reference apparatus placeholder:** bibliography link labelled *"commented-bibliography draft worksheet — a draft, not the final apparatus"* (F4 catch). Q11c COI handling noted explicitly (Leibfried acknowledged, Sanner not).
  - **Status line updated:** declares §1+§2 only; references the now-closed open-call brief as audit record; states the §5 first-instance pilot-reader requirement remains outstanding; route under separate stewardship consideration.
- **Folded into the SVGs:**
  - [`views/figures/schematic-01-framework-orientation.svg`](../views/figures/schematic-01-framework-orientation.svg) — added a **dashed blue ellipse** (stroke-dasharray="5,4") around the PW slice's carrier F₁ at radius 44. The PW slice now carries three non-colour cues (thick stroke on F₁, the dashed enclosure, the explicit label) in addition to the blue colour. Survives greyscale + colourblindness per F5's source-level inspection criteria + F6's "enclosure or bracket" recommendation. XML re-validated.
  - [`views/figures/schematic-02-comparison-geometry.svg`](../views/figures/schematic-02-comparison-geometry.svg) — anti-redundant circles now have **dashed borders** (stroke-dasharray="4,3"), enlarged to r=17 to fit an explicit **"✗ R" marker** inside each (red, bold, font-size 13). Redundant circles unchanged (solid borders + "✓ R"). The bottom-panel anti-redundant-vs-redundant contrast is now triple-encoded: colour (red vs green), line-style (dashed vs solid borders), and marker (✗ R vs ✓ R). F5's symmetric-contrast recommendation satisfied; F1/F2/F3/F4/F6's "use shape + label + line-style" recommendations satisfied. XML re-validated.
- **Open call closed and retracted:**
  - [`index.md`](../index.md) — landing-page section updated: header *"open call closed 2026-05-28"*; brief explanation that responses received did not yield a confirmed-human qualifying reader and that direct invitation was also attempted without yield; the substantive feedback has been folded; the §5 requirement remains outstanding and routed to separate stewardship consideration. Two links retained — to the draft and to the closed-call brief — for transparency.
  - [`docs/view-framework-overview-v0.1-pilot-reader-brief.md`](view-framework-overview-v0.1-pilot-reader-brief.md) — closing notice added at the top: *"CALL CLOSED 2026-05-28 (archived for transparency)"* + an explicit "no further feedback is being solicited via this channel". The body of the brief is retained as the audit record of the call as posted (a substantive change to the brief body would defeat the audit purpose). The closing metadata adds a *Closed: 2026-05-28 (same day; no qualifying yield)* line.
- **§5 first-instance status — UNCHANGED + escalated for separate stewardship.** Six rounds + direct outreach: no confirmed-human foundations-metrology physicist outside the Freiburg–NIST orbit. The artefact-category specification's first-instance pilot-reader requirement remains an outstanding precondition for tagged release; the View remains a pre-pilot-review draft. The route to satisfying §5 is now a stewardship problem outside the agent's reach (further direct outreach; conference-time contact; collaborator referral; or — as a last resort — invocation of the category-level rule's "documented omission with explicit justification" carve-out, which the first-instance rule does **not** currently allow).
- **Push to `origin/main`:** all local commits (the open-call setup, six feedback log entries, the brief Figure 1 correction, this consolidated fold + retraction commit) pushed in this turn. Pages rebuild within ~1 minute; the closed-call landing page goes live as the View's first impression.
- **Lock-Key held throughout.** No Coastline / Ledger / MN / Sail / notebook / committed-JSON / results-JSON edits in the fold. View prose advanced from `0d1765b` to this commit. Schematics advanced (presentational; both XML-valid; SVG diffs additive — colour-encoding unchanged, only shape/line-style/marker added).

## 2026-05-28 — Guardian determination: §5 first-instance pilot-reader requirement reclassified; pin verified; Q12 self-audit logged; front-glossary ratified; COI elevation flagged for §5/apparatus stage

This entry consolidates five Guardian-directed items issued together: a structural spec reclassification, a toolkit-pin verification, a Q12 self-audit substituting for the missing pilot read, ratification of the front-glossary deviation, and a held flag on COI disclosure visibility at release.

### Item 1 — Spec amendment recorded (Q5 §5 first-instance reclassification)

- **Guardian framing (2026-05-28, verbatim summary):** the §5 first-instance pilot-reader requirement — *"the first View's executive summary must be reviewed by at least one external physicist working at the foundations-metrology interface, outside the steward's immediate Freiburg–NIST orbit, with feedback incorporated before the tagged release. No omission permitted for the first View."* — was mis-specified. It assumes an audience exists that does not yet exist; the View is the recruitment mechanism for the audience the gate would consult. The gate has a chicken-and-egg dependency on the very readership the View is built to create. Six open-call rounds + direct invitation on 2026-05-28 confirmed the spec defect by contact with the task: no qualifying reader was reachable. This is an amend-from-task-contact case (the discipline the project committed to at the Harbour View Structural Deliberation Note v0.6 Q10c carve-out precedent), not pressure-driven gate erosion.
- **Reclassification (logged here as the spec amendment; not folded into the deliberation note itself today — deferred to that note's next genuine revision per the established discipline):**
  > §5 first-instance pilot-reader requirement: **was** *"mandatory pre-release, no omission permitted"*. **Now** *"best-effort pre-release; if unavailable, documented Q12 self-audit + disclosed limitation + standing post-release invitation, foldable via supersession (Q9) at the next version."*
- **Guardian condition (held, not negotiable, recorded here as load-bearing):** the gap is **disclosed, not omitted**. The View's Status line carries the disclosure in plain language; the omission becomes a documented limitation rather than a silent failure. **A7 honesty discipline does the protective job that the gate was meant to do**; without the disclosure, the Guardian would veto release. With the disclosure present, no veto.
- **Why this is clean rather than risky.** The fixed-snapshot + supersession machinery (Q8 / Q9) is already in place to absorb late reader feedback. A pilot read does not have to be a pre-release gate when a v0.2 can fold reader feedback cleanly the moment it arrives. The only risk genuinely lost is catching a readability failure *before* first publication — and that risk is covered by the Q12 self-audit recorded below as Item 3.
- **Knock-on effect (Scout EC-5).** The pilot read was positioned *between* §1+§2 and §3–§5 specifically so feedback could trigger cheap TOC revision before the deeper sections were written. With no pilot read, that timing rationale collapses — there is no longer reason to hold §3–§5 drafting. **§§3–5 drafting is unblocked and proceeds straight through, against the pinned commit `938b0a6`.**

### Item 2 — Toolkit pin verified: `a6518a7 → 938b0a6` is safe for §3 / §4 to cite

- **Question:** does `938b0a6` (the figure-citing commit) only touch `views/figures/`, or does it also touch `numerics/`?
- **Answer (`git diff --stat a6518a7..938b0a6`):** the commit touches `numerics/examples/open_instrument_module.ipynb` (the four appended cells for the Harbour-View type-(b) + type-(c) figures, plus re-execution outputs), the two new figure SVGs under `views/figures/`, and a set of documentation files (spec note, task card, logbook, MN v0.3 §8, CL-2026-006 v0.5, README, roadmap, tutorial, index, View-related drafts). It does **not** touch `numerics/results/`.
- **Substantive integrity check (the load-bearing one):** `git diff --stat a6518a7..938b0a6 -- 'numerics/results/'` returns **empty**. The committed results JSONs `numerics/results/sorci_nuisance_budget_v0.1.json` and `numerics/results/open_instrument_redundancy_v0.1.json` are **byte-identical** between `a6518a7` and `938b0a6`. This was the load-bearing Q10c immutability condition for the figure-citing commit and is reconfirmed now.
- **The notebook diff is additive only:** 265 insertions + 24 deletions; the 24 deletions are `execution_count` and `output` field churn from notebook re-execution (the four new presentational cells do not alter the existing data-generating logic). The 85-test suite was green at `938b0a6` at commit time (logbook 2026-05-28); this remains valid.
- **Conclusion.** Pin clean. §3/§4 can cite `938b0a6` as the figure-citing commit without further verification. Toolkit Provenance, Figure Provenance, §4 Colab link, and §5.2 reproduction pointer — all already advanced to `938b0a6` in the outline (this turn earlier; commit `e335466`) — hold.

### Item 3 — Q12 self-audit on §1 + §2 (pilot-read substitute, logged per Guardian condition)

Run against the spec v0.6 §4 Q12 rules. Result by rule:

- **Q12 rule 1 (avoid unexplained abbreviations).** ✅ Clean. *Quantum mechanics*, *special relativity*, *general relativity*, *Page–Wootters*, *quantum Darwinism* written out at first use; project terms (*Harbour View*, *Coastline*, *Breakwater Ledger*, *Methodological Note* + *MN*, *Sail*) defined in the glossary section immediately above §1; the only acronyms in the body (*N* for number of carriers, `I(C:M)`, `F[τ]`, `R_δ`, `δ`) are mathematical objects defined inline at their introduction.
- **Q12 rule 2 (prefer descriptive phrases over internal labels).** ✅ Operationalised through the six-round-folded idiom revisions: "structural transplant" → "borrows one structure"; "sharpened, not discharged" → "made more precise, but not solved"; "window onto them, not a door" → "summarises those records, it does not replace them"; "load-bearing nuisance-discrimination" → "Falsifiability here depends on a decisive test: distinguishing a genuine loss of temporal consensus from ordinary measurement noise". Remaining Harbour-fluent constructions in the body are either explicitly required artefact pointers ("Coastline v0.4 §IV", "Ledger CL-2026-006 v0.5") or anchored on first use.
- **Q12 rule 3 (short, declarative sentences where possible).** ✅ Mostly. The §1 headline split into transplant + regime-caveat paragraphs (per F5/F6) is the principal improvement. One long sentence retained in §2 Regime-of-Validity: *"Spacetime curvature enters perturbatively, as the leading-order contribution to a cross-probe residual that no single proper-time variable reproduces — i.e., as a Claim II.3 incompressible residual and a Claim IV cross-probe-mismatch quantity (Coastline v0.4, External Constraints item 5; Regime of Validity; Anti-Claims §7)."* Length retained because the three-way Q11b anchor is load-bearing; rule 6 subordinates rule 3 here.
- **Q12 rule 4 (define specialist terms at first use).** ✅ Mostly clean. *einselected* defined inline ("already environment-selected, in the Zurek sense") per F3. *Anti-redundant* defined contextually in §1 figure caption + §2 *Multipartite is not redundant* subsection. *I(C:M)*, *F[τ]*, *R_δ* defined at introduction in Claim IV with explicit roles. **Two soft flags noted for future tightening (not blockers):** *macrofragment* (used once in Claim IV in `I(C:M)` definition) and *cross-probe residual* (used in Regime-of-Validity); both are standard within the quantum-Darwinism / clock-network lineage the body has just cited, so a foundations-metrology reader carries the concept; flag retained for the next View version.
- **Q12 rule 5 (avoid performative jargon).** ✅ Cleaner than v0.1's pre-fold state. The F6 idiom revisions (rule 2) removed the principal offenders. No remaining "decision", "agreement", "consensus-as-personification" usage in the body (rhetorical *consensus* is anchored to its Claim IV operational measures at Anti-Claim §2).
- **Q12 rule 6 (preserve technical accuracy; subordinates rule 3 where conflict).** ✅ A7 audit-positive: every claim cites the Coastline section where it is established; *sharpened/not solved*, *candidate*, *within an assumed regime*, *underdetermined*, *respect, not parity* — all carry the conditionality the underlying notes assert. F1, F3, F5, F6 each independently confirmed: no sentence reads more confidently than the underlying framework note supports.
- **Q12 rule 7 (audience test).** **Pilot-read substitute logged.** The pilot read this rule operationalises has not been performed; per the Item 1 reclassification, this is documented (not silent). Substantive surrogate: six rounds of feedback (F1–F6, mixed inside-orbit human / outside-orbit-claimed / AI-self-disclosed / Harbour-fluent self-disqualifying) converged consistently on the same load-bearing readability fixes, all folded; the only one of the six who explicitly performed a *no-Harbour-background* read (F3) reported §1 as "mostly yes, followable" after the fixes were proposed. The remaining audience-test gap is *whether a confirmed-human external foundations-metrology physicist would experience the post-fold §1 as followable* — an empirical question the spec gate would have answered; the Guardian determination accepts the residual uncertainty as a disclosed limitation.

**Q12 self-audit verdict: PASS for §1 + §2 release** under the Item 1 reclassification, with the two soft flags (*macrofragment*, *cross-probe residual*) recorded for the next View version. Audit performed by Agent (drafter) against the spec v0.6 §4 Q12 rules; intended as Guardian-reviewable evidence, not Guardian sign-off.

### Item 4 — Front-glossary deviation ratified (Guardian approval recorded)

The spec v0.6 §6 first-instance figure plan does not specify glossary placement; the spec v0.6 §11 recommendation 1 *(declare artefact-type explicitly on the first page)* implies but does not mandate that artefact-type vocabulary be defined immediately. The first-View v0.1 commit (`2503c07`) implements a **front-glossary deviation**: the five-entry glossary (*Harbour View*, *Coastline*, *Breakwater Ledger*, *Methodological Note*, *Sail*) is placed in its own clearly-labelled section *"A note on language, and a short glossary"* between the document-level metadata block and §1 (rather than buried in the reference apparatus or in the metadata block, where the F3 audience-test reader observed it would be skipped). The section opens with F6's Q12 language-boundary note. **The Guardian ratifies this deviation as an improvement, on record here so it is not treated as quiet drift; future Views may either inherit the front-glossary pattern or document a different choice at the TOC-lock step.**

### Item 5 — COI elevation flagged (held for §5 / apparatus drafting stage)

The current §1 + §2 commit references Sorci et al. (2026) by Ledger anchor (CL-2026-006 v0.5) in §1 and §2; the Acknowledgements & Conflict-of-Interest block where the steward's NIST Boulder prior association with D. Leibfried (a Sorci-paper co-author) is declared lives in the reference apparatus, which is the *next* drafting pass. **Guardian held requirement (recorded here, action at §5/apparatus stage):** at release time, the Leibfried declaration must be *visible* in the released View — not held in a deferred apparatus and not surfaced only by reference. Concretely, the Acknowledgements & COI block lands in the same commit as §§3–5 + the bibliography, and is placed where a reader who reaches the Sorci-citation paragraphs can see the declaration without traversing a separate document. The 2026-05-26 logbook correction (Leibfried acknowledged, Sanner explicitly not) provides the canonical wording.

### Operational state after this entry

- View `views/view-framework-overview-v0.1.md` Status line now carries the disclosure (Guardian condition met).
- Landing-page `index.md` re-aligned: pilot-reader gate reclassified header; disclosure framing matches the View.
- Brief `docs/view-framework-overview-v0.1-pilot-reader-brief.md` retains its closing notice from the prior turn; no edit this turn.
- Spec deliberation note `docs/harbour-view-structural-deliberation-note-v0.6.md` **untouched** (per the deferred-amendment discipline); the reclassification folds into that note's next genuine revision.
- §§3–5 drafting **unblocked** against `938b0a6`.
- COI elevation **held** for the §5/apparatus drafting commit.
- Two Q12 soft flags (*macrofragment*, *cross-probe residual*) **registered** for the next View version, not the current drafting pass.

**Lock-Key held.** Coastline / Ledger / MN / Sail / notebook / committed-JSON / results-JSON untouched.

## 2026-05-28 — First Harbour View §§3–5 + reference apparatus drafted straight through; full draft complete

- **Actor:** Steward (Guardian determination + GO direction "draft straight through to §5") / Agent (drafter; consolidated revision pass). **Commit:** *(this change).*
- **Single drafting pass** against pinned commit `938b0a6`, per the Scout-EC-5-rationale-collapsed determination. §1 + §2 unchanged from commit `21883fd`; §§3–5 + reference apparatus drafted in this turn; stale TOC-outline + View-placeholder language updated. The full View is now drafted end-to-end.
- **§3 Methodology** (~480 words + Figure 3). Opens by anchoring to MN v0.3 and naming the *Methodological Note* artefact category. Defines the temporal-redundancy functional `R_δ(N, ε)` and (in the Claim IV vocabulary of §2) classifies it as a **classification anchor**, with `F[τ]` as **resolution anchor** (extensive, anti-correlated with `R_δ`) — the Claim IV self-consistency split is named explicitly. Derives the per-carrier distinguishability `ε(γ_φ, t) = (1 − e^{−γ_φ t}) / 2` from the single-carrier pure-dephasing master equation; cites the four-way coincidence (trace-distance / Helstrom / projective / Holevo) per MN v0.3 §8.1. Describes the MN v0.3 §2 analytic poles (product pole = redundant; cat pole = anti-redundant) and the MN v0.3 §8.3 D5 cross-link (`ε = 0.20, N = 64 → R_{0.10} = 64/9` exactly). The §2 *multipartite ≠ redundant* distinction is restated dynamically (MN v0.3 §8.4). The Tank 2025 (arXiv:2509.17775) structural match (MN v0.3 §8.5) is surfaced — *structural* match, not numerical (per A7). Figure 3 (type-(b), sweet/anti-sweet shaded) is embedded inline with the caption naming the per-N undefined-boundary values (`0.273`, `1.136`, `1.827` at `N = 4, 32, 128`). **The MN v0.3 §8.6 einselection caveat is preserved as a single verbatim block** in the closing subsection *"What §3 does not settle — the einselection caveat (verbatim from MN v0.3 §8.6)"*.
- **§4 Worked exemplar** (~750 words + Figure 4 + live Colab link). Opens by anchoring to CL-2026-006 v0.5 and the figure-citing commit `938b0a6`; declares the verdict-unchanged honesty up front. **§4.1** Module 3a: reproduces the three model-conditional findings verbatim by reference from CL-2026-006 v0.5 *Quantitative grounding* — (1) per-channel signs non-uniform (heating/dephasing reduce `I(C : M)`; depolarising prep-infidelity raises and dominates); (2) detection layer latent-invariant (`ΔI = ΔV_state = 0`, observed-only); (3) decomposition model-conditional (motional dephasing visibility-invisible in secular model yet still lowers `I(C : M)`; channels non-additive; squeezing-prep-infidelity model aggressive — toolkit-v0.2 refinement registered). Bearing on D1: theoretical decomposition produced; per-channel attribution model-conditional; toolkit sharpens D1 into a demand on controlled scaling + explicit channel models; D2/D3 remain experimental and unmet; **verdict stays UNDERDETERMINED**. CBG-note correction (v0.5) carried by reference. Claim IV split surfaced again: budget measures classification anchors, not the resolution anchor `F[τ]`. **§4.2** Module 3b: rising-boundary table; ε bridge derived; D5 cross-link validated to `< 10⁻⁹`; einselection caveat **carried by single inline reference** to §3 (not re-quoted, keeps §4 within page budget); Anti-Claim #6 stays open. Figure 4 (type-(c), heatmap with anti-sweet demarcation + dotted cross-links to §3 slices) embedded inline. Live Colab link at commit `938b0a6` with documented fallback statement. **§4.3** Module-conditionality summary cites toolkit anti-claims A1–A5 + Q11c COI cross-link to Block 5 (Leibfried / not Sanner).
- **§5 Open problems + neighbouring work + invitations** (~720 words). §5.1 lists five open problems (Anti-Claim #6 principal; Claim IV measure-registration deferred; Sorci D2/D3 experimental; toolkit-v0.2 refinement registered; strong-field extension deferred) each anchored to the underlying note that records it. §5.2 surfaces the **Discriminant Conditions** of CL-2026-007 v0.3 (Smith & Ahmadi: D2 structural null test via `θ`-toggle suppressing `γ_Q⁻¹`) and CL-2026-008 v0.2 (clock-network class: framework-side D1 satisfied by Coastline v0.4 *Regime of Validity*; experimental D2 isolating the GR cross-probe signature remains the route to *compatible*). F2's recommendation surfaced. §5.3 covers citation locator (regime O6 ii), reproduction pointer at commit `938b0a6`, reader-feedback invitation per the §5-reclassified supersession route (Q9), and direct contact. §5.4 honestly names what is **not yet supported** (public issue tracker; PRs against framework artefacts via the View; external-circulation discovery campaign).
- **Reference apparatus** assembled (six blocks per spec §6). **Block 1 commented bibliography** with A1–A3 external constraints (QM Nielsen & Chuang 2010; SR + weak-field GR MTW 1973) carrying the constraint-only / no-parity marker; **A4 reclassified to *method-source*** for QIT (the §3 distinguishability machinery is a technique source, not an external coastline; Coastline's Endorsement-Marker handles the constraint-only posture); Blocks B (PW lineage: page1983, mendes2019, smith2020) and C (QD lineage: zurek2003, zurek2009, ollivier2004, ollivier2005) as prior-art; Block D Sorci 2026 with explicit COI cross-link to Block 5; Block E Tank 2025 as structural-match anchor for §3. **Block 2** version-pinning appendix (Coastline v0.4, MN v0.3, CL-2026-006 v0.5, CL-2026-007 v0.3, CL-2026-008 v0.2). **Block 3** Toolkit Provenance with commit `938b0a6`, results JSONs, notebooks, environment (QuTiP 5.2.3 / Python 3.13.7 / NumPy 2.4.4 / matplotlib 3.10.8 / pytest 9.0.3), and inherited toolkit anti-claims A1–A5 reproduced. **Block 4** per-figure Figure Provenance for all four figures (two pinning regimes per Q10c carve-out — source-file-pinned schematics with accessibility-encoding declarations; notebook-pinned data figures with parameter grids, sweet/anti-sweet labelling, live-link fallback). **Block 5** Acknowledgements & Conflict-of-Interest visibly placed: **D. Leibfried acknowledged** (NIST Boulder 2010–2012 prior steward association); **C. Sanner explicitly NOT acknowledged** (per the 2026-05-26 logbook correction; the steward has no prior relationship with Sanner). Block 5 explicitly notes Leibfried's Sorci co-authorship and the independence-of-commentary consideration — Guardian's Item-5 *visible at release* condition satisfied. **Block 6** citation-metadata stub.
- **Consistency verification (run before commit):**
  - **Claim IV split** named in §2 (introduction), §3 (line ~99 — `R_δ` classification, `F[τ]` resolution, anti-correlation), §4 (line ~157 — budget measures classification anchors, not resolution anchor), §5 (line ~196 — deferred-protocol open-problem framing). Self-consistent across §§2–5.
  - **Q11b relationship-claim anchoring:** §2 GR-regime sentence triple-anchored to Coastline v0.4 *External Constraints* item 5 + *Regime of Validity* + *Anti-Claims* §7. §4 opens by anchoring every relationship statement to CL-2026-006 v0.5. The *respect, not parity* posture from §2 Anti-Claim #4 is extended explicitly to §4.
  - **Einselection caveat** appears once verbatim in §3 (closing subsection); §4 references by name; no repetition.
  - **Ledger version inconsistency fixed:** §2's *Regime of validity* paragraph initially cited CL-2026-007 *v0.2*; corrected to **v0.3** (the authoritative version, consistent with §5 and the version-pinning appendix).
  - **Q12 language discipline preserved:** the F1+F2+F3+F4+F5+F6-folded idiom revisions in §1 are not undone in §3/§4/§5; specialist terms (*classification anchor*, *resolution anchor*, *redundancy plateau*, *anti-redundant pole*, *macrofragment*, *cross-probe residual*, *Discriminant Condition*) are introduced with brief inline definitions or cross-referenced to underlying notes; long sentences appear only where the three-way Q11b anchor or the verbatim-quote discipline subordinates Q12 rule 3 to rule 6.
- **Stale outline / placeholder language cleaned up.** TOC outline lines 9, 23, 25, 376–378 updated to reflect the §5-reclassification + §§3–5 GO state. View placeholder lines 91–end replaced with the §§3–5 + apparatus prose.
- **Lock-Key held throughout.** Coastline v0.4 / Ledger CL-2026-006 v0.5 + CL-2026-007 v0.3 + CL-2026-008 v0.2 / MN v0.3 / Sail / notebook / committed JSONs / results-JSON files all untouched; the View summarises and presents, it does not edit. The full draft is ready for **two-pass steward + Guardian review** at the next turn.

## 2026-05-28 — Two-pass review on the full Harbour View v0.1 draft: steward first-pass PASS; Guardian second-pass yielded seven precision findings (no veto); narrow correction commit landed

### Steward first-pass — PASS

Hard-rule audit clean across Q1, Q10b, Q10c, Q10d, Q10e, Q10f, Q11a, Q11b, Q11c, Q12, A7, A8. Claim IV resolution/classification split named and used consistently in §2 (definitions), §3 (`R_δ` classification, `F[τ]` resolution, anti-correlation, CL-2026-007 v0.1 failure mode named), §4 (budget measures classification anchors, not the resolution anchor), §5 (deferred-protocol open-problem framing). The Guardian's pre-drafting Claim IV consistency flag is satisfied. Three minor observations recorded by the steward for release-prep — stale §1+§2 status lines, "confirmed-human" phrasing softening, Block 6 citation-metadata stub — all addressed in this commit or held for tagged release.

### Guardian second-pass — seven precision findings (no veto), all folded

1. **§4 Claim IV overstatement.** "Budget measures classification anchors (`I(C : M)` and, through cross-anchor invariants, `R_δ`)" was too strong. Module 3a budgets `I(C : M)` and the observed visibility; at `N = 2`, `R_δ` functions only as a directional cross-anchor consistency check (values restricted to `{undefined, 1, 2}`; one structural transition, not a continuous curve; MN v0.3 §3). **Folded:** the §4.1 closing paragraph now says the budget measures `I(C : M)` and the observed visibility; `R_δ` is explicitly *not* an additional budget object and its directional-only role at `N = 2` is named; `F[τ]` is also explicitly *not* the budget's object.
2. **§3 "co-moves" too broad.** The pinned sources support "co-classifies at registered poles / cross-anchor invariants", not generic co-movement under arbitrary nuisance — Module 3a's per-channel signs are non-uniform (and §4.1 says so), so the broader wording invited an overclaim. **Folded:** "co-moves with" → "co-classifies with `I(C : M)` at the registered analytic poles and in the toolkit's cross-anchor invariant tests (MN v0.3 §3; toolkit work plan v0.1 §5)", with a new clarifying sentence explicitly stating that co-movement under arbitrary channels is *not* asserted and cross-referencing the §4.1 non-uniform-signs finding.
3. **`ε` mis-labelled as "distinguishability".** MN v0.3 reserves "distinguishability" for `c` (or the trace distance); `ε = (1 − e^{−γ_φ t}) / 2` is the Helstrom/projective flip/error parameter. **Folded** at three locations: `ε` is now called the *per-carrier flip/error probability* throughout, *derived from* the carrier's distinguishability against a reference reading. The relation `c = e^{−γ_φ t}` (distinguishability) and `ε = (1 − c) / 2` (flip probability) is stated explicitly. The anti-sweet wording is updated to "where dephasing has driven `ε` close enough to `1/2`…".
4. **Holevo wording.** "`ε` identical across … the Holevo information" mis-described the Holevo quantity, which is `χ = 1 − h₂(ε)`, a function of `ε`, not `ε` itself. **Folded:** the four-way coincidence wording now reads "the trace distance, the Helstrom optimal-decision error, the optimal projective bit-decision error, and the Holevo information all yield the same effective flip/error parameter `ε(γ_φ, t)`: the first three equal `ε` directly, and the Holevo information is the strictly-monotonic `χ = 1 − h₂(ε)`."
5. **Dynamical-redundancy conflation.** The prior paragraph blended (a) the static GHZ-shadow anti-redundant pole with (b) the dynamical product-carrier dephasing collapse, and used "already zero" — but MN v0.3 reports `R_δ = 1` (not zero) at the anti-redundant pole; "no proper fragment carries `T`" is the *condition*, while the count is `R_δ = 1`. **Folded:** split into two clearly labelled faces — (i) the static anti-redundant pole where `R_δ = 1` already by the GHZ Schmidt structure (MN v0.3 §2 + Appendix B), with no open-system process invoked; (ii) the dynamical product-carrier collapse where the plateau retreats as `ε → 1/2` and `R_δ` eventually becomes undefined (MN v0.3 §8.4; the boundary curve of Figures 3 and 4). The two-face structure carries the *multipartite ≠ redundant* certification cleanly.
6. **Stale §1+§2 status lines.** Four locations in the View (Date, Status, the in-glossary "§1 + §2 pilot-reader draft" intro, and the closing footer) plus two locations in `index.md` (the section header + the link text) still described a §1+§2-only draft. **Folded:** all updated to "full draft complete; awaiting tagged release"; the View's Lineage line now also lists CL-2026-007 v0.3 and CL-2026-008 v0.2 (the cross-View pointers surfaced in §2 and §5.2 — were missing). The steward's "confirmed-human" → "(human, foundations-metrology, outside the Freiburg–NIST orbit)" softening also folded.
7. **Figure Provenance commit pins (Block 4).** Schematics were said to be "retrieve via `git log`" — release-fragile. **Folded:** Figures 1 and 2 are now explicitly pinned to commit **`2503c07`** (the commit at which the SVG files last changed — verified with `git log -1 --format='%H' -- views/figures/schematic-0X-*.svg`); Figures 3 and 4 remain pinned to commit `938b0a6` as before.

### What the corrections did NOT change

- **No new claims** introduced; every rewording stays within the underlying notes.
- **No Claim IV split rearchitecture** — §2 definitions, §3 anchor-roles statement, §4 budget-scope statement, §5 open-problem framing are all preserved; the corrections sharpened the wording, not the structure.
- **No figure regeneration** — the four SVGs are unchanged; only the *pin recorded in Block 4* changed (from "retrieve via `git log`" to the explicit `2503c07` / `938b0a6` hashes).
- **No reference-apparatus restructuring** — the six-block organisation, Q11a anchor + role + endorsement-scope-marker discipline, and the visible Acknowledgements & COI placement (Leibfried acknowledged; Sanner explicitly not) are unchanged.

### Outstanding release-prep tasks (held for the tagged-release commit)

- **Block 6 citation-metadata stub** to be populated at tagged release with the resolved Zenodo version DOI + the O6-regime-(ii) citation locator string.
- **Next-move 5 — Jekyll permalink** (per-View front-matter `permalink: /views/framework-overview/v0.1/` + a `views/framework-overview/index.md` redirect stub for Q9b canonical-current URL) applied at the release commit.
- **Next-move 6d — Colab live-link verification** at the release commit (the link to `colab.research.google.com/github/uwarring82/temporal-micro-consensus/blob/938b0a6/...` is verifiable now that `938b0a6` is on the remote since the 2026-05-28 push).

The full draft is **release-ready pending the steward's go on the tagged-release commit**. Lock-Key held throughout the correction pass.

## 2026-05-28 — Residual second-pass: three precision residuals + housekeeping folded; tag held; message-internal contradiction surfaced

The second-pass review on commit `577765b` was returned in a single message containing **both** (a) three specific factual residual findings against the commit with the explicit instruction *"I would not tag yet"* and (b) a *"PASS / Go. Tag the release commit"* sign-off. The two halves contradict directly. The agent's discipline call: **fold the residuals (uncontroversial — they are real defects in the document); hold the tag (controversial — the sign-off and the findings cannot both stand)**, and surface the contradiction here for explicit steward resolution. Tagging a release whose final commit message identifies known precision defects would fail A7 *honest representation*; the open-call brief's logged-disagreement discipline applies (nothing is silently rejected, but factual defects are not silently overruled by a sign-off in the same message either).

### Residual findings verified and folded

- **R1 — §5.1 Claim IV bullet leaked the F2 over-broad wording back in.** The §5.1 measure-registration open-problem bullet still said *"`I(C : M)` and `R_δ` as classification anchors, co-moving, plateau-collapsing under nuisance"* — exactly the F2 overclaim that was corrected in §3 at the prior commit. Worse, the wording attached "plateau-collapsing" to `I(C : M)` as well as `R_δ`, but only `R_δ` exhibits the plateau-and-collapse structure. **Folded:** the §5.1 parenthetical is now *"`I(C : M)` and `R_δ` as classification anchors that co-classify at the registered analytic poles and in the cross-anchor invariant tests, with `R_δ` additionally exhibiting plateau-collapse under per-carrier nuisance; broader claims of generic co-movement or shared plateau-collapse under arbitrary channels are not asserted."* Aligns with the §3 F2 fix.
- **R2 — §4.2 retained an `ε`-as-"distinguishability" mislabel.** The §4.2 opening sentence still said *"derives the per-carrier distinguishability `ε(γ_φ, t)` from the single-carrier master equation"* — F3 mislabel that escaped the prior pass because `ε` appears in §4.2 outside of §3. **Folded:** *"derives the per-carrier flip/error probability `ε(γ_φ, t)`"* — uniform with §3 throughout.
- **R3 — Figure 4 caption over-stated *undefined* as *no longer measurable*.** The Figure 4 caption read *"where `R_δ` is undefined and the framework's classification anchor is no longer measurable"* — operationally inaccurate. The functional `R_δ` is *still being evaluated* on the anti-sweet grid; what fails is the threshold condition `(1 − δ) H_T`, so no finite `m_δ` exists and the functional *returns* `R_δ` *undefined* as a meaningful output. **Folded:** *"where the functional is still evaluated but the redundancy threshold `(1 − δ) H_T` is never reached, so no finite `m_δ` exists and `R_δ` is returned as undefined on that grid"*. Operationally accurate without "unmeasurable" overclaim.

### Housekeeping (TOC outline status drift)

- TOC outline line 9 (Status) updated to reflect post-drafting state: §1+§2 at `2503c07`; §§3–5 + apparatus at `ca8872a`; Guardian-second-pass at `577765b`; this residual-pass at the present commit. *"Cleared: drafting straight through"* (the pre-drafting GO marker) replaced with *"Full draft complete; awaiting steward direction on the tagged-release commit."*
- TOC outline next-move 10 status (lines ~378) flipped from *"🟡 GO 2026-05-28"* to *"✅ DONE 2026-05-28"* with the three folding-commit pointers recorded. Next-move 11 (two-pass review) added as a new explicit item, marked **DONE** through the residual pass with the contradiction note. Next-move 12 (tagged release) carries the **HELD pending steward direction** flag.

### Message-internal contradiction recorded

The steward's second-pass message contained, in order: (i) the *"I would not tag yet"* statement; (ii) three residual findings with line numbers and proposed fixes; (iii) a housekeeping note; (iv) checks that passed; (v) a "Second pass (steward + Guardian): PASS" header; (vi) a correction-verification table mapping the prior-turn F1–F7 fixes to checkmarks but **not addressing the new residuals or the housekeeping note**; (vii) "Go. Tag the release commit, trigger Zenodo archive, and update the citation locator. The first Harbour View is issued." The agent's interpretation: the two halves were written from different stances within the same review session (a careful Guardian-style precision pass that surfaced new residuals, and a separate steward-style ratification of the prior-turn F1–F7 corrections) and were not reconciled before the message was sent. The agent does not assume which stance is "the steward's intent"; both are recorded; the steward decides at the next direction.

### Operational state after this entry

- All three residual precision defects are folded; the document no longer contains the specific wording errors the second-pass review identified.
- The TOC outline status no longer drifts; it now reflects the actual drafting + correction state.
- The tagged-release commit is **held** until the steward explicitly confirms which half of the contradictory second-pass message represents the operative direction. Two possibilities:
  - **(a) "Tag now":** the residuals were trivially-foldable precision items and the sign-off carries; this commit removes the only obstacles named in the residual findings, and the steward issues a one-word *"tag"* go-ahead. The tagged-release commit (Block 6 citation-metadata + Jekyll permalink + Colab verification) follows.
  - **(b) "One more pass":** the residual findings represent a substantive change in the draft and a fresh second-pass review on this commit is warranted before tagging. The agent runs a self-audit pass and surfaces any further precision items, or the steward runs the pass themselves.
- The agent recommends **(a)** with the note that the residual fixes are uncontroversial; but the recommendation is held under the steward's call. **Lock-Key held throughout** the correction pass: Coastline / Ledger / MN / Sail / notebook / committed-JSON / results-JSON files all untouched.

## 2026-05-28 — Guardian figure-check pass: Schematic 2 top panel imported CCUF structure (Anti-Claim #5 violation); top panel dropped; Schematic 1 N-symbol collision fixed; palette item deferred

A focused Guardian pass on the SVG figures (read directly from the file source on GitHub raw, since the Pages render is outside the bash allowlist) yielded a blocking cross-artefact-anchoring finding plus two precision items.

### Blocking finding — Schematic 2 top panel imports CCUF structure (Anti-Claim #5 violation + Q1 anchoring violation; resolved by dropping the top panel)

The Schematic-2 top panel drew the three-length nesting `L_source ⊂ L_apparatus ⊂ L_comparison` (geometrically: source innermost, comparison outermost — correct as drawn) and labelled it "comparison geometry / spatial hierarchy of the consensus reading". The §2 figure caption + the SVG `<desc>` field both anchored that hierarchy to Coastline v0.4. **A direct grep of Coastline v0.4 returned zero occurrences of `L_source`, `L_apparatus`, or `L_comparison`.** The closest references in the Coastline are:

- *Regime of Validity* (carried into §2 of the View): "a well-defined inter-worldline comparison protocol" — a general proviso, not a three-length nesting.
- *Anti-Claims §7*: settings absent a "well-defined inter-worldline comparison protocol" are out of scope — same general proviso, not a three-length structure.
- *Deferred Items* (CCUF-bridge bullet): names "L₀ consensus-emergence (microscopic temporal records)" and "L₁ architectural agreement (CCUF η-parameter, comparison-geometry boundary)" as a *two-layer* structural-analogy at distinct scales, and **explicitly defers the bridge** to a future coastline note pending independent maturation. Anti-Claim §5 reinforces: "A bridge to the Causal Clock Unification Framework (CCUF) or to any other Harbour framework. Such bridges may exist (see Deferred Items) but are not committed at v0.4."

The three-length `L_source ⊂ L_apparatus ⊂ L_comparison` nesting is therefore **CCUF-architectural structure** that the Coastline names only in its Deferred-Items CCUF-bridge bullet (and as a two-layer L₀/L₁ analogy, not three lengths). Importing it into the View — and anchoring it to the Coastline — is **simultaneously** a Q1 violation (the View introduces structure not on record in the underlying notes) **and** an Anti-Claim #5 violation (the View imports CCUF-style structure when no CCUF bridge is committed at Coastline v0.4) **and** an internal inconsistency (the View's own §2 anti-claim list says no CCUF bridge, while Figure 2's top panel was drawing precisely a CCUF-style scale-layer hierarchy).

**Resolution: top panel dropped.** Reanchoring is not available — the true source (CCUF) is explicitly deferred, so any reanchoring path violates Anti-Claim #5 in a different wording. The clean honest move is to remove the top panel and let Figure 2 carry only the *multipartite-vs-redundant* content, which IS directly anchored to Coastline v0.4 (Claim V + the v0.3+ *many vs redundant* terminology note). Spec §6's first-instance figure plan reads "*one schematic of the comparison geometry / framework structure*" (or, not and); the framework-structure reading is fully covered by the multipartite-vs-redundant panel.

Concrete changes folded:

- **`views/figures/schematic-02-comparison-geometry.svg`**: rewritten from `viewBox="0 0 900 540"` to `viewBox="0 0 900 290"`. Top-panel content removed (three nested rectangles, three `L_*` labels, the "Spatial hierarchy of the consensus reading" italic caption, the horizontal divider, and the top-panel title text). The remaining content — title *"Multipartite ≠ redundant"*, italic "Which fragments carry the record R?", left anti-redundant panel (Ψ_total ellipse + 5 dashed-bordered fragments with ✗ R markers), right redundant panel (4 solid-bordered fragments with ✓ R + ellipsis + F_N), vertical divider, two captions, and the footer — was repositioned vertically to fit the shorter viewBox. The SVG `<title>` and `<desc>` were rewritten to describe only the contrast content. Filename retained for repository stability; XML re-validated (46 elements).
- **View §2 figure subsection.** Heading renamed from *"A vantage point on the geometry"* to *"A vantage point on multipartite ≠ redundant"*. Figure 2 alt-text + caption rewritten to describe only the multipartite-vs-redundant contrast; all `L_source`, `L_apparatus`, `L_comparison` mentions removed. The §2 *Regime of validity* paragraph is unchanged — its references to the Coastline's "inter-worldline comparison protocol" remain anchored to the actual Coastline content.
- **Block 4 Figure 2 entry**: description updated to "*Type (a) schematic; multipartite-vs-redundant contrast*"; the filename-vs-content mismatch (the SVG is named `schematic-02-comparison-geometry.svg` but no longer contains comparison-geometry content) noted explicitly with the audit-trail explanation that the v0.1 of this View carries only the multipartite-vs-redundant contrast after a Guardian-cross-artefact-anchoring check. Filename retained for repository stability; renaming would propagate to git-blame and source-file pin records without epistemic gain.

### Precision finding — Schematic 1 N-symbol collision (Q12; resolved by relabel)

The Schematic-1 PW-slice label *"Page–Wootters slice (N = 2)"* used the symbol `N` to mean *two parties* (carrier + τ); the rest of the figure uses `N` to mean *carrier count* (F_1 … F_N). A foundations-metrology reader sees "N = 2 against the carrier labels" and reads "two carriers", then finds only one highlighted. Symbol `N` was doing two jobs.

**Resolution:** label changed to *"Page–Wootters slice (bipartite)"* in the SVG, in the Figure 1 alt-text + caption, and in the Block 4 Figure 1 entry. *bipartite* refers unambiguously to the two-party configuration *carrier + τ* (the canonical Page–Wootters reading). The Figure 1 caption now also includes a parenthetical: *"(The label avoids the numeral `N = 2` because `N` is the carrier count throughout the figure; bipartite refers to the two-party configuration carrier + τ, not to two carriers.)"* The §4.1 reference *"at `N = 2`"* in the Module 3a context is **unchanged and correct** — in the Sorci two-carrier setup `N` IS the carrier count and `N = 2` literally means two carriers; that usage and the Page–Wootters bipartite slice are distinct, and the relabel sharpens the distinction. The SVG `<desc>` field was also updated to explain the relabel choice for archival audit.

### Deferred to next View version — Schematic 2 palette valence (G3, not load-bearing)

The Schematic-2 anti-redundant/redundant contrast uses red (#a52a2a) and green (#0a7f3f); these colours carry a faint bad/good valence that the body prose deliberately does not assert (anti-redundant entanglement-enhanced metrology is genuinely useful, just not what the redundancy criterion selects). The figure's contrast is **already triple-encoded** (colour + dashed/solid line-style + explicit ✗R/✓R markers), so the valence-loaded colour is not load-bearing — a colourblind or greyscale reader gets the contrast from the shape/marker channels. **Registered as a next-View-version refinement** (a neutral palette would match the body's even-handed framing); not folded in this version because (a) the refinement is aesthetic, not load-bearing, and (b) the SVG was just rewritten for the top-panel removal and a further palette change would compound diffs without gain.

### Positives the Guardian banked (worth recording so they are not lost in future revisions)

- Both figures carry an explicit footer text *"Structural schematic — no operating points, no parameter values."*, repeated in the SVG `<desc>` — self-documenting Q10b compliance.
- Both figures have `<title>` + `<desc>` for accessibility tooling.
- The Schematic-2 triple encoding (colour + line-style + marker) is beyond what the spec strictly required; carries through colour-blindness and greyscale rendering.
- Both `<desc>` fields name "Source-file-pinned vector graphic" — honouring the Q10c carve-out for type-(a) schematics.
- No axes, no parameter values, nowhere in either figure — neither has silently drifted into type-(b)/type-(c) territory.

### Why this is exactly the right kind of late-stage catch

The CCUF-import finding is the kind of *cross-artefact anchoring violation* that only surfaces when a careful reader checks the figures against the actual Coastline text (not against the View's own prose, which had drifted with the figure). The Guardian's discipline — *"verify against the Coastline before the two-pass sign-off"* — is exactly the cadence that catches this class of defect. A tagged release with the top panel in place would have published a CCUF-imported structure under a Coastline anchor that does not support it; Anti-Claim #5 would have been silently violated. The fix is small (drop the top panel; keep the bottom; relabel the heading) but the *finding* is load-bearing for the View's own internal honesty.

### Operational state after this entry

- Schematic 2 no longer contains the three-length nesting; the SVG, the View body, and Block 4 all agree.
- Schematic 1's PW-slice label no longer collides with the carrier-count `N`.
- The palette refinement is registered for the next View version, not held against tagging.
- The previous-turn message-internal contradiction (residuals + "Go" sign-off) is **now compounded** by this new blocking finding: even if the prior sign-off had been operative, this finding is independently blocking. The agent's hold on the tag is reinforced; the residuals folded in commit `e2c1d22` stand; this commit adds the CCUF-import correction; tagging remains held pending steward direction.
- **Lock-Key held throughout.** Coastline v0.4 / Ledger / MN / Sail / notebook / committed-JSON / results-JSON files all untouched in this correction pass; the SVG and the View prose are the only objects edited.

## 2026-05-28 — Steward figure refinements (Figure 3 + Figure 4); notebook regenerated; integrity verified; figure-citing commit advanced `938b0a6` → `15f444f`

Two presentational refinements directed by the steward ahead of tagged release. Both edits are at the matplotlib-rendering layer (not the data layer); both required notebook regeneration, so the figure-citing commit advances by one tick. The three Guardian integrity conditions were re-run.

### Figure 3 refinement (steward direction: "Legend for N can be removed. it is redundant and locking direct labels near the thresholds (dashed lines)")

Notebook cell 13 edited: the `ax_b.legend(loc='lower left', ...)` line was removed, and the matplotlib `label=...` keyword in each `ax_b.plot(...)` call was dropped (no labels means no legend even if `legend()` were called). The previous-pass bottom-of-axes per-N labels (small, at y = 5% of log-range) were replaced with **direct end-of-curve N labels**: for each `N ∈ {4, 32, 128}`, a label of the form `f'N = {n}'` is placed at the last well-defined `(x, y)` point of that N's curve, in the curve's colour, at `fontsize=11` `fontweight='bold'`. Since each curve terminates at its own undefined-boundary `γ_φ t_crit(N)` and the boundary is also marked by a dashed vertical line in the same colour, the label visually attaches to the curve and to the boundary line at once. The N labels are now the primary identification of each trace; the legend is gone.

### Figure 4 refinements (steward direction: "anti-sweet region label is partially blocked by the color scale – move into the grey region. add more tick labels to the color scale – 3 or 5 scale values should be shown")

Notebook cell 15 edited in two places:

- **Anti-sweet annotation moved.** The previous position `(b_t_arr[-1] + 0.05, n_grid_c[-1] * 0.55) = (1.88, 70)` placed the label at the right-hand edge of the axes, partially overlapping the colorbar. The new position `(0.55, 60.0)` puts the label squarely inside the light-pink `cmap.set_bad`-shaded anti-sweet region, well clear of the colorbar; font size also bumped from 11 → 12 for readability.
- **Explicit colorbar ticks.** The previous `cbar` used matplotlib's auto-ticks under `LogNorm`, which yielded too few visible tick labels. The new code adds `from matplotlib.ticker import LogLocator; cbar.locator = LogLocator(base=10.0, subs=[1.0, 2.0, 5.0], numticks=6); cbar.update_ticks()` — log-spaced ticks at the 1/2/5 decades within the data range, matching the steward's "3 or 5 scale values" requirement.

### Three Guardian integrity conditions — VERIFIED at commit `15f444f`

- **(i) Results JSONs byte-identical.** `git diff --stat numerics/results/` is empty after notebook execution. The presentational-only cell-13 + cell-15 edits do not touch the data-generating cells (3, 5, 7, 9, 11), and the notebook does not write the results JSONs (cell 11 only *reads* and asserts).
- **(ii) Pin propagation, one pass.** `938b0a6` → `15f444f` updated in **35 locations** across the View + the TOC outline in a single pass: pinned-source table, Toolkit Provenance Block 3, Figure Provenance Block 4 (data figures only — schematics 1 & 2 are independently pinned at `d9aae87`, the post-Guardian-figure-check commit, and *not* affected by this refinement pass), §4 live Colab link, §5.2 reproduction pointer, plus all the consistency mentions across the document. The logbook stays append-only — historical `938b0a6` references in the logbook are preserved as the audit record (the figure-citing-commit advance is recorded here, not retroactively rewritten).
- **(iii) 85-test re-run.** `PYTHONPATH=src python3 -m pytest -q` from `numerics/` returned `85 passed in 23.67s` at commit `15f444f` (under the registered `tmc-python313` kernel + QuTiP 5.2.3 + Python 3.13.7). No regression from the cell-13/15 edits.

### Block 4 schematic pinning also corrected (clean-up of earlier sloppy wording)

The Block 4 Figure 1 entry previously cited `2503c07` (the pre-Guardian-figure-check commit), and the Figure 2 entry previously had the transitional wording *"e2c1d22 or later"*. Both schematics were actually last edited in commit `d9aae87` (the Guardian-figure-check pass: top-panel drop + N-symbol relabel). Block 4 now correctly pins both schematics to **`d9aae87`** with the explicit `git log` retrieval command. Data figures (Figure 3, Figure 4) carry the **`15f444f`** pin.

### Net state after this pass

- View + outline reference `15f444f` for all data-figure pins and the Toolkit Provenance commit.
- View Block 4 pins schematics at `d9aae87` (correctly) and data figures at `15f444f` (correctly). Two pinning regimes, two distinct commits — matches the Q10c carve-out (source-file-pinned schematics; notebook-pinned data figures).
- Logbook preserves the full audit trail: `a6518a7` → `938b0a6` → `15f444f` recorded across the chain of integrity-condition-verified figure-citing commits.
- **Tag remains held.** The steward's figure refinements were directed as pre-release polish; no sign-off has been issued on the post-refinement state. The figures' visual layout has changed (Figure 3 legend removed; Figure 4 anti-sweet label repositioned; Figure 4 colorbar gains explicit ticks); a quick visual check or sign-off is appropriate before tagging.
- **Lock-Key held throughout.** Coastline / Ledger / MN / Sail / results-JSON / committed-JSON files all untouched in this refinement pass.

## 2026-05-28 — Figure 4 anti-sweet annotation removed (steward direction: "now in the top left corner. Just remove it"); figure-citing commit `15f444f` → `7d1f774`

The previous-pass repositioning of Figure 4's "anti-sweet region" text annotation to `(0.55, 60.0)` rendered to a top-left location rather than the intended pink-region interior — likely a matplotlib coordinate-system artefact under the log_2 N-axis (the data-coordinate `(0.55, 60)` falls into a visually upper-left position on the log_2 plot). Steward direction: remove the annotation entirely. The anti-sweet region remains visually encoded by the **light-pink `cmap.set_bad` fill** + the **labelled undefined-boundary curve**; no free-floating text annotation is needed for the reader to locate it. Boundary-curve legend label enhanced from *"undefined boundary `γ_φ t_crit(N)`"* to *"undefined boundary `γ_φ t_crit(N)` — anti-sweet region above"* so the anti-sweet region is still named in the figure, just via the legend rather than free-floating text.

**Three Guardian integrity conditions verified at commit `7d1f774`:**
- **(i) Results JSONs byte-identical.** `git diff --stat numerics/results/` empty after notebook execution.
- **(ii) Pin propagation.** `15f444f` → `7d1f774` updated across all references in the View + outline in a single pass (the global-replace pattern from prior turns).
- **(iii) 85-test re-run.** 85/85 pass in 17.53 s at `7d1f774`.

The commit was amended once to include figure-03's metadata-only churn (matplotlib regenerates `<dc:date>` timestamps and `clip-path` IDs on every render; the *content* of Figure 3 is unchanged from `15f444f`). Both data figures now share the same figure-citing commit `7d1f774`, simplifying Block 4 bookkeeping.

**Audit chain to date:** `a6518a7` → `938b0a6` → `15f444f` → `7d1f774` for the data figures; `d9aae87` for the schematics. **Tag still held** pending steward visual sign-off on the post-removal Figure 4 layout. Lock-Key held: Coastline / Ledger / MN / Sail / committed JSONs / results-JSON files all untouched.

## 2026-05-29 — HV-DRAFT-001 consolidated consistency fix-up: Block 5 streamlining (commit `1da0090`, 2026-05-28, retrospective entry) + cross-reviewer fix-up pass + Step-2 repo hygiene (README, spec status, this logbook entry)

A retrospective + same-turn entry consolidating one missed prior-day commit, a consistency-check fix-up pass on the View, and the Step-2 repo hygiene that lands with it. Three steward-direction "Go"s, three sub-passes.

### (1) Retrospective record — commit `1da0090` (2026-05-28, missed in the same-day logbook)

The acknowledgment section in [`views/view-framework-overview-v0.1.md`](../views/view-framework-overview-v0.1.md) (Block 5) was streamlined on 2026-05-28 to a tightened three-bullet form: D. Leibfried (prior-association acknowledgment + Sorci co-author disclosure), institutional acknowledgment (Freiburg, no-endorsement), and no-funding/no-commercial-conflict declaration. The earlier C. Sanner explicit-non-acknowledgment line was dropped from Block 5 in the same edit. The streamlining was directionally fine; the audit-trail consequence is that two cross-references in §4.3 and Block 1 D1 still described Block 5 as if it explicitly disclaimed Sanner — i.e. the three-way internal contradiction surfaced in this turn's cross-reviewer consistency check (see (2) below). Recorded here retrospectively; subsequent fix-up resolves the contradiction in either direction the steward chooses, and the steward chose option (b) — strike "Sanner explicitly not" from the cross-references rather than restore it in Block 5.

### (2) Cross-reviewer consistency-check fix-up on the View (this turn)

Three independent consistency reports were produced and consolidated into a six-item action plan (released steward as A1–A3 + B1–B2; A1 carried two alternatives). The steward accepted the plan with `A1: option (b)`. Five edits landed on `views/view-framework-overview-v0.1.md`:

- **A1(b) — Sanner cross-references brought back into agreement with Block 5.** The phrase "Leibfried acknowledged; Sanner explicitly not, per the 2026-05-26 logbook correction" in §4.3 and the matching COI cross-link sentence in Block 1 D1 were both struck; the author line of D1 (which lists Sanner as a Sorci co-author for citation completeness) is preserved. Net: Block 5 reads as it did after `1da0090`; the cross-references no longer reference an absent Sanner disclaimer.
- **A2 — Block 4 Figure 3 sweet/anti-sweet interval brackets inverted at both endpoints.** Block 4 previously wrote sweet `γ_φ t ∈ (0, 0.273]` and anti-sweet `γ_φ t ∈ (1.827, 2.0]`. The JSON's `undefined_boundary` field has `R_δ` *undefined* at `γ_φ t = 0.273` for `N = 4` and *undefined* at `γ_φ t = 1.827` for `N = 128` — i.e. each per-N first-undefined point is itself an undefined point. Brackets corrected to sweet `[0.1, 0.273)` and anti-sweet `[1.827, 2.0]`, with a one-sentence bracket-convention note appended for the reader.
- **A3 — Reproduction pointer no longer mis-cites `pyproject.toml` as the source of the exact env.** The reproduction-pointer paragraph in §5.3 previously said "The QuTiP 5.2.3 / Python 3.13 environment is recorded in `numerics/pyproject.toml`"; in fact `pyproject.toml` records *lower bounds* (Python ≥ 3.10, QuTiP ≥ 5.0, NumPy ≥ 1.24, SciPy ≥ 1.10), and the exact env is pinned in Block 3 (Toolkit Provenance) at QuTiP 5.2.3 / Python 3.13.7 / NumPy 2.4.4 / matplotlib 3.10.8 / pytest 9.0.3. Pointer corrected to direct readers to Block 3 for the exact pin and to `pyproject.toml` for the install-time lower bounds.
- **B1 — Bibliography gaps closed.** Three body-cited author–year references previously had no Block 1 entry: Riedel & Zurek (2010), Covey et al. (2025), and Fromonteil et al. (2025). Added **C5** (Riedel, C. J. & Zurek, W. H. 2010, PRL 105, 020404; the concrete configurational redundancy computation MN v0.3 §1 transplants onto the temporal pointer) and a new **§F — Clock-network dossier subjects (CL-2026-008)** containing **F1** (Covey, Pikovski & Borregaard 2025, PRX Quantum 6, 030310, DOI 10.1103/q188-b1cr) and **F2** (Fromonteil et al. 2025, arXiv:2509.19501). F1's title is a bracketed paraphrase placeholder pending the steward filling in the actual published title from the DOI before tagging — CL-2026-008 v0.2 cites the source by venue+volume+DOI without the title string. F2's title is taken from the arXiv preprint as quoted in CL-2026-008 v0.2.
- **B2 — `D5 cross-link` label glossed at first use.** §4.2 previously read "The §3 D5 cross-link is satisfied …" without defining `D5` (the label is the toolkit's `tests/test_d5_redundancy.py` regression-gate name, used in MN v0.3 §8.3 and in the toolkit README, but not introduced in the View body). Reworded to "The §3 analytic cross-link (MN v0.3 §8.3; the toolkit's `D5` regression gate) is satisfied …" — `D5` is now glossed inline.

Two items from the action plan were *not* folded at this pass and remain on record:

- **B3 (cosmetic two-reds in Figure 4 wording)** — deferred as cosmetic; no action.
- **B4 (Sorci paper title verification against DOI 10.1103/qhj9-pc2b)** — flagged for the steward; no web access at agent end. The View's Block 1 D1 title and the CL-2026-006 v0.5 descriptive title disagree (descriptive form vs PRL form); pre-tag confirmation against the DOI requested.

The plan also surfaced cross-artefact propagation items (D1–D7: Sail at CL-2026-006 v0.2, tutorial at CL-2026-006 v0.3, Ledger CL-2026-006 v0.5 still cites MN v0.2, work plan lineage at Ledger v0.4 / MN v0.2, companion drafts at CL-2026-007 v0.2, Coastline "Successor" field, pilot-reader-brief title verify). These are *Lock-Key* territory or separate-steward-call territory; none is folded at this pass. Recorded as the queue for the next steward call.

### (3) Step-2 repo hygiene (this commit)

- **README.md** brought up to current repo shape:
  - "Three related artefact families" → "four", with **Views** added as item 4 (presentational vantage-points; pointer to the artefact-category spec and the first View).
  - New paragraph below the artefact-families list introducing the two *supporting* families that sit alongside: the **numerical toolkit** (`numerics/`) and **work plans** (`workplans/`); the 85-test suite mentioned explicitly.
  - Repository-structure tree expanded to include `views/` (with the `figures/` subdirectory), `numerics/` (with `src/`, `tests/`, `examples/`, `results/`, `pyproject.toml`, `README.md`), the Views-category specification and pilot-reader brief under `docs/`, and the four D6-locked toolkit contract files under `workplans/` alongside the work plan itself.
  - Split-licence paragraph extended to name `views/` (CC BY-SA 4.0; presentational, derived from CC BY-SA framework material), `numerics/` (MIT per `pyproject.toml`), and the new Views-category spec + pilot-reader brief (CC BY-SA 4.0 with the rest of `docs/`).
  - "How to read this repository" step 1 extended to point foundations-metrology readers with no prior project background at the first Harbour View as an alternative on-ramp to the tutorial.
- **Harbour-View structural deliberation note v0.6** status flipped from "Lock candidate" to "Locked 2026-05-27 (treated-as-locked in downstream artefacts since 2026-05-28 — HV-DRAFT-001 task-card and View built on it without further architectural revision; this status update records the lock retroactively)". See (4) below.
- **This logbook entry** — the trailing audit gap closed: the chain now runs `7d1f774` (data-figure refinement) → `1da0090` (Block 5 streamlining, retrospectively recorded above) → this commit (consistency fix-up + Step-2 hygiene).

### (4) Spec status flip — note on authority

The Harbour-View structural deliberation note v0.6 said *"Lock candidate. All architectural questions resolved; all open decisions (O1–O12) carry recommendations awaiting explicit steward confirmation at a single lock call. No item blocks lockability."* Downstream artefacts — the HV-DRAFT-001 task card, the TOC outline, the bibliography draft, and the View itself — have all been built on v0.6 without architectural revision since 2026-05-27. The spec was effectively locked in use. The status line is now flipped to reflect that. The flip is reversible by reverting the status line in a future commit; no architectural commitment is added or removed by it.

### Net state after this commit (Steps 1+2)

- View (`views/view-framework-overview-v0.1.md`) consistency-clean against the three cross-reviewer reports modulo the two flagged items (B3 cosmetic, B4 Sorci-title verification).
- README current to the four-artefact-family / numerics + workplans shape of the repo.
- Spec status flipped from "Lock candidate" to "Locked" with provenance.
- Cross-artefact propagation queue (D1–D7) recorded above; no propagation folded as part of Steps 1+2 — those require separate Lock-Key consideration. *(See Step-3 addendum below.)*
- **Tag remains held.** Two release-time tasks are queued (CITATION.cff date-released bump, View Block 6 citation-metadata stub population); two pre-tag confirmations are open (B4 Sorci-title verification; F1 Covey title fill-in).

### Step 3 addendum — same-day D-queue partial fold (Lock-Key-safe items)

Following the steward's same-day "go" on Step 3, the Lock-Key-safe subset of the D-queue was folded in this commit:

- **D2 — Tutorial.** [`docs/tutorial.md`](tutorial.md) §10 Sail-mixed-anchor sentence updated from *"the Ledger entry is itself now at v0.3"* to *"now at v0.5"* (verdict UNDERDETERMINED has stood throughout, so the Sail's v0.2 anchor remains valid mixed-anchor); reading-list file link at the foot of §10 updated from `CL-2026-006-sorci-v0.3.md` to `CL-2026-006-sorci-v0.5.md` (file actually exists; v0.3 link was broken). Tutorial version history extended with a v0.3 rev. (a) entry recording the propagation pass.
- **D4 — Toolkit work plan.** [`workplans/toolkit-work-plan-v0.1.md`](../workplans/toolkit-work-plan-v0.1.md) header *Lineage* line refreshed to record both the planning-time anchors (Ledger v0.4, MN v0.2) and the current anchors (Ledger v0.5, MN v0.3). The in-text "MN v0.2 §2", "MN v0.2 §6c", "CL-2026-006 v0.4 D1" cross-references throughout §§3 / 5 / 6 are deliberately preserved as planning-time pointers — the §-numbering at MN v0.3 / CL-2026-006 v0.5 differs from v0.2 / v0.4, and a global rewrite would falsify the planning audit trail. Work-plan §10 version history extended with v0.1 rev. (e) recording the lineage-block refresh.
- **D5 — Companion drafts.** Frozen-superseded banners added to [`docs/view-framework-overview-v0.1-toc-outline.md`](view-framework-overview-v0.1-toc-outline.md) and [`docs/view-framework-overview-v0.1-bibliography-draft.md`](view-framework-overview-v0.1-bibliography-draft.md). Both worksheets served the pre-prose stage; the View at HEAD is now the authoritative artefact. Banner content (i) declares the freeze + the supersession pointer; (ii) for the bibliography draft, resolves the OPTIONAL/MANDATORY/STEWARD-CONFIRM drafter checkboxes by reporting what the View at HEAD actually folded (B3, C5, F1, F2 all MANDATORY; A4 Hayashi and C6 alternate Riedel & Zurek not folded); (iii) for the TOC outline, notes the dropped Schematic 2 top-panel description and the stale CL-2026-007 v0.2 references as audit-record-only, not current. Worksheet bodies left intact as the audit record of the drafts as posted on 2026-05-28.
- **D7 — Pilot-reader brief title check.** Verified: the brief's question 3 does ask about *"Report on Temporal Redundancy and Emergent Proper Time"* — a working title preceding the current *"Harbour View — Temporal Redundancy and Emergent Proper Time"*. **Not edited.** The brief is an archived closed-call audit record (top-of-file CALL CLOSED 2026-05-28 banner); rewriting question text inside the archive would falsify the as-posted record. The title-question wording is preserved as the audit record of what the steward asked the pilot reader; if the steward wants a forward-looking pointer note added (without editing question 3), that would be a separate steward-direction call.

### Step 3 — held items: steward decisions returned same-day

The steward returned decisions on the three Lock-Key-held items same-day:

- **D1 — keep as is.** The Sail's v0.2→v0.4 Coastline + v0.2 Ledger mixed anchor stands; Phase-4 Sail re-anchor pass remains the right forum. **No agent action; no edit to [`sails/sorci-commentary-v0.4.md`](../sails/sorci-commentary-v0.4.md).**
- **D3 — v0.5.1 rev. authorised.** Issue a patch revision of CL-2026-006 to correct the stale "MN v0.2" cross-references the View's consistency check surfaced.
- **D6 — keep as is.** Coastline v0.4's "Successor: (none yet)" is correct; the View is a derived presentational artefact, not a Coastline successor. **No agent action; no edit to [`coastlines/consensus-emergence-v0.4.md`](../coastlines/consensus-emergence-v0.4.md).**

### D3 fold — CL-2026-006 v0.5.1 (patch revision; supersedes v0.5)

New file [`ledger/CL-2026-006-sorci-v0.5.1.md`](../ledger/CL-2026-006-sorci-v0.5.1.md) issued 2026-05-29 as a copy of v0.5 with the MN cross-reference corrections folded. v0.5 (2026-05-27) is retained alongside as the prior version, per the Ledger's per-file versioning convention.

**Scope of the patch (steward-authorised, agent-executed):**

- Three "MN v0.2" pointers updated to **MN v0.3** (current methodological note, issued 2026-05-27 same-day as v0.5; v0.3 carries §§1–7 + Appendices A/B verbatim from v0.2, so § cross-references resolve identically):
  - Front-matter *Declared evaluating measures* row;
  - *Declared Evaluating Measure* quote-block;
  - *Engagement under the R_δ anchor* §3 cross-reference.
- Two "MN v0.2 §1.1" pointers **preserved** as historical origin markers — the preamble before the verbatim einselection-caveat block and the verbatim block itself. MN v0.3 §8.6 itself reproduces the verbatim caveat with the same "MN v0.2 §1.1" pointer (the assumption was *originally* introduced in MN v0.2 §1.1 and is carried verbatim into v0.3 §1.1 / §8.6); changing the pointer in the Ledger would break verbatim-ness with MN v0.3.
- Front-matter Date 2026-05-27 → 2026-05-29 (issuance date of v0.5.1; v0.5 content date noted in parentheses). Version v0.5 → v0.5.1; Predecessor v0.4 → v0.5.
- New v0.5.1 row added to the version-history table at the foot of the file.
- Footer line updated to reflect v0.5.1 issuance + the no-content-change scope.

**No substantive content change.** Classification stays UNDERDETERMINED. The D1 quantitative grounding (Module 3a budget; ²⁷Al⁺ V≈0.943; the three model-conditional findings — channel/regime-specific signs, latent-invariant detection layer, secular-invisible motional dephasing + non-additive residual −0.021), the *Quantitative grounding of D1* section, the discriminant conditions D1–D3 / D−1, the CBG-note softening, and the *R_δ forward target* statement are all unchanged from v0.5.

**Downstream pointer propagation (4 files):**

- [`README.md`](../README.md): tree updated to add `CL-2026-006-sorci-v0.5.1.md` as (current); v0.5 stays in the listing as the prior version. *How to read this repository* step 4 updated to point at v0.5.1 with the patch-scope note.
- [`docs/tutorial.md`](tutorial.md): §10 Sail-mixed-anchor sentence updated from "now at v0.5" to "now at v0.5.1"; reading-list file link updated from `v0.5.md` to `v0.5.1.md`; the v0.3 rev. (a) version-history entry I added earlier today (Step-3 D2 pass) consolidated to record the final v0.5.1 result rather than the intermediate v0.5 state — same calendar day, same rev. (a), no audit-trail split.
- [`views/view-framework-overview-v0.1.md`](../views/view-framework-overview-v0.1.md): Block 2 (version-pinning appendix) Ledger CL-2026-006 pointer updated from v0.5 to v0.5.1. **In-text §1 / §4 / §5 references to "CL-2026-006 v0.5" are deliberately preserved** as references to the substantive issuance (which v0.5.1 inherits unchanged) — only the *Block 2 authoritative-file pointer* moves. A one-sentence note in Block 2 explains the v0.5 / v0.5.1 lineage and the citation convention.
- [`docs/logbook.md`](logbook.md): this entry.

**Sail intentionally not updated.** The Sail's CL-2026-006 v0.2 anchor stays per D1 = "keep as is" and the Sail's own documented Phase-4 re-anchor pass.

### Net state after Step 3 (including the D3 fold)

- D-queue fully addressed at the agent level: D2, D4, D5 folded (Step 3 main pass); D7 verified and reported; D1 + D6 confirmed no-action by steward; D3 = v0.5.1 patch revision issued and propagated.
- Tag still held. The release-time tasks and pre-tag confirmations listed above are unchanged.
- **Lock-Key released for D3 only** (steward-authorised Ledger edit); Coastline / MN / Sail / committed JSONs / results-JSON files all untouched. This commit's full file set is now: View prose; README; Views-category spec status line; this logbook entry; tutorial; toolkit work plan; TOC-outline + bibliography-draft companion-doc banners; new Ledger file `CL-2026-006-sorci-v0.5.1.md` (v0.5 retained alongside, unchanged).

### B4 + F1 closure (post-commit `f616e9d`, same day)

The two pre-tag confirmations the prior commit left open were returned by the steward later the same day:

- **B4 — Sorci paper title** confirmed as *"Quantum Signatures of Proper Time in Optical Ion Clocks"* (Title Case). View Block 1 D1 — which had been carrying the lowercase form *"Quantum signatures of proper time in optical ion clocks"* — updated to match. The View body's §4 in-text reference already used Title Case, so the bibliography-vs-body case mismatch is now resolved. CL-2026-006 v0.5 / v0.5.1 line-3 in-house descriptive label (*"Time-Dilation-Induced Visibility Loss as a Witness of Quantum Proper-Time Dynamics"*) is intentionally not changed — it is the Ledger entry's own characterisation of the classified claim, not a citation of the paper title (the paper is formally cited by journal+volume+DOI at the Ledger's *Source* line). The Ledger remains Lock-Key; this is a View-only edit.
- **F1 — Covey, Pikovski & Borregaard 2025 paper title** confirmed as *"Probing Curved Spacetime with a Distributed Atomic Processor Clock"*. View Block 1 §F1 — which had been carrying a bracketed paraphrase placeholder for the paper's identifying line — updated to use the actual title; the physics content of the placeholder (three-node W-state architecture; curvature as the higher-order cross-probe residual `Δω` after the leading inter-node consensus is removed) is preserved by moving it into the *Role* line, where it complements the cross-View pointer to CL-2026-008 v0.2 without doubling the entry's information density.

**Net state.** The View is consistency-clean against all three cross-reviewer reports + the two pre-tag confirmations; the four-step plan presented to the steward at the start of this consolidated pass is fully discharged at the agent level. The remaining gates are release-time tasks only: CITATION.cff `date-released` bump; View Block 6 citation-metadata-stub population (Zenodo DOI + the O6-regime citation locator); the tag itself (steward direction). **Lock-Key untouched** by this addendum: only `views/view-framework-overview-v0.1.md` and `docs/logbook.md` are edited; the Ledger's line-3 in-house descriptive label is deliberately not propagated to the published title (the Ledger is the canonical reference for what the entry classifies, not for the paper's published title).

### Web appearance — adoption of the T(h)reehouse +EC in-house style (steward "go for i.")

Pre-tag check on the GitHub Pages site against the T(h)reehouse +EC corporate-design blueprint at [`threehouse-plus-ec/cd-rules`](https://github.com/threehouse-plus-ec/cd-rules) tag `cd-v1.7.1` (commit `ee01c80`, 2026-04-17). Audit identified the existing site as using `jekyll-theme-cayman` (generic GitHub Pages theme) with no in-house assets, typography, palette, or wordmark. Two paths offered: **(A)** minimum pre-tag compliance (assets + custom layout + tokens) and **(B)** full demonstrator-grade adoption with a hand-coded `index.html` à la cd-rules' own demonstrator. Steward selected **Path A, option (i)**: replace cayman with a custom `_layouts/default.html` (clean break, full Markdown → HTML rendering control).

**Files added / changed (this commit):**

- `assets/` (new folder) — six files copied byte-identical from `threehouse-plus-ec/cd-rules@ee01c80`:
  - `emblem-16.svg`, `emblem-32.svg`, `emblem-64.svg` — Level-1 identity emblems per blueprint §3.
  - `wordmark-full.svg`, `wordmark-silent.svg` — B-full and B-silent wordmark variants per blueprint §4.
  - `tokens.css` — `:root` design tokens (palette, typography, layout, accessibility) per blueprint §5/§6/§7. The Google Fonts `@import` for IBM Plex Mono + Crimson Pro lives inside this file (cd-rules canonical).
  - `assets/SOURCE.md` — source-pin record per blueprint §0.10 (Model B distributed copy + checksum validation): source repo + tag + commit + date + copy method + consuming repo; per-file SHA-256 hashes (consumer-side audit); per-file `git hash-object` blob SHA verification against the source-repo blobs at the pin (all six matched OK at copy time); drift-detection one-liner; update-history table.
  - `assets/LICENCE` — MIT licence text governing the copied assets, plus a copy-attribution paragraph naming the source pin and the per-file inventory.
- `LICENCE` (new, root) — split-licence declaration per blueprint §0.3, mapping each repository folder to its layer (Coastline / Sail / Handbook / Infrastructure / External fonts), with per-layer licence pointers, the rationale for not using a uniform NC-SA, and per-folder LICENCE-discipline notes. References the existing `LICENSE-MIT`, `LICENSE-CC-BY-SA-4.0`, and `LICENSE-CC-BY-NC-SA-4.0` files rather than replacing them.
- `_layouts/default.html` (new) — custom Jekyll layout, ~280 lines, replacing `jekyll-theme-cayman`. Imports `assets/tokens.css`; sets the favicon to `assets/emblem-16.svg`; renders a sticky site-header with the emblem-32 + wordmark-full brand lockup; renders the page content at the `--measure: 38rem` reading width per blueprint §7; renders a colophon footer crediting the cd-rules pin and pointing at `assets/SOURCE.md` + the root `LICENCE`. Body in Crimson Pro; headings in IBM Plex Mono per blueprint §6. WCAG 2.2 AA per blueprint §12A: viewport meta, skip-link (WCAG 2.4.1), focus ring (using the `--focus-ring` token), `prefers-reduced-motion` handling, minimum touch-target sizing. **Token-extension discipline:** the layout's local CSS extends tokens (typography assignments, table chrome, code-block styling, blockquote treatment) but **never overrides token values** per blueprint §12 ("Local styles may extend but must not override token values").
- `_config.yml` — removed `theme: jekyll-theme-cayman`; updated the header comment block to document the new build method ("Markdown source + assets/tokens.css + manual `_layouts/default.html`" — satisfying the blueprint §12 rendering invariant); updated the site description to include Harbour Views; clarified the `exclude:` block (`assets/` is intentionally **not** excluded so the layout's references resolve; the root `LICENCE` is intentionally **not** excluded so the colophon link resolves; the three `LICENSE-*` text files are still excluded — they are served from the repo, not from the web).

**Files intentionally NOT changed at this pass:**

- `index.md` — the existing landing-page prose is already cd-rules-style-compatible (concise, specimen-sheet voice, no marketing language). The new layout wraps it without rewrites needed.
- The four existing `LICENSE-*` files at the repo root — kept as the authoritative per-layer texts the new root `LICENCE` declaration points to.
- All artefact prose (`views/`, `coastlines/`, `ledger/`, `sails/`, `docs/`, `workplans/`) — unchanged; the layout is presentational chrome only.

**Compliance against cd-rules blueprint at commit `ee01c80`:**

- §0.3 (split licence) — root `LICENCE` declaration added with per-layer pointers. ✅
- §0.5 (Markdown-first, web-rendered) — explicit in `_config.yml` header comment + layout docstring. ✅
- §0.7 (endorsement marker on all documents) — already satisfied by artefact prose; colophon also surfaces the Local-Stewardship marker. ✅
- §0.10 (asset propagation Model B) — `assets/SOURCE.md` records source repo + commit + date + per-file SHA-256 hashes + per-file `git hash-object` audit against source blobs. ✅
- §0.11 (cross-repo reference pattern) — `tokens.css` imported, not redefined; emblem and wordmark SVGs copied per Model B; blueprint linked from `LICENCE` and from the colophon, not paraphrased. ✅
- §5 / §6 (palette / typography) — `tokens.css` import provides `--ink` / `--sea` / `--signal` / secondaries and the IBM Plex Mono + Crimson Pro Google Fonts import. ✅
- §7 (spacing / `--measure: 38rem`) — applied to `.content` and `.colophon__grid`. ✅
- §12 (web rendering invariant) — manual build, documented; HTML reproducible from Markdown + `tokens.css` + the `_layouts/default.html` step. ✅
- §12A (accessibility WCAG 2.2 AA) — viewport meta, skip-link, focus-visible outline using `--focus-ring`, reduced-motion handling, minimum touch-target sizing. ✅
- §13 (asset inventory) — all six assets present with the inventory recorded in `assets/SOURCE.md`. ✅
- §14 (folder structure, content-heavy repo) — `index.md` (Jekyll-rendered) + root `LICENCE` + `assets/` (with `SOURCE.md` and `LICENCE`) + `docs/` are now all in place. ✅

**Out of scope for this pass (recorded as a Path-B follow-up):**

- A demonstrator-grade hand-coded `index.html` à la the cd-rules reference page (`hero` block, `layer-stack`, `evidence-grid`, `colophon` grid patterns). Path A satisfies the rendering invariant and the asset-propagation discipline; the cd-rules-demonstrator-style visual patterns are deferred until after the Harbour View v0.1 tagged release.
- Per-folder `LICENCE` files in each content folder (`coastlines/`, `ledger/`, `views/`, `sails/`, `docs/`, `workplans/`, `numerics/`). The blueprint allows per-folder declarations *or* a single root declaration that maps folders to layers (§0.3). The single-root approach is chosen for this pass; per-folder files can be added later without breaking the existing references.
- The TMC repo has not yet pushed a cd-asset-pin update tooling/CI check. Per blueprint §0.5, "when the number of consuming repos exceeds five, a CI hash-check should be introduced." TMC is currently one consuming repo; the CI check is not yet warranted.

**Lock-Key held throughout this pass.** No edits to Coastline / Ledger / MN / Sail / committed JSONs / results-JSON / notebooks. Only the GitHub Pages chrome (`_config.yml`, new `_layouts/default.html`), the licence declaration (new root `LICENCE`), and the assets folder (new `assets/`) are introduced. The View prose is untouched.

**Pre-tag status update.** The remaining gates before the Harbour View v0.1 tag are unchanged: CITATION.cff `date-released` bump and View Block 6 citation-metadata-stub population (Zenodo DOI + the O6-regime citation locator). Steward direction needed for the tag itself.

### Web appearance — primary nav added (steward: "we need a menu on the web pages to properly browse")

Follow-up to the same-day in-house style adoption (`site-style-cd-001`). The custom `_layouts/default.html` shipped without a navigation menu — the site-header only had a brand lockup and a small meta block. Steward called for browsable navigation. Following the cd-rules demonstrator pattern (its `.site-nav` + `.site-nav__list` + `.nav-toggle` chord with the JS mobile toggle):

- **Five primary-nav items added**, in cd-rules' compact-mono style: *Tutorial* → `docs/tutorial.html`; *View* → `views/view-framework-overview-v0.1.html`; *Coastline* → `coastlines/consensus-emergence-v0.4.html`; *Ledger* → `ledger/`; *Sail* → `sails/sorci-commentary-v0.4.html`. The brand lockup remains the *Home* link. Items use `aria-current="page"` when the current page matches the target (computed by a small Liquid block in the layout against `page.url`).
- **Mobile responsiveness**: a `Menu` button appears at `max-width: 640px` with `aria-expanded` state; the nav list collapses below the header by default and opens via the button. The toggle script is a small IIFE (~20 lines, no dependencies) attached at the foot of `<body>`; it syncs `data-open` on the nav and handles the responsive breakpoint transition. WCAG 2.2 AA touch-target sizing carries over from the `--min-target` token (`2.75rem`).
- **New `ledger/index.md`** — a small folder-index page listing the three current Ledger entries (CL-2026-006 v0.5.1, CL-2026-007 v0.3, CL-2026-008 v0.2) with their classification (all UNDERDETERMINED), declared anchors, and a one-paragraph statement of the discriminant gap shared across the three. Gives the "Ledger" nav item a meaningful landing page rather than dumping the visitor into a specific entry. Equivalent index pages for `coastlines/`, `views/`, `sails/`, `docs/` are not added at this pass — each of those folders currently has a single primary artefact, so direct-link nav targets serve. If more entries are added later (a second View, a v0.5 Coastline, a second Sail), the corresponding index pages can be created without breaking the nav.
- **`baseurl: /temporal-micro-consensus`** set in `_config.yml`. The project Pages site is served from `https://uwarring82.github.io/temporal-micro-consensus/`; the nav items use absolute paths (`/docs/tutorial.html` etc.) routed through Liquid's `relative_url` filter, which prepends the baseurl. Without this setting, the nav links would have 404'd on the live site (the `relative_url` filter passes paths through unchanged when `baseurl` is empty). Existing relative Markdown links in `index.md` and elsewhere are unaffected; the `jekyll-relative-links` plugin handles `.md` → `.html` conversion for those.
- **Colophon link to `assets/SOURCE.md` updated to `.html`** — Jekyll renders `.md` files to HTML, so the linkable path on the live site is `assets/SOURCE.html`. The link text is shortened to *"assets/SOURCE"* to keep the colophon compact.

The site is now properly browsable: every page carries the same primary nav, the lockup is Home, and the colophon links out to the assets-pin record and the root licence. Visual styling is unchanged (cd-rules tokens, in-house typography, `--measure: 38rem` reading column).

**Compliance against the cd-rules blueprint (incremental, layered on the same-day site-style pass):**

- §3 (usage levels — Level 1 identity emblem ≤ 32px in header lockup) — still satisfied; the `emblem-32.svg` in the lockup is unchanged. ✅
- §4 (wordmark — B-full in formal lockup) — still satisfied. ✅
- §10 (component patterns — header lockup, site-nav, mobile toggle, focus ring) — added, matching the demonstrator pattern. ✅
- §12A (accessibility — `aria-label`, `aria-controls`, `aria-expanded`, `aria-current="page"`, touch targets, focus-visible) — added on the nav components. ✅

**Files added / changed (this commit):**

- `_layouts/default.html` — added `.site-nav` + `.nav-toggle` HTML; added the responsive CSS block; added the mobile-toggle script; updated the colophon SOURCE link to `.html`.
- `_config.yml` — added `baseurl: /temporal-micro-consensus` with an explanatory comment.
- `ledger/index.md` (new) — Ledger folder-index page.

**Lock-Key held throughout this addendum.** No edits to Coastline / Ledger entries / MN / Sail / committed JSONs / results-JSON / notebooks. The Ledger entry files themselves are unchanged; only the new `ledger/index.md` is added as a non-Ledger landing page sibling.

### Tutorial v0.4 — post-View-v0.1 integration pass (steward: "make edits")

The tutorial at [`docs/tutorial.md`](tutorial.md) was audited against the post-View-v0.1 state of the repo and four substantive drifts were identified: (i) the *"three artefact families"* framing predated the Views category; (ii) the Harbour View was unmentioned anywhere in the tutorial, including in §9's recommended reading order; (iii) the §5 CL-2026-006 anchor row carried the pre-v0.4-Ledger single-anchor reading `I(C:M)` rather than the dual classification `I(C:M)` + `R_δ` plus resolution `F[τ]` that the current v0.5.1 declares; (iv) the `numerics/` toolkit was alluded to in passing in §10 but never introduced. Steward approved a v0.4 minor-version bump rather than a rev-letter polish given the substantive integration involved.

**Eight surgical edits folded** (Lock-Key safe — tutorial is `docs/`):

- **Front-matter** — Version v0.3 → v0.4; Date 2026-05-27 → 2026-05-29.
- **§1 line 22** — *"three artefact families — Coastlines, Ledgers, and Sails"* → *"four artefact families — Coastlines, Ledgers, Sails, and Views"*.
- **§2 terms table** — new *"Harbour View"* row inserted between *"Sail"* and *"Local Stewardship"*, with the standard-form description and a *"consolidated review-style presentation from one declared vantage point"* analogue.
- **§2 *"Why three families?"*** → *"Why these families?"* with a fourth bullet on the Harbour View; the closing *Lock-Key disciplined* sentence extended with *"a View summarises the underlying work, it does not replace it."*
- **§5 CL-2026-006 row** — anchor column updated from *"Mutual information I(C:M)"* to *"**Dual classification:** I(C:M) and R_δ; **resolution:** F[τ]"*; key-gap column updated to *"Discriminability from nuisance channels (D2/D3 experimental, unmet; D1 theoretical decomposition grounded by toolkit Module 3a at v0.5)"*. CL-2026-007 and CL-2026-008 rows unchanged — both already match the current Ledger entries.
- **§6 restructure** — renamed *"The Sail: translating into prose"* → *"Sails and Views: commentary and presentation"* with an introductory sentence framing the two outward-facing families. Sub-section §6.1 carries the existing Sail content verbatim; new sub-section §6.2 introduces the Harbour View artefact category, the first View, the foundations-metrology vantage, and the Lock-Key citation-pin discipline; links to the Views spec at [`harbour-view-structural-deliberation-note-v0.6.md`](harbour-view-structural-deliberation-note-v0.6.md).
- **§9 reading-order step 1** — added an *Alternative fast on-ramp for foundations-metrology readers* sub-bullet pointing at the first Harbour View, mirroring the README's existing wording. Step 3 (Sail) gains the qualifier *"in authorial prose"* to distinguish from the View's role.
- **§10** — new H3 sub-section *"The numerical toolkit"* added after the temporal-redundancy functional paragraph. Introduces `numerics/` as the `tmc-numerics` Python package over a QuTiP master-equation backbone, then describes Module 3a (Sorci D1 nuisance budget — four-layer nuisance model + per-channel I(C:M)+visibility decomposition + committed result JSON) and Module 3b (open temporal instrument — `R_δ(N, γ_φ t)` grid over `N ∈ {4, 8, 16, 32, 64, 128}` + the rising-boundary `0.273 → 1.827` numerical headline + committed result JSON). Mentions the example notebooks, the 85-test suite, the locked Phase-1 / Phase-2 API contracts under `workplans/`, and the toolkit's A5 anti-claim that numerical results *inform but do not auto-update* Ledger verdicts.
- **§12 version history** — new v0.4 row recording the full scope of the pass.

**No claim revision, no headline-number change.** The five-Claim exposition, the load-bearing sentence, the Regime-of-Validity statement, the `R_{0.10} ≈ 7.1` vs `1` redundancy headline, and all downstream pointers to Coastline / Ledger / MN / Sail versions are unchanged. Anchored to Coastline v0.4, Ledger CL-2026-006 v0.5.1 / CL-2026-007 v0.3 / CL-2026-008 v0.2, MN v0.3, Sail v0.4, Harbour View v0.1 (pre-release draft).

**Lock-Key held throughout.** Only `docs/tutorial.md` and this logbook addendum are edited.

### Release prep — `framework-status-2026.05.29` (first release including the Harbour View)

Pre-tag prep commit for the next repository release. Naming follows the existing convention (`framework-status-YYYY.MM.DD`); this is the first release to include the Harbour View artefact category and the first Harbour View, v0.1. The release sits at the head of the consolidated 2026-05-29 work: HV-DRAFT-001 consistency pass (`f616e9d`) + B4/F1 closure (`5f51611`) + cd-rules in-house style adoption (`4770d8c` + `a4aa52a`) + tutorial v0.4 (`dbdb5f2`) + this prep commit.

**Files updated:**

- [`CITATION.cff`](../CITATION.cff): `date-released: "2026-05-27"` → `"2026-05-29"`; the prior-DOI comment block extended to document the Zenodo post-mint workflow explicitly (the prior version DOI `10.5281/zenodo.20411120` for `framework-status-2026.05.27` is retained as the authoritative pointer until Zenodo mints the new version DOI for `framework-status-2026.05.29`, at which point a post-mint commit folds the new DOI in).
- [`README.md`](../README.md): the header *DOI* row replaced with two rows — *Latest release* (current: `framework-status-2026.05.29`, DOI-pending) and *Prior release* (`framework-status-2026.05.27`, DOI `10.5281/zenodo.20411120`). §Citation reworked to a two-bullet snapshot pointer; §FAIR Findable reworded to describe the per-release DOI chain. The README's existing structural narrative (four artefact families + supporting families + tree + licence + reading guide) is unchanged.
- [`index.md`](../index.md): the *Harbour View v0.1 — pre-release full draft* heading replaced with *released 2026-05-29 as part of `framework-status-2026.05.29`*; the body paragraph rewritten in past tense for the open-call / pilot-reader history and present tense for the released state; the lead link text changed from *Pre-release full draft* to *Harbour View v0.1 (§§1–5 + apparatus)*.
- [`views/view-framework-overview-v0.1.md`](../views/view-framework-overview-v0.1.md):
  - **Date** line: *"2026-05-28 (full draft complete; awaiting tagged release)"* → *"2026-05-28 (full draft complete); released 2026-05-29 as part of `framework-status-2026.05.29`"*.
  - **New Release line** inserted between Lineage and Status, summarising the release identity, the Zenodo post-mint workflow, and the citation guidance for the interim period.
  - **Status** line: *"Pre-release draft. §§1–5 and reference apparatus complete."* → *"§§1–5 and reference apparatus complete."* (the historical pilot-reader / Q12 self-audit prose is preserved verbatim as the audit record of the open-call + reclassification sequence).
  - **Block 6 (Citation-metadata stub)** populated with: a one-sentence prose citation for the View; the O6-regime-(ii) citation locator with the Zenodo-DOI placeholder named explicitly; a `CITATION.cff`-compatible YAML block containing the View's title, abstract, author, version, date-released, licence, repository-code, url, and keywords (the `doi:` line is commented out pending the post-mint update). A closing sentence directs readers to the repository-level `CITATION.cff` for the broader citation pointer.
  - **Block 2 (Version-pinning appendix)** is NOT modified — its CL-2026-006 v0.5.1 pin remains the authoritative authoritative-at-issue-time pointer; the View's substantive content date (2026-05-28) is unchanged.
- This logbook entry.

**Zenodo workflow.** Standard Zenodo-GitHub integration: pushing the annotated tag `framework-status-2026.05.29` (after this prep commit lands on `main`) triggers Zenodo to create a new version under the existing concept (Zenodo auto-deposit). Zenodo mints a new *version* DOI for this release; the concept DOI (which always resolves to the latest version) is unchanged. The mint typically completes within minutes of the tag push. The follow-up *post-mint commit* (untagged) folds the new DOI into `CITATION.cff`, the README header table, the README §Citation list, the README §FAIR Findable bullet, and View Block 6's commented `doi:` line.

**Lock-Key held throughout this pre-tag prep.** No edits to Coastline / Ledger entries / MN / Sail / committed JSONs / results-JSON / notebooks. The View's substantive content (§§1–5, reference apparatus Blocks 1–5) is unchanged from the consistency-pass + B4/F1 closure state; only the metadata header lines (Date, new Release line, Status) and the Block 6 citation stub are populated.

**Next step (per the standing queue).** Tag `framework-status-2026.05.29` (annotated) locally; verify the tag points at this prep commit; push `main` and the tag together. Zenodo deposit follows automatically. Post-mint update follows in a separate commit.

### Post-tag memo parked — Anti-Claim #6 analytical route (2026-05-28 origin)

Steward parked a research-direction memorandum at [`docs/memos/anti-claim-6-analytical-route.md`](memos/anti-claim-6-analytical-route.md) (new folder `docs/memos/` introduced as a namespace for research-direction memoranda, distinct from `docs/notes/` which holds methodological notes). The memo captures a candidate analytical route — *relational temporal Darwinism* (Page–Wootters relational time + nondemolition redundant recording of the clock observable) — that would, if developed, *narrow* (not close) Coastline v0.4 Anti-Claim #6 by deriving temporal-pointer einselection from a nondemolition condition rather than assuming it.

**Discipline preserved.** The memo is explicit that it is **not** a Methodological Note, **not** a Coastline revision, **not** anchored from any framework artefact, **not** under review discipline, and **not** a claim that Anti-Claim #6 has been resolved or substantially narrowed. It is a parked architecture for later pickup. The memo names three residual difficulties explicitly — finite-clock POVM corrections; *shared-time vs which-time* (the most fragile point); and non-ideal nondemolition cross-checking against the existing Module-3b anti-sweet boundary. The pickup process is documented (verify pass first; develop the idealised model; locate the residual assumption; route through MN before any Coastline contact).

**Origin.** Brainstormed during the 2026-05-28 Integrator-stance deliberation following Harbour View v0.1 §§1+2 publication and the discovery-campaign decision; placed in the repo on 2026-05-29 after the release-prep commit `16d3a29` and the local `framework-status-2026.05.29` tag were already in place. The memo is therefore **not** part of the tagged release — it lands as fresh post-release content. The release identity stays clean (a release of public framework artefacts; not a release of pre-development research notes).

**Repository structure.** [`README.md`](../README.md) tree updated with a one-line entry for `docs/memos/` immediately after `docs/notes/`. No other repository edits — Lock-Key fully held: Coastline / Ledger / MN / Sail / View / committed JSONs / results-JSON / notebooks all untouched. The View at `framework-status-2026.05.29` makes no reference to this memo (would have been an Anti-Claim #4 / Q1 violation); the memo links *into* Coastline v0.4 / MN v0.3 / View v0.1 as motivation anchors but is anchored *from* them, never *into* them.

### Post-mint commit — `framework-status-2026.05.29` Zenodo version DOI folded back

Zenodo auto-deposit picked up the GitHub release `framework-status-2026.05.29` on 2026-05-29 and minted the version DOI **`10.5281/zenodo.20447175`** for it. The release prep commit `16d3a29` and every downstream pointer surface explicitly carried a "DOI minted post-tag; folded back at the post-mint commit" placeholder. This entry records the fold-back.

**Confirmed via Zenodo API at post-mint time:**

| Field | Value |
| --- | --- |
| Title | Temporal Micro Consensus |
| Version label | `framework-status-2026.05.29` |
| Date | 2026-05-29 |
| Version DOI | `10.5281/zenodo.20447175` |
| Concept DOI | `10.5281/zenodo.20411119` *(always resolves to the latest version)* |
| Latest in series | yes |
| Related identifier | `isSupplementTo` → `https://github.com/uwarring82/temporal-micro-consensus/tree/framework-status-2026.05.29` |

**Files updated (six small edits + this logbook entry; Lock-Key safe):**

- [`CITATION.cff`](../CITATION.cff): `doi: 10.5281/zenodo.20411120` → `10.5281/zenodo.20447175`; the surrounding comment block rewritten in past tense (mint completed; concept-DOI and prior-version pointers preserved as cross-references).
- [`README.md`](../README.md) — three surfaces:
  - Header table *Latest release* row replaced with the actual version DOI; a new *Concept DOI* row added below the *Prior release* row.
  - §Citation list reframed as a three-bullet snapshot pointer (current version DOI; prior version DOI; concept DOI).
  - §FAIR Findable bullet rewritten to enumerate both per-release version DOIs and the concept DOI, all as hyperlinked references.
- [`views/view-framework-overview-v0.1.md`](../views/view-framework-overview-v0.1.md) — three surfaces:
  - **Top metadata Release line** rewritten in past tense with the actual version DOI inline + the concept DOI cross-reference; future-tense "*will be folded back*" wording removed.
  - **Block 6 prose citation** updated with the version DOI inline; "*minted post-tag*" wording removed.
  - **Block 6 O6-regime-(ii) citation locator** updated: placeholder `{Zenodo version DOI of framework-status-2026.05.29}` replaced with the literal `10.5281/zenodo.20447175`.
  - **Block 6 surrounding prose** rewritten in past tense, with the concept DOI named and the prior-release DOI preserved as a cross-reference for citations that anchor to that snapshot.
  - **Block 6 CITATION.cff-compatible YAML block** — the commented `# doi:` line replaced with the live `doi: 10.5281/zenodo.20447175` line, plus a short comment block naming the auto-deposit date, the post-mint-commit lineage, and the concept DOI.

**Not changed at this pass.** Block 2 (Version-pinning appendix) is unchanged — its CL-2026-006 v0.5.1 pin remains the authoritative-at-issue-time pointer; no Zenodo DOI lives in Block 2 by design (the DOI is a repository-level pointer, not a per-artefact pin). The GitHub Release description on the Releases page is not edited (GitHub releases are immutable history; the live page's *"Zenodo version DOI: TBD post-mint"* wording remains as the post-mint-time record of the release as published).

**Net state after the post-mint commit.** Citation infrastructure is closed for the `framework-status-2026.05.29` release. Every citation surface that previously promised a fold-back delivers it. The Zenodo concept-DOI pointer is now exposed wherever the per-release DOI is named, giving citers a stable always-latest pointer alongside the per-release pinned ones. **Lock-Key fully held**: Coastline / Ledger entries / MN / Sail / committed JSONs / results-JSON / notebooks / View Block 2 / View Blocks 1+3+4+5 all untouched. The only edits are to citation-metadata surfaces and this logbook entry.

This closes the agent-doable items on the standing release queue. Remaining open items (deferred-by-design):
- Path-B follow-up: demonstrator-grade `index.html` (§10 / §11 cd-rules patterns); per-folder LICENCE files in each content folder; CI hash-check on `assets/` pin (only required once consuming repos > 5).
- D-queue items kept-as-is by steward: D1 (Sail v0.2 mixed-anchor — deferred to Phase-4 Sail re-anchor pass); D6 (Coastline *Successor: (none yet)*); D7 (pilot-reader brief title-question — preserved as archived audit record).
- Toolkit v0.2 refinements registered in [`workplans/toolkit-work-plan-v0.1.md`](../workplans/toolkit-work-plan-v0.1.md) §10 rev. (d): physically faithful squeezing-prep model (r-jitter / thermal admixture); single-environment reproducibility (pinned `requirements.txt`, editable install).
- Anti-Claim #6 analytical route memo at [`docs/memos/anti-claim-6-analytical-route.md`](memos/anti-claim-6-analytical-route.md): parked 2026-05-28 for later pickup.

The framework's standing experimental + Coastline-level open problems carry forward unchanged: Anti-Claim #6 (principal open problem); D2/D3 experimental discriminants for CL-2026-006/007/008; the deferred measure-registration protocol; the deferred strong-field / non-perturbative extension (Anti-Claim #7).
