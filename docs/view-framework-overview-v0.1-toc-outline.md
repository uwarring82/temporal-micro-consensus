# First Harbour View — Table of Contents & Per-Section Outline (pre-prose, awaiting steward sign-off)

**Document type:** TOC + per-section bullet outline for the first Harbour View. Companion to **Task Card HV-DRAFT-001** ([harbour-view-task-card-HV-DRAFT-001.md](harbour-view-task-card-HV-DRAFT-001.md)); first move per the card. **Not** the View itself; **not** prose.

**Spec anchor:** [Harbour View — Structural Deliberation Note v0.6](harbour-view-structural-deliberation-note-v0.6.md) (lock candidate). All structural choices below derive from §6 (first-instance) and §§4 Q1–Q12 (category rules). No structural improvisation.

**Target file (post-lock):** `views/view-framework-overview-v0.1.md` (placement per O2, pending steward confirmation; this TOC outline stays in `docs/` as a pre-View artefact).

**Status:** **LOCKED 2026-05-28** (Guardian sign-off held by steward; one substantive amendment — the Q10c carve-out for schematics — recorded as a first-instance figure-handling note below). No prose begins until §12 next-moves 5–7 cleared (Jekyll versioned-subpath check → figure generation → commented-bibliography assembly).

---

## Lock Record (2026-05-28, Guardian sign-off)

Sign-off reviewer: U. Warring (Guardian stance held by steward), per task-card HV-DRAFT-001 reviewer role.

**One substantive finding from drafting-contact:** Q10c ("every figure is a static render generated by a version-pinned notebook") is over-broad for **type-(a) schematics**, which encode no results — they illustrate structure. Forcing a conceptual diagram through a QuTiP notebook uses a notebook as a vector-graphics editor: ceremony without reproducibility payoff. This is a spec defect, not an outline defect.

**First-instance figure-handling note (binding for this View):** **type-(a) schematics are authored vector sources (SVG or TikZ) committed to the repository and pinned by source-file commit hash in Figure Provenance**; **type-(b) and type-(c) data figures remain notebook-pinned per Q10c at commit `938b0a6`**. The provenance discipline (commit-pinning) is fully satisfied for both regimes; the tool differs because the artefact differs.

**Spec amendment deferred (not v0.7):** a one-line Q10c carve-out — *"data figures notebook-pinned; schematics source-file-pinned"* — to be folded into the spec at its next genuine revision, whenever one is independently warranted. No spec v0.7 spun for this finding alone. Discipline: amend from task-contact, not from another review pass.

**Seven open items resolved** (detail at the foot under "Resolutions at TOC-lock call"). Briefly: (1) schematics handled as authored vector sources per the carve-out above; (2) type-(b) N-set = {4, 32, 128} confirmed; (3) notebook caveat surfacing — verify first, update only if absent; (4) pilot reader — steward's call, foundations-metrology physicist outside the Freiburg–NIST orbit, mandatory no omission; (5) concordance paragraph N/A confirmed; (6) post-lock placement `views/` confirmed; (7) cross-View pointers — drafter's choice at prose stage, Guardian leans §5.

**Cleared next step:** spec §12 next-moves 5 → 6 → 7 in order: Jekyll versioned-subpath check → figure generation (source-file-pinned schematics + notebook-pinned data figures, with notebook caveat verification) → commented-bibliography assembly. Prose for §1 + §2 begins **after** that; pilot-reader review on §1 + §2 before deeper-section drafting (Scout EC-5).

**Lock-Key held throughout:** Coastline / Ledger / MN / Sail untouched.

---

---

## Declared structural cap (§6 first-instance + §6 category rule)

- Hard cap: **5 sections + reference apparatus**.
- Expected total length: ~12–15 pages markdown source.
- TOC-lock discipline (§10 scope-creep mitigation): once locked, addition or deletion of any section requires explicit steward + Guardian sign-off before prose drafting resumes. Pilot-reader feedback identifying a section-level need is an admissible TOC-revision trigger (Scout EC-5).

## Declared primary audience (O8, Q2)

- **Foundations-metrology physicists.**
- Secondary audiences (philosophers of physics; methodologists) acknowledged as future-View candidates; not co-equal readers of this View.
- Task-card hard-rule 4 strengthening: the **executive overview must be followable by a foundations-metrology physicist with no Harbour background**.

## Declared structural pattern (§5 first-instance recommendation)

- **Hybrid: Option B (layered) + Option D (question-driven entry).**
  - Executive overview structured along question-driven lines.
  - Deeper sections structured along sequential narrative (framework → methodology → exemplar → open problems).

## Declared artefact-type sentence (§11 rec 1, to appear on the View's first page)

- Placeholder text (to be lightly polished at prose stage, not invented now): *"This document is a Harbour View — a presentational overview of several Harbour artefacts (Coastline, Methodological Note, Breakwater Ledger entry, and toolkit results), issued under Local Stewardship. It is neither a peer-reviewed publication nor an authoritative version of any framework artefact it presents."*

## Concordance paragraph (Q5 / Scout EC-4)

- **Not applicable** — this is the first View on this body of work; no predecessor or sibling View exists at issuance.
- Confirm with steward at TOC-lock call (open item 5).

## Pinned source set (task-card "You are given")

| Artefact | Pinned version | Path |
| --- | --- | --- |
| Coastline | v0.4 | [`coastlines/consensus-emergence-v0.4.md`](../coastlines/consensus-emergence-v0.4.md) |
| Methodological Note | v0.3 | [`docs/notes/temporal-redundancy-functional-v0.3.md`](notes/temporal-redundancy-functional-v0.3.md) |
| Ledger entry | CL-2026-006 v0.5 | [`ledger/CL-2026-006-sorci-v0.5.md`](../ledger/CL-2026-006-sorci-v0.5.md) |
| Toolkit results (Module 3a) | `v0.1.json` | [`numerics/results/sorci_nuisance_budget_v0.1.json`](../numerics/results/sorci_nuisance_budget_v0.1.json) |
| Toolkit results (Module 3b) | `v0.1.json` | [`numerics/results/open_instrument_redundancy_v0.1.json`](../numerics/results/open_instrument_redundancy_v0.1.json) |
| Generating notebooks | commit **`938b0a6`** | [`numerics/examples/sorci_module.ipynb`](../numerics/examples/sorci_module.ipynb), [`numerics/examples/open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) |

---

## Section 1 — Executive overview (≤1 page)

**Purpose (§6):** question-driven entry; headline finding; one sentence on what this document is; one schematic figure. **Pitched at task-card hard-rule 4 audience standard** (foundations-metrology physicist, no Harbour background).

**Bullet outline (no prose):**

- Opening question (Option-D entry): *"Is classical proper time emergent from many microscopic carriers, and if so, what would it take to demonstrate it?"* Anchors: Coastline v0.4 Load-Bearing Sentence; Claim II; Anti-Claim #6.
- One-bullet headline finding: the configurational→temporal transplant has been **sharpened, not discharged** — Anti-Claim #6 remains open; the open temporal instrument (MN v0.3 §8) is a candidate exemplar *within the einselection regime*, not a resolution.
  - Anchors: Coastline v0.4 Anti-Claim #6; CL-2026-006 v0.5 (verdict **UNDERDETERMINED**, Lock-Key / toolkit anti-claim A5); MN v0.3 §8.6 (einselection caveat).
- One-sentence document-type declaration (Q12 + §11 rec 1) — see "Declared artefact-type sentence" above.
- One-sentence Endorsement Marker (Q7): Local Stewardship; constraint-only / no-parity posture toward external coastlines (A4).
- One-sentence pointer to the artefact taxonomy with the brief glossary located in the reference apparatus (Q12 two-register rule).
- Concordance paragraph: **omitted** (first View; no predecessor or sibling). Steward confirm.

**Figure (Type (a) — schematic; §6 figure plan):**

- Content: a framework-orienting schematic — many microscopic carriers (`F_1, ..., F_N`) sharing a single effective proper-time variable; Page–Wootters bipartite channel marked as a sub-structure within the multipartite-redundancy picture.
- No operating-point claim → **Q10b sweet/anti-sweet rule does not bind** (carries no operating-point claim).
- **Source: authored vector source (SVG), commit-pinned by source-file hash** (per the Q10c carve-out for schematics, recorded in the Lock Record). Not notebook-pinned.
- **Source file:** [`views/figures/schematic-01-framework-orientation.svg`](../views/figures/schematic-01-framework-orientation.svg) (authored 2026-05-28; source-file commit hash recorded in Figure Provenance at View-issue time per the Q10c carve-out — schematic source-file pinning is the per-file commit at which the SVG last changed, retrieved from `git log` at View issuance).

**Hard-rule discipline applicable to this section:**

- Q1 (no new claims): headline is a restatement of pinned-source content; the schematic illustrates structure asserted by the Coastline.
- Q12 (externally readable): no Harbour-only jargon in body; taxonomy glossary in apparatus; specialist terms ("redundancy," "consensus," "carrier") defined at first use.
- A7 (figure honesty): figure carries no operating-point claim; clean against Q10b.
- A8 (not pedagogical): one-sentence anchors only; no first-principles content.

**Audience test cue:** §5 pilot-reader review hits this section first (§5 first-instance requirement).

---

## Section 2 — Framework (~3 pages)

**Purpose (§6):** what the Coastline claims and what it does *not*; seven anti-claims preserved; one schematic figure.

**Bullet outline (no prose):**

- Coastline anchor: **v0.4** (cited at section opening per Q8).
- Claims I–V presented at presentational depth only — each summarised in plain language with cross-reference to the Coastline section. No new wording, no new boundaries.
  - **Claim I** — problem statement (proper time as classical observable on a microscopic-carrier basis).
  - **Claim II** — emergence criterion (multipartite-redundancy structure; configurational→temporal transplant **noted explicitly as analogy lacking a worked exemplar**, per the v0.3 status-of-specialisation paragraph).
  - **Claim III** — failure mode (loss of consensus; nuisance-discrimination load-bearing).
  - **Claim IV** — operational anchors (the measure-registration protocol is **deferred**; the resolution-anchor-vs-classification-anchor distinction is registered, per the 2026-05-26 design note + roadmap).
  - **Claim V** — positioning vs Page–Wootters (bipartite-correlation channel within the consensus picture) and quantum Darwinism (configurational analogue; temporal application is the specialisation).
- **Seven anti-claims preserved** (Coastline v0.4 §"Anti-Claims" #1–7; verbatim or with explicit cross-reference per §11 rec 4):
  1. Consensus is not a fundamental theory of time emergence.
  2. Microscopic carriers do not literally "decide"; the term *decision* is stipulatively excluded.
  3. Page–Wootters and quantum Darwinism are not deficient; both are assumed as background.
  4. No parity with externally-validated physical frameworks (A4 mirror).
  5. No CCUF bridge committed at v0.4.
  6. **Quantum-Darwinism emergence criteria are not established for temporal records** — principal open problem; transplant is an analogy without a worked exemplar.
  7. No strong-field / non-perturbative / quantum-gravitational regimes; scope is SR-and-weak-field (post-Newtonian) only, under the Regime-of-Validity provisos.
- "**Multipartite ≠ redundant**" terminology note (Claim II / Claim V positioning, v0.3+) — surfaced here because it carries forward into §3 (no entanglement → R_δ degenerates) and §4 (the open temporal instrument exhibits redundant temporal structure, distinct from anti-redundant entanglement-enhanced clock networks).
- **GR-regime boundary statement (v0.4):** scope extended to SR-and-weak-field (post-Newtonian); strong-field deferred.
  - Anchors per **Q11b** (relationship claim → external constraint): Coastline v0.4 External Constraints item 5 (GR weak-field) + Anti-Claim #7 + Regime-of-Validity section. No anchor → delete sentence.
- One-bullet pointer to Ledger entries that classify external work *against* this framework: CL-2026-006 v0.5 (Sorci et al.), CL-2026-007 v0.2 (Smith & Ahmadi), CL-2026-008 v0.2 (clock-network class). Presentational only — evidence the framework has been operationalised; no new claims (open item 7 — confirm scope of this pointer).

**Figure (Type (a) — schematic; §6 figure plan):**

- Content: comparison-geometry / framework-structure schematic. The L_source / L_apparatus / L_comparison separation (Q10a vocabulary) over many carriers; Page–Wootters bipartite slice marked as a sub-structure; multipartite ≠ redundant illustrated by contrasting (i) anti-redundant entanglement-enhanced state and (ii) redundant consensus state.
- No operating-point claim → Q10b does not bind.
- **Source: authored vector source (SVG), commit-pinned by source-file hash** (per the Q10c carve-out for schematics, recorded in the Lock Record). Not notebook-pinned.
- **Source file:** [`views/figures/schematic-02-comparison-geometry.svg`](../views/figures/schematic-02-comparison-geometry.svg) (authored 2026-05-28; source-file commit hash recorded in Figure Provenance at View-issue time per the Q10c carve-out — schematic source-file pinning is the per-file commit at which the SVG last changed, retrieved from `git log` at View issuance).

**Hard-rule discipline applicable to this section:**

- Q1: every claim, anti-claim, and the GR-boundary clause cites Coastline v0.4 verbatim or by section.
- **Q11b**: GR-regime sentence MUST anchor to External Constraints item 5 + Anti-Claim #7 + Regime of Validity. Same rule applies to any "consistent with quantum mechanics / quantum information theory" phrasing — anchor or delete.
- A7: each anti-claim presented at exactly the depth the Coastline asserts; no softening, no rewording.
- Q12: specialist terms ("redundancy," "consensus," "Page–Wootters correlation," "quantum Darwinism," "weak-field GR," "post-Newtonian") briefly defined at first use; no Harbour-only shorthand.

**Audience test cue:** pilot reader (§5) also reads this section before deeper sections drafted.

---

## Section 3 — Methodology (~3 pages)

**Purpose (§6):** the temporal-redundancy functional; einselection caveat preserved; one signal-to-noise figure showing **both** favourable and noise-dominated regions.

**Bullet outline (no prose):**

- MN anchor: **v0.3** (cited at section opening per Q8); §§1–7 (theoretical core, byte-identical to v0.2) + §8 (numerical companion) framed as the methodological instrument.
- **The temporal-redundancy functional `R_δ(N, ε)`** — what it measures (presentational, no derivation):
  - Object: the configurational-Darwinism `R_δ` redundancy count, transplanted to a *temporal* axis, at redundancy threshold `1−δ` across `N` carriers.
  - The four-way coincidence (trace-distance / Helstrom / projective / Holevo) underpinning the per-carrier accessibility `ε` (MN v0.3 §8.1).
- **The dynamical bridge `ε(γ_φ, t) = (1 − e^{−γ_φ t}) / 2`** from a single-carrier pure-dephasing master equation (MN v0.3 §8.1) — what it does, what it depends on (the secular pure-dephasing model).
- **Analytic poles from MN §2** (carried verbatim from v0.2; byte-identical in v0.3) — presentational note: where `R_δ` is well-defined vs undefined; the boundary structure that the §8 numerics reproduce.
- **§8.4 *redundancy-lost-without-entanglement* structural point** — with no entanglement structure, `R_δ` degenerates; multipartiteness alone is not redundancy (Claim II terminology note carried into the methodology).
- **§8.5 match to arXiv:2509.17775** (Sorci-area configurational `R_δ`) — *structural* match (the temporal-side computation now exists), **not numerical**. The configurational/temporal asymmetry closed at the level of computation; experiment still pending. Wording preserved verbatim from MN v0.3 §8.5 per A7.
- **D5 cross-link** (`ε=0.20, N=64 → R_{0.10} = 64/9` exactly) — the analytic vs numerical agreement that validates the bridge; foreshadows §4.
- **Einselection caveat (MN v0.3 §8.6) preserved VERBATIM** — single inline block; identical to the block in CL-2026-006 v0.5; Anti-Claim #6 remains open; the open temporal instrument is a candidate exemplar *within the assumed regime*, not a resolution.
  - This block is one of the View's two verbatim insertions. Source: MN v0.3 §8.6.

**Figure (Type (b) — signal-to-noise at dedicated parameter spots; §6 figure plan; Q10b MANDATORY):**

- Content: `R_δ(γ_φ t)` at fixed `N` (proposed: `N ∈ {4, 32, 128}`, the rising-boundary set from Module 3b; open item 2).
  - **Sweet region:** small `γ_φ t` — `R_δ` well-defined and large; redundancy supported within the einselection regime.
  - **Anti-sweet region:** `γ_φ t` past the first-undefined sweep point (rising with `N` — `0.27` at `N=4` → `1.83` at `N=128` per the committed sweep). Visually demarcated per Q10b (load-bearing — a figure showing only the sweet region is rejected).
- Generator: [`numerics/examples/open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) at commit **`938b0a6`** (toolkit-resident per §6 first-instance rule); static render only (Q10c).
- Figure Provenance entry (Q10f): type (b); generating notebook repository URL + commit `938b0a6`; environment-deps (QuTiP 5.2.3; pinned `requirements.txt` or equivalent); parameter values (`N` set, `γ_φ t` range); explicit sweet vs anti-sweet labelling; live-link fallback statement.

**Hard-rule discipline applicable to this section:**

- Q1: `R_δ` definition, the `ε` bridge, the §8.5 match wording, and the einselection caveat all restate MN v0.3 §8 verbatim or by section cross-reference.
- **Q10b** (sweet AND anti-sweet, load-bearing): the type-(b) figure displays both regions — this is an acceptance gate.
- Q10c (static render from pinned notebook): commit-pinned at `938b0a6`.
- A7: the §8.5 "match to arXiv:2509.17775" wording follows MN v0.3 verbatim — **structural** match, not numerical. No over-confidence.
- A8: no first-principles derivation of the master equation; pointer to MN §8.1 instead.
- Q12: define "redundancy threshold," "trace distance," "Holevo information," "einselection" briefly at first use.

---

## Section 4 — Worked exemplar (~3 pages)

**Purpose (§6):** Module 3a + 3b results; model-conditionality preserved; one parameter-sweep figure (situating the §3 type-(b) operating points in their full landscape) + one live Colab link to the pinned notebook.

**Bullet outline (no prose):**

- Ledger anchor: **CL-2026-006 v0.5** (cited at section opening per Q8). Toolkit anchor: commit **`938b0a6`**, results files [`sorci_nuisance_budget_v0.1.json`](../numerics/results/sorci_nuisance_budget_v0.1.json) (Module 3a) and [`open_instrument_redundancy_v0.1.json`](../numerics/results/open_instrument_redundancy_v0.1.json) (Module 3b).

### §4.1 — Module 3a: the Sorci D1 nuisance budget

- **What it produces:** the *theoretical* D1 nuisance budget — the latent / observed decomposition (`ΔI` vs `ΔL_state` vs `ΔL_obs`), per channel; the Eq. (12)-coefficient validation; the detection-row identity (latent `ΔI = ΔL_state ≡ 0`, observed `ΔL_obs ≈ 0.099`); the ²⁷Al⁺ extrapolation `V ≈ 0.943`.
- **What it does NOT do — and this is the View's central honesty point for §4:** it does **not** discharge D1. Per Ledger CL-2026-006 v0.5 (Lock-Key / toolkit Anti-Claim A5: numerics inform, do **not** auto-update the verdict), the budget **sharpens** D1 (a demand on controlled scaling + explicit channel models) rather than discharging it. **Verdict UNCHANGED — UNDERDETERMINED.** D2 and D3 remain experimental and unmet.
- **Three model-conditional findings preserved (CL-2026-006 v0.5):**
  1. Per-channel signs are **non-uniform** — heating/dephasing *reduce* `I(C:M)`; the depolarising prep-infidelity model *raises* and dominates it.
     - **Toolkit refinement registered (Toolkit Provenance):** the v0.1 symmetric-depolarising model is aggressive for a high-cutoff bosonic mode; a physical squeezing-prep-infidelity model (`r`-jitter / thermal admixture) is registered as a toolkit-v0.2 refinement.
  2. Detection is **latent-invariant** (`ΔI = ΔV_state = 0`, observed-only).
  3. Motional dephasing is **visibility-invisible in the secular model** yet still lowers quantum `I(C:M)` — channels non-additive (wording per the v0.5 fix that removed the v0.4 self-contradiction).
- **CBG-note correction carried (CL-2026-006 v0.5):** CL-2026-005 computes the coherent `K(t)` (non-Markovian), **not** the Markovian D1 budget; the D1 budget is produced by the temporal-micro-consensus numerics toolkit. (Toolkit Q5.)

### §4.2 — Module 3b: the open temporal instrument

- **What it produces:** the `R_δ(N, γ_φ t)` grid + the undefined-boundary table (`γ_φ t_crit` rises with `N`: `0.27` at `N=4` → `1.83` at `N=128`); the `ε` bridge derived; the D5 cross-link validated (`ε=0.20, N=64 → R_{0.10} = 64/9` exactly).
- **Einselection caveat carried verbatim** — same block as §3; **single inline cross-reference here, not a repeat**, to keep §4 within its 3-page cap.
- **Anti-Claim #6 remains open** — Module 3b is a candidate exemplar **within the einselection regime**; not a resolution. Source: MN v0.3 §8.6 + Coastline v0.4 Anti-Claim #6.

### §4.3 — Module-conditionality summary

- One bullet: both modules are model-conditional under the toolkit anti-claims A1–A5; the Toolkit Provenance subsection records the inherited anti-claims and pins (commit `938b0a6`; results-file versions; notebook versions).

**Figure (Type (c) — parameter-space sweep landscape; §6 figure plan):**

- Content: the `R_δ(N, γ_φ t)` heatmap from [`open_instrument_redundancy_v0.1.json`](../numerics/results/open_instrument_redundancy_v0.1.json) (Module 3b), with the undefined-boundary curve marked (rising with `N`) and the **anti-sweet region (above the curve) explicitly demarcated** per Q10b (v0.5 closure: the sweet/anti-sweet rule binds type (c) sweeps that display operating points).
- This figure **situates the §3 type-(b) slices in their full landscape** (per §6 figure-plan wording — "situating the type (b) operating points in context").
- Generator: [`numerics/examples/open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) at commit **`938b0a6`**; static render (Q10c).
- Figure Provenance entry (Q10f): type (c); notebook URL + commit; environment-deps; parameter grids (`N`, `γ_φ t`); explicit anti-sweet demarcation; live-link fallback.

**Live Colab link (Q10d):**

- One in-text link to [`open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) at commit **`938b0a6`**, opened at the pinned commit where Colab supports it.
- **Accessible-regime guardrails (Q10e):** widget bounds confined to the model-valid regime (within the einselection assumption + within the committed sweep grid). The einselection caveat surfaced **inline in the notebook header** at the visitor's first interaction.
  - **Open item 3:** confirm the existing `open_instrument_module.ipynb` already surfaces the caveat at first interaction; if not, a one-pass toolkit-side notebook-header update is required *before* figure regeneration. This is a toolkit-side edit, not a View edit (Lock-Key preserved).
- **Convenience layer only:** the static figure is the citable artefact; link rot is not an erratum; documented fallback ("if this link is unavailable, the notebook at commit `938b0a6` reproduces all figures") appears in Figure Provenance.

**Hard-rule discipline applicable to this section:**

- Q1: every result restates Module 3a/3b committed JSON or the v0.5 Ledger verdict; the verdict-unchanged sentence is the load-bearing honesty statement.
- **Q11b**: any "consistent with Sorci visibility witness" or similar relationship phrasing MUST anchor to **CL-2026-006 v0.5**; no anchor → delete.
- Q10b (sweet AND anti-sweet for a type-(c) sweep displaying operating points): the anti-sweet region above the undefined-boundary curve is mandatory.
- Q10c (static render from pinned notebook): commit-pinned at `938b0a6`.
- Q10d (live link = convenience layer): static figure is the citable artefact.
- Q10e (accessible-regime guardrails): widget bounds + einselection caveat surfaced.
- A7: the **verdict-unchanged** sentence and the three model-conditional findings carry exactly the qualifications of CL-2026-006 v0.5.
- A8: no derivation of the master equations from first principles; pointer to MN v0.3 §8 instead.
- Q12: define "nuisance budget," "mutual information `I(C:M)`," "trace distance," "dephasing rate `γ_φ`," "secular approximation" briefly at first use.

---

## Section 5 — Open problems + invitations (~2 pages)

**Purpose (§6):** what remains open; how to cite; how to reproduce; how to contact the steward; what kinds of engagement are not yet supported.

**Bullet outline (no prose):**

### §5.1 — Open problems (each anchored to pinned source; no new claims)

- **Anti-Claim #6 (principal open problem):** redundant *temporal* records demonstrated, neither configurational nor anti-redundant — within the einselection regime AND beyond it. Anchors: Coastline v0.4 Anti-Claim #6 + Deferred Items first bullet; MN v0.3 §8.6; CL-2026-006 v0.5.
- **Claim IV measure-registration protocol (resolution-anchor vs classification-anchor):** deferred in Coastline v0.4; the 2026-05-26 design note (CL-2026-007 work) is registered in the roadmap. Cited, not restated.
- **D2 and D3 of the Sorci dossier remain experimental and unmet** (CL-2026-006 v0.5).
- **Toolkit-v0.2 refinement registered:** physical squeezing-prep-infidelity model (`r`-jitter / thermal admixture) to replace the v0.1 symmetric-depolarising model.
- **Strong-field / non-perturbative / quantum-gravitational extension** deferred (Coastline v0.4 Deferred Items + Anti-Claim #7).

### §5.2 — Invitations to engage (§11 rec 6)

- **Citation locator** (per O6 regime (ii)): `{Zenodo version DOI} / {file path within release}`. Explicit citation-string placeholder until release tag.
- **Reproduction pointer:** [`numerics/examples/sorci_module.ipynb`](../numerics/examples/sorci_module.ipynb) + [`numerics/examples/open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) at commit `938b0a6`; QuTiP 5.2.3; both notebooks executed end-to-end with zero errors at issuance (Phase-3 gate condition met, logbook 2026-05-27).
- **Contact:** steward direct (U. Warring, Physikalisches Institut, Albert-Ludwigs-Universität Freiburg).

### §5.3 — Kinds of engagement NOT YET supported

- Public issue tracker (held until external engagement warrants).
- Pull requests against framework artefacts (Lock-Key: sharpening routes through the next Coastline / Ledger / MN version, not via the View — Q1 mirror).
- External-circulation discovery campaign (deferred per spec §10).

**No figure for §5.**

**Hard-rule discipline applicable to this section:**

- Q1: open-problem list restates what is already deferred / open in the pinned sources.
- Q11b: any external-relationship phrasing cites a Ledger entry or Coastline section.
- A7: invitations are informational, not promotional ("the View's purpose is to make the work findable and citable... distinct from advocacy").
- Q12: short declarative sentences; no internal labels in body text.

---

## Reference apparatus (no page cap; six blocks per spec §6)

### Block 1 — Commented bibliography (Q11a)

Every external reference carries: **anchor** (DOI / canonical citation / archived URL); **role annotation** from the controlled vocabulary (*external-constraint*, *prior-art*, *method-source*, *contrast-contested*); for *external-constraint* entries, an explicit **constraint-only / no-parity marker** (A4 + Q7 mirror).

Pre-flagged entries (illustrative; full list assembled at the bibliography-assembly step, spec §12 next-move 7):

- Ollivier, Poulin, Zurek (2004, 2005) — *prior-art* (configurational redundancy / environment-as-witness).
- Zurek (quantum Darwinism foundational papers) — *prior-art*.
- Page, Wootters (1983 + descendants) — *prior-art* (bipartite-correlation channel).
- Mendes, Soares-Pinto (2019) — *prior-art*.
- Sorci et al. (2026, *Phys. Rev. Lett.* 136, 163602) — *prior-art* / candidate-witness (also dossier subject, CL-2026-006 v0.5).
- arXiv:2509.17775 (Sorci-area configurational `R_δ`) — *prior-art* / structural-match anchor (MN v0.3 §8.5).
- General relativity — *external-constraint* (constraint-only, no parity).
- Special relativity — *external-constraint* (constraint-only, no parity).
- Quantum mechanics — *external-constraint* (constraint-only, no parity).
- Quantum information theory — *external-constraint* (constraint-only, no parity).
- Thermodynamics — *external-constraint* (constraint-only, no parity).

### Block 2 — Version-pinning appendix

- Coastline v0.4 (`coastlines/consensus-emergence-v0.4.md`).
- MN v0.3 (`docs/notes/temporal-redundancy-functional-v0.3.md`).
- Ledger CL-2026-006 v0.5 (`ledger/CL-2026-006-sorci-v0.5.md`).
- (Cross-reference if surfaced in §2 per open item 7) Ledger CL-2026-007 v0.2; Ledger CL-2026-008 v0.2.

### Block 3 — Toolkit Provenance subsection (Q4, Scout EC-6)

- Committing commit hash: **`938b0a6`**.
- Results files: [`numerics/results/sorci_nuisance_budget_v0.1.json`](../numerics/results/sorci_nuisance_budget_v0.1.json); [`numerics/results/open_instrument_redundancy_v0.1.json`](../numerics/results/open_instrument_redundancy_v0.1.json).
- Notebook files: [`numerics/examples/sorci_module.ipynb`](../numerics/examples/sorci_module.ipynb); [`numerics/examples/open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb).
- Inherited toolkit anti-claims A1–A5 (per toolkit work plan v0.1) reproduced or cited by version.
- Environment: QuTiP 5.2.3; pinned `requirements.txt` (or explicit pinned package list) at commit `938b0a6`.

### Block 4 — Figure Provenance subsection (Q10f)

Per-figure table populated after figure generation (spec §12 next-move 6). Two pinning regimes per the Lock Record Q10c carve-out:

- **Type-(a) schematics — source-file-pinned.** For each schematic: type (a); **source-file path + source-file commit hash** (SVG or TikZ); authoring tool noted; no environment-deps section (no execution required); no sweet/anti-sweet labelling (no operating-point claim); no live-link fallback statement.
- **Type-(b) and type-(c) data figures — notebook-pinned.** For each data figure: type (b/c); generating notebook repository URL + pinned commit `938b0a6`; residency (toolkit-resident, per §6 first-instance rule); environment-deps (QuTiP 5.2.3; pinned `requirements.txt` or equivalent); parameter values / sweep ranges / fixed parameters; explicit sweet vs anti-sweet labelling (Q10b); live-link fallback statement.

### Block 5 — Acknowledgements & COI (Q11c — separate from citations)

- **D. Leibfried — acknowledged** (NIST Boulder 2010–2012; the steward's prior association).
- **C. Sanner — explicitly NOT acknowledged** (per the 2026-05-26 logbook correction; the steward reports no relationship with Sanner; the Sail v0.2 → v0.3 correction propagates here).
- Both Leibfried and Sanner are co-authors of Sorci et al. (2026); the independence-of-commentary consideration for any external circulation therefore concerns the steward's prior association with **Leibfried alone**.
- Institutional acknowledgement: Physikalisches Institut, Albert-Ludwigs-Universität Freiburg.

### Block 6 — Citation-metadata block

- `CITATION.cff`-compatible block at View foot.
- Release-time citation locator string (regime (ii) per O6); fills in at tagged release.

---

## Resolutions at TOC-lock call (Guardian sign-off, 2026-05-28)

1. **Schematic figures (§§1–2): authored vector sources, source-file-pinned.** Q10c is over-broad for type-(a) schematics — it is load-bearing for *data* figures where the figure encodes results, but a category error for conceptual diagrams that encode no results. Type-(a) schematics are therefore **authored vector sources (SVG or TikZ) committed to the repository and pinned by source-file commit hash** in Figure Provenance. The toolkit is not extended with a schematic-generator notebook (over-engineering rejected). Provenance discipline (commit-pinning) is fully satisfied. Type-(b)/(c) data figures unchanged — still notebook-pinned at `938b0a6`. **Spec amendment deferred** (one-line Q10c carve-out for the spec's next genuine revision; not v0.7-spun for this finding alone).
2. **Type-(b) `N`-value set for §3: `N ∈ {4, 32, 128}` confirmed.** The rising-boundary set displays the actual structural finding (`γ_φ t_crit` rising with `N`). A single `N=64` slice would hide that structure.
3. **Notebook caveat surfacing (Q10e): verify, update only if absent.** Real acceptance gate for the Colab live-link only — not for the static figures. Read `open_instrument_module.ipynb` header at the figure-gen step; if the einselection caveat is absent at first interaction, apply a one-pass toolkit-side header update before figure regeneration. Toolkit edit, not View edit; Lock-Key preserved. May be a no-op.
4. **Pilot reader identity: steward's call, constraint affirmed.** Must be a foundations-metrology physicist genuinely **outside the Freiburg–NIST orbit** — because the COI structure runs through Leibfried at NIST, an inside-orbit reader would not be an independent audience test. Mandatory for the first View per §5 first-instance, no omission permitted. Steward selects at the pilot-reader timing per Scout EC-5.
5. **Concordance paragraph: confirmed N/A.** First View; no predecessor or sibling. Omitted.
6. **Post-lock placement: confirmed `views/`** per O2. This TOC outline stays in `docs/` as the pre-View planning artefact.
7. **Cross-View pointers (CL-2026-007 v0.2, CL-2026-008 v0.2): include, one bullet — placement leans §5, drafter's choice at prose stage.** Permissible in §2 (presentational, anchored in Coastline v0.4's own references; not a new claim), but the point — "the framework has been operationalised against external work" — is a *status* point that sits more naturally as a closing bullet in §5.1 or as a §2→§3 transition. Either placement acceptable; Guardian lean is §5; drafter decides at prose stage. Not a blocker.

---

## What this outline does NOT do (task-card "Do not")

- No prose drafted.
- No new claims introduced.
- No definitions or anti-claims added beyond those in the pinned sources.
- No edits to Coastline / MN / Ledger / Sail (Lock-Key preserved).
- No teaching content.
- No improvement to the spec.

## Next steps (post-lock; build status as of 2026-05-28)

Outline locked 2026-05-28. Build status per spec §12:

1. **§12 next-move 5 — Jekyll permalink setup (CHECK DONE 2026-05-28; APPLICATION DEFERRED TO RELEASE).** Read-only check complete: [`_config.yml`](../_config.yml) currently produces flat URLs, not the `/views/framework-overview/v0.1/` versioned-subpath structure Q9b assumes. **Steward-decided resolution:** per-View front-matter `permalink: /views/framework-overview/v0.1/` plus a `views/framework-overview/index.md` redirect stub for the Q9b(b) canonical-current URL. **Application is a release-time task**, not a drafting-time task — does not block figure or bibliography work; applied just before tagged release.
2. **§12 next-move 6 — figure generation.**
   - **6a — notebook einselection-caveat surfacing (Q10e).** ✅ **DONE 2026-05-28 (NO-OP).** Markdown cell 0 of [`numerics/examples/open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) already surfaces the einselection caveat verbatim as a blockquote before any code cell. No toolkit-side header update required. Re-verify at the start of 6c per Guardian's "do as part of 6c" direction.
   - **6b — type-(a) schematics authored, source-file-pinned.** ✅ **DONE 2026-05-28.** [`views/figures/schematic-01-framework-orientation.svg`](../views/figures/schematic-01-framework-orientation.svg) (§1) and [`views/figures/schematic-02-comparison-geometry.svg`](../views/figures/schematic-02-comparison-geometry.svg) (§2) authored and committed. Both XML-valid; both Q10b-clean (no operating-point claims); both Q1-clean (illustrate structure the Coastline already asserts). Source-file commit hash retrieved from `git log` at View-issue time. **Pilot-reader audit point flagged:** colourblind accessibility and greyscale-print viability (standard figure-honesty check at the pilot-reader stage, not a blocker now).
   - **6c — type-(b) and type-(c) data figures.** ✅ **DONE 2026-05-28.** Notebook [`open_instrument_module.ipynb`](../numerics/examples/open_instrument_module.ipynb) extended with two new figure cells; static SVG renders saved at [`views/figures/figure-03-redundancy-functional-snr.svg`](../views/figures/figure-03-redundancy-functional-snr.svg) (§3 type-(b): `R_δ` vs `γ_φ t` at `N ∈ {4, 32, 128}` with sweet AND anti-sweet regions shaded; per-N undefined-boundary dashed verticals; cross-reference labels) and [`views/figures/figure-04-redundancy-landscape.svg`](../views/figures/figure-04-redundancy-landscape.svg) (§4 type-(c): `R_δ(N, γ_φ t)` heatmap with the undefined-boundary curve overlaid and the anti-sweet region demarcated per Q10b v0.5 closure). Extension is **presentational only** (labelling + rendering from the existing `cur` grid, no new computation). **Figure-citing commit advanced from `a6518a7` to `938b0a6`** (this commit; recorded across all five integrity-(ii) pin locations).
   - **6c-integrity (Guardian, 2026-05-28) — ALL THREE CONDITIONS VERIFIED:**
     - **(i) Results JSONs byte-identical** ✅ — `git diff` empty on [`numerics/results/sorci_nuisance_budget_v0.1.json`](../numerics/results/sorci_nuisance_budget_v0.1.json) and [`numerics/results/open_instrument_redundancy_v0.1.json`](../numerics/results/open_instrument_redundancy_v0.1.json) after notebook execution. (Structural: cell 11 only reads + asserts; no JSON-write code path.)
     - **(ii) Pin propagated consistently** ✅ — `a6518a7` → `938b0a6` updated in all five locations in a single pass: pinned-source table, Toolkit Provenance Block 3, Figure Provenance Block 4, §4 live Colab link, §5.2 reproduction pointer.
     - **(iii) Test suite re-run** ✅ — 85/85 tests pass on commit `938b0a6` (QuTiP 5.2.3, Python 3.13.7).
   - **6c-integrity statement** (binding for the next data-figure move): three integrity conditions on any future notebook extension:
     - **(i) Results JSONs must stay byte-identical.** After the notebook extension, run `git diff` on [`numerics/results/sorci_nuisance_budget_v0.1.json`](../numerics/results/sorci_nuisance_budget_v0.1.json) and [`numerics/results/open_instrument_redundancy_v0.1.json`](../numerics/results/open_instrument_redundancy_v0.1.json). Diff MUST be empty. Non-empty diff → the extension has silently altered a computed result; **stop and report**; treat as a substantive change requiring its own scrutiny and a results-version bump, not a figure edit. (Q10c immutability protection.)
     - **(ii) Pin propagates consistently.** The figure-citing commit advances one tick from `938b0a6`; the new hash MUST be updated in ALL FIVE locations in a single pass: pinned-source table (outline + View), Toolkit Provenance (Block 3), Figure Provenance (Block 4), §4 live Colab link, §5.2 reproduction pointer. Stale pin = citation that resolves to pre-figure content.
     - **(iii) Test suite re-run.** Run the 85-test suite on the new commit; must remain green before any figure is cited.
   - **6d — Colab live-link verification.** Pending tagged release; the View's §4 live Colab link must resolve `https://colab.research.google.com/github/uwarring82/temporal-micro-consensus/blob/938b0a6/numerics/examples/open_instrument_module.ipynb` (commit-pinned form, where Colab supports it). Documented fallback already drafted: "if this link is unavailable, the notebook at commit `938b0a6` reproduces all figures." Verification is a release-time visual check; not a blocker.
3. **§12 next-move 7 — commented-bibliography assembly (Q11).** ✅ **DRAFTED 2026-05-28.** Worksheet [`view-framework-overview-v0.1-bibliography-draft.md`](view-framework-overview-v0.1-bibliography-draft.md) with all entries carrying anchor + role annotation + endorsement-scope marker. A1–A5 canonical-text anchors **LOCKED 2026-05-28** (steward call): Nielsen & Chuang 2010 (A1/A4), MTW 1973 (A2/A3), Callen 1985 (A5, conditional on body surfacing). Q11b enforcement checklist appended; two load-bearing audit points pre-identified (§2 GR-regime; §4 Sorci-visibility). Folded into View apparatus at prose stage.
4. **§12 next-move 8 — prose drafting for §1 + §2 only.** Pending 6c completion + pin propagation.
5. **§12 next-move 9 — pilot-reader review of §1 + §2** (steward-selected foundations-metrology physicist outside the Freiburg–NIST orbit; mandatory, no omission). Pilot-reader audit also covers schematic colourblind/greyscale accessibility per 6b. TOC revision admissible per Scout EC-5 if pilot-reader feedback identifies a section-level need.
6. **§12 next-moves 10–12 — remaining-section prose; two-pass steward + Guardian review** (figure-honesty Q10b, bibliography audit Q11, language/readability audit Q12); **tagged release** with O6 citation locator (`{Zenodo version DOI} / views/view-framework-overview-v0.1.md`). Jekyll permalink + redirect stub (next-move 5 resolution) applied here.

---

*End of TOC + per-section outline. **LOCKED 2026-05-28** (Guardian sign-off). Lock-Key held: Coastline / Ledger / MN / Sail untouched.*
